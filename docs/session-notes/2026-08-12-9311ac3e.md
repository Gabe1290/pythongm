# Session notes — 2026-08-12 (`9311ac3e`)

Auto-extracted from the Claude Code transcript. Machine: `gabe-NUC10i7FNK`, project dir: `/home/gabe/projects/pygm2`.
User messages are quoted; the rest is Claude's explanation text
(tool calls and results omitted). Curate freely, then add
`<!-- curated -->` near the top so the extractor won't overwrite it.

---

## [2026-08-12] User

> we have a 2.5 D Doom-like extension. Would it be possible to do something like a Minecraft extension because students are very excited about Minecraft. Are the any copyright issue I should be aware of? Just answer do not implement

### Claude

Short answer: technically yes, legally you need to be careful about how you frame it.

**Feasible?** Very. A voxel-world extension (place/break cubes, chunked grid, simple textures) fits the same folder-extension pattern as `raycast_2_5d` — new room-renderer + actions, no core changes. It's a bigger lift than the raycast work (chunk meshing/culling for performance, inventory UI, world persistence) but nothing exotic.

**Copyright/trademark issues to actually watch:**
- **"Minecraft" the name/logo** — trademarked by Mojang/Microsoft. Don't call it that, don't use their logo or branding, don't market it as Minecraft-compatible. Call it something generic ("Block World," "Voxel Builder").
- **Original textures/assets** — Minecraft's actual texture files (grass block, dirt, etc.) and its distinctive font are copyrighted. Don't ship or bundle real Minecraft textures/sounds. Hand-drawn/original pixel art in a similar blocky style is fine — the general *aesthetic* of low-res voxel cubes isn't ownable, only the specific expression is.
- **Mechanics/gameplay concepts** — game mechanics themselves (place/break blocks, crafting, survival loop) are not copyrightable. Clone Wars-style "blocky sandbox building game" clones are extremely common and legal (Minetest/Luanti is a well-known fully open-source example). This is the safe territory.
- **`.minecraft` world/mod file formats** — if you ever import real Minecraft world saves or resource packs, that's importing copyrighted third-party content, different risk profile than building your own format.

Bottom line: build your own voxel engine with original art and a different name, and you're in "inspired by" territory same as Minetale/Terasology/Minetest — no copyright problem. The risk only shows up if you use their name/logo or their actual asset files.

---

## [2026-08-12] User

> I want to steer clear of any copyright issues with Minecraft. Can we base it on Luanti?

### Claude

Architecturally, no — Luanti is a separate C++/Lua engine, not something you can import into a Python/PySide6 codebase; "basing it on Luanti" in practice would mean writing your own voxel engine (same pattern as the raycast extension) while borrowing ideas from its node-based world format, not forking its code.

Where Luanti *is* directly useful is its assets: the engine itself is LGPL (fine to be inspired by, awkward to actually embed), but block textures from Minetest Game and community packs are usually CC0 or CC-BY-SA — CC0 ones you can drop in freely, CC-BY-SA ones require attribution and share-alike (any exported student game bundling them would need to stay CC-BY-SA too, which could be a headache for a "kids export their game" tool). My recommendation: build the engine from scratch like raycast_2_5d, and only pull in specific CC0-licensed Luanti/Minetest texture packs for block art (checking each pack's license individually) rather than treating "based on Luanti" as reusing its codebase.

---

## [2026-08-12] User

> Draft a plan to build the engine from scratch like raycast_2_5d, and only pull in specific CC0-licensed Luanti/Minetest texture packs for block art (checking each pack's license individually)

### Claude

I've confirmed the extension mechanism's shape from `extensions/raycast_2_5d/` and its README contract. Drafting the plan directly as a folder extension from day one (skipping the "build in core, extract later" detour the raycast arc had to do as Stage B).

Wrote the plan to [docs/VOXEL_WORLD_PLAN.md](docs/VOXEL_WORLD_PLAN.md) — not committed, since you didn't ask for that yet.

Key calls made in it:
- **Extension-native from commit 1** (`extensions/block_world/`), skipping the "build in core, extract to extension later" detour the raycast arc had to do as its own multi-commit Stage B/C.
- **Phase 0 is a licensing audit before any code** — per-file CC0 verification of any Luanti/Minetest texture pack (not just trusting the pack description), logged in a checked-in `ASSETS.md`, with original pixel art as the fallback if nothing CC0 fits.
- **Renderer staged cheapest-first**: 2a flat single-layer (closest re-skin of the existing 2D raycaster), 2b heightmap with stepping/stacking (covers most of what students will actually want to build), 2c free vertical look flagged as a likely-deferred stretch goal, same treatment the raycast plan gave HTML5/Kivy floor-casting.
- **Scoped down deliberately**: no infinite terrain, no crafting/inventory beyond a fixed hotbar, no visual block-placing editor (worlds authored via generator script first, like `raycast_2`/`raycast_3`'s mazes) — those are flagged as legitimate later phases, not bundled in.
- **Export parity (HTML5/Kivy) pushed to last**, since that was consistently the most expensive part of the raycast arc.
- Carried forward known landmines from the raycast build (exact-grid-line ray origins, `create` re-firing on `restart_room`, one-sided collision handlers) so they don't get rediscovered the hard way.

Nothing implemented — this is just the plan document, ready to review or hand off to a build session.

---

## [2026-08-12] User

> We will start with the plan. We need assets from Luanti/Minetest

### Claude

All 32 files copied cleanly (1.2 MB). Now writing the audit doc.

Phase 0 is done. Summary:

**Sourced and audited:** [Hand Painted Pack Expanded](https://content.luanti.org/packages/shaft/hand_painted_expanded/) (CC0-1.0), by shaft / Miloslav Číž ("drummyfish", a well-known public-domain-only art contributor). Verified the license three ways — not just the ContentDB page: the pack's own `texture_pack.conf`, a bundled full CC0-1.0 legal-code file, and its `sources.txt` per-file provenance list (grepped for any CC-BY/BY-SA/proprietary source — found none; every cited upstream is itself CC0/public domain).

**Imported:** a curated 32-file starter set — terrain (dirt, grass, stone, cobble, sand, sandstone, gravel, clay), wood (log, planks x3), leaves, glass, water, ice, snow, a few ore/decorative blocks (coal, gold, diamond, brick, obsidian, mese), and six wool colors — into `extensions/block_world/textures/source_hand_painted_expanded/`, license file included.

**Documented:** `extensions/block_world/ASSETS.md` has the full audit table, including four other CC0 candidates I checked and rejected (7px — incomplete WIP; c64 16px — half the blocks unmodified; The Pixel Pack — kept as backup; Dungeon Soup — skipped, its "based off of DCSS textures" wording needed a deeper check I didn't want to do without a clear reason to prefer it over the already-clean pack).

`docs/VOXEL_WORLD_PLAN.md` is updated to mark Phase 0 done. Nothing committed — let me know if you want these added to git, and whether to move on to Phase 1 (the extension skeleton + world data model).

---

## [2026-08-12] User

> move on tp Phase 1

### Claude

Now the README, matching the raycast extension's pattern:

Now the regression tests:

Now let's run the new tests plus a quick full-suite sanity check.

All 15 pass. Now the full suite gate:

Found it — my extension's `__init__.py` has no `PLUGIN_NAME`, and `raycast_2_5d`'s doesn't either, so both now default to `"?"` and collide in `tests/test_plugin_loading_in_ide.py`'s uniqueness check. Fixing mine (the correct convention per `plugins/audio_actions.py`) resolves it without touching raycast's file.

Fixed. Full suite gate again to be sure:

Found it — no `**` around `state.py` in the original. Correcting:

`__pycache__` is gitignored, so it won't get swept in. Phase 1 is complete.

**What landed** (`extensions/block_world/`, not committed yet):
- `extension.json` + `__init__.py` — loads cleanly through `events/plugin_loader.py`, no actions/renderer declared yet (Phase 2/3 add those; empty placeholders would've just been dead code).
- `state.py` — the world data model: a sparse `room.extension_state["block_world"]` store (`get_block`/`set_block`/`remove_block`/`iter_blocks`/`bounds`), plus a `BLOCK_TYPES` registry mapping 27 block types to the 32 Phase-0 textures (with per-face textures for things like grass and logs, and `solid`/`transparent` flags for later collision/rendering use). `to_block_list`/`load_block_list` round-trip to a flat-list-of-dicts shape — I checked how room JSON actually persists tile layers first rather than inventing a new shape, and matched that convention.
- `README.md` — same explain-the-extension pattern as `raycast_2_5d/README.md`.
- `tests/test_block_world_state.py` — 15 tests, including one that resolves every registered block type's textures and asserts the files exist on disk, so a future rename in `ASSETS.md`'s asset set fails loudly here instead of silently in Phase 2's renderer.

**One real bug caught by the full-suite gate**, not just the new tests: adding a second extension with no `PLUGIN_NAME` collided with `raycast_2_5d`'s own missing one in `test_plugin_loading_in_ide.py`'s module-uniqueness check (both defaulted to `"?"`). Fixed by giving `block_world` a proper `PLUGIN_NAME`, which is the correct convention anyway. Suite: 2539 → 2540 passed, 0 failed.

`docs/VOXEL_WORLD_PLAN.md` is updated marking Phase 0 and 1 done. Nothing is committed — let me know if you want this staged, and whether to continue straight into Phase 2 (the renderer) or pause here.

---

## [2026-08-12] User

> Does raycast_2_5d have a proper plugin name?

### Claude

No — `extensions/raycast_2_5d/__init__.py` has no `PLUGIN_NAME` at all, which is exactly why it defaulted to `"?"` and collided with `block_world` before I fixed mine. It's a pre-existing gap, not something my change touched — I left it alone since fixing it wasn't necessary to resolve the test failure and wasn't part of Phase 1's scope. Want me to add `PLUGIN_NAME = "2.5D Raycast View"` to it too (matching its `extension.json`'s `name` field), as a small separate cleanup?

---

## [2026-08-12] User

> yes
