# Session notes — 2026-07-13 (`11852301`)

Auto-extracted from the Claude Code transcript. Machine: `SEMLOG-GTH-0001`, project dir: `/home/edu-thulleng/USR/Documents/pygm2`.
User messages are quoted; the rest is Claude's explanation text
(tool calls and results omitted). Curate freely, then add
`<!-- curated -->` near the top so the extractor won't overwrite it.

---

## [2026-07-13] User

> prepare a summary of all your explainations that were made from this machine. Append it to a file like what was done on ther computers working on this project

### Claude

I'll reconstruct what was done from this machine (the Linux box) by checking recent git history and the session transcripts here, then append a dated session-notes entry to [CLAUDE.md](CLAUDE.md) matching the existing format.

The script has a backfill mode. Let me check for any other historical project directories on this machine, then run the backfill.

This box lacks the `cleanupPeriodDays` retention fix that SESSION_NOTES.md says each machine needs (its May sessions were already lost this way). I'll apply it, verify one commit attribution, then write the curated entry.

Now committing and pushing to `main` (per the documented workflow — git is the sync between machines):

The shell is still in `docs/session-notes/` from an earlier command — re-running from the repo root:

The remote has newer commits from another machine — pulling with rebase, then pushing:

Done — everything is on `main` as commit `ffccd21` (rebased onto a fresh commit from your other machine and pushed).

What "like on the other computers" turned out to mean: the repo already has the session-notes system set up from the Windows box (`scripts/export_session_notes.py` + the Stop hook, commit `cb9ecac`), and the Windows box's 18 sessions were backfilled then — but this Linux box (`SEMLOG-GTH-0001`) never was. So I did the same three things here:

1. **Backfilled the raw extracts** — ran the exporter over this machine's surviving transcripts across its three historical project paths (`~/USR/Documents/pygm2`, `~/Dropbox/pygm2`, `~/Echanges/...`). Six files written to [docs/session-notes/](docs/session-notes/) (three trivial sessions skipped): the June 11–15 audit marathon, the June 15 checklist-checkboxes session, the June 25 printable-checklists session, the big June 26 testing-fixes session, plus two small ones.

2. **Appended a curated summary entry** to [docs/SESSION_NOTES.md](docs/SESSION_NOTES.md) (newest-at-top, same format as the Windows entries). It summarizes the sessions from this machine that weren't already curated — the L/M/W platform checkbox columns, the checklist-PDF session (including the near-deletion of `TESTING_CHECKLIST.md`, which turned out to be a distinct document referenced from code), and the June 26 batch: IDE polish, export routing/no-admin fixes, the seven Kivy-export fixes, and the recorded decision to keep pip-based export dependencies. The audit sessions already had entries, so I didn't duplicate them.

3. **Applied the per-machine retention fix** — set `"cleanupPeriodDays": 3650` in this box's `~/.claude/settings.json`, which SESSION_NOTES.md calls for on each machine but had only been done on the Windows box. It matters here: this box's May/early-June transcripts were already silently deleted by the 30-day default, so the oldest surviving session is 2026-06-11. Anything older from this machine is unrecoverable, but everything still on disk is now extracted into git.

Going forward the Stop hook keeps this machine's extracts current automatically — you just commit and push at the end of a session.

---

## [2026-07-13] User

> commit and push everything. This machine will be reformatted

### Claude

Since this machine is being wiped, I'll commit the working tree and also check for anything that lives only on this box — stashes, local-only branches, unpushed commits — before it's lost.

The commit succeeded but the old repo's embedded token has expired. The main repo uses SSH — pushing via that URL instead:

Now porting these into [CLAUDE.md](CLAUDE.md) so they survive the reformat (they're machine-local otherwise):

---
