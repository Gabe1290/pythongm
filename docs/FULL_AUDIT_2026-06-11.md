# Full-Codebase Audit — 2026-06-11

Adversarially-verified multi-agent audit of the entire pygm2 source tree
(18 scoped finder agents + batch verification + 2 independent refutation
votes per high). The run spanned three sessions / two machine crashes; the
methodology and known-rejected-findings list from `CLAUDE.md` were baked
into every finder prompt, so the previously rejected eval/exec/cache/zip-slip
items were excluded at the source.

**Pipeline:** 119 raw findings → 114 after dedup → **111 confirmed**
(3 refuted): **15 high / 61 medium / 35 low**. All 15 highs survived both
extra refutation votes (none downgraded).

Conventions (same as `LATENT_BUG_AUDIT_2026-06-03.md`):

- The audit is **a lead, not ground truth** — re-verify each claim against
  current code before fixing; correct this doc if a claim is overstated.
- Flip `[ ]` → `[x]` as items are fixed; note the fixing commit.
- Every fix needs a regression test.

---


## High (15) — ALL FIXED (2026-06-12)

All 15 highs were re-verified and fixed with regression tests across
commits `120124c`..`94a7ba0` (one commit per finding; H13+H14 and H6+H7
share commits because they share root causes). 61 medium / 35 low remain
open below.

- [x] **H1 — `core/ide_window.py:973` — Closing the Welcome tab in any non-English UI destroys the welcome_tab widget and breaks all subsequent project loads** *(fixed `482b42a`, tests/test_welcome_tab_close.py)*
  - *Found by:* core-ide-window; *category:* correctness
  - close_editor_tab guards the Welcome tab with a comparison against the hardcoded English string "Welcome", but the tab is titled with self.tr("Welcome") (verified: the shipped pygm2_fr.qm translates it to 'Bienvenue'; es/de/ru/etc. translations exist too). In a translated UI the guard fails, so the Welcome tab is treated as a regular editor: it is removed and self.welcome_tab gets deleteLater()'d (line 1017-1018). Line 1021-1022 then re-adds the already-doomed widget; once the event loop processes deleteLater, the C++ object is destroyed. Every later use of self.welcome_tab — most importantly on_project_loaded's `self.editor_tabs.addTab(self.welcome_tab, ...)` (line 3847) — raises RuntimeError('Internal C++ object already deleted'). That exception propagates into project_manager.load_project's broad `except Exception`, which returns False, so the IDE shows 'Failed to load project' for every project until restart. Other paths that re-add the welcome tab (closing the last editor tab line 1021, floating the last editor line 3705) hit the same dead wrapper.
  - **Failure:** A French-speaking student (the project's primary audience) clicks the close 'x' on the 'Bienvenue' tab — tabsClosable(True) puts a close button on it. The tab vanishes. From then on, File > Open Project / clicking any recent project always fails with 'Failed to load project' (RuntimeError swallowed inside project_manager.load_project), and the project manager is left holding loaded state the IDE window never received. Only restarting the IDE recovers. In English the same click is a harmless no-op, which is why this was never noticed.
  - **Suggested fix:** Compare by identity instead of text: `editor_widget = self.editor_tabs.widget(index); if editor_widget is self.welcome_tab: return` at the top of close_editor_tab (and never deleteLater the welcome tab). Optionally hide the close button on the welcome tab via tabBar().setTabButton(index, QTabBar.RightSide, None).
  - **Verifier:** close_editor_tab guards with the English literal (line 973) while the tab is titled self.tr("Welcome") (line 964) and pygm2_fr_core.ts translates it to 'Bienvenue' in context PyGameMakerIDE; editor_tabs.setTabsClosable(True) (line 951) puts a close button on it with no setTabButton override. In a translated UI the tab is removeTab'd and welcome_tab.deleteLater()'d (1016-1018); deferred deletion is not cancelled by the re-add at 1022, so on_project_loaded's addTab(self.welcome_tab) at line 3847 raises RuntimeError, which propagates through project_loaded.emit (project_manager.py:285) into the broad except (line 290), returning False — every subsequent load shows 'Failed to load project' until restart.
  - **Vote:** confirmed: Reproduced the full chain. (1) ide_window.py:964 titles the tab self.tr("Welcome"); main.py:282-293 installs translators for any non-'en' language; verified empirically that the shipped pygm2_fr.qm translates context 'PyGameMakerIDE'/'Welcome' to 'Bienvenue'. (2) setTabsClosable(True) at line 951 with no setTabButton suppression, so the Welcome tab has a close button. (3) The line-973 guard `tab_text != "Welcome"` passes for 'Bienvenue', so the Welcome tab takes the editor-close path: removeTab + welcome_tab.deleteLater() (1016-1018), then re-added at 1022. (4) Verified empirically with offscreen Qt that re-adding does NOT cancel the deferred delete: once the event loop runs, the C++ object is destroyed and any reuse raises RuntimeError('Internal C++ object already deleted'). (5) on_project_loaded (connected at line 1128) then raises RuntimeError at line 3847's addTab(self.welcome_tab,...) and aborts before setting current_project_path / game runner / asset tree, so every subsequent project open leaves the IDE without the project until restart; lines 1022 and 3706 hit the same dead wrapper. One mechanism detail is wrong: PySide6 does NOT propagate slot exceptions through emit() (verified empirically — traceback printed, emit returns), so load_project's except is never reached, it returns True, and the 'Failed to load project' dialog at ide_window.py:1375 does not appear. The real symptom is a silent failure: status says 'Opened project' while the IDE receives nothing — matching the finding's own 'project manager left holding loaded state the IDE window never received'. The core defect (English-literal guard vs translated tab title at line 973), the widget destruction, and the broken-all-subsequent-loads-until-restart consequence are all real and high-severity for the project's French-speaking primary audience.
  - **Vote:** confirmed: Verified end-to-end with no existing guard. The only protection for the Welcome tab is the hardcoded English comparison `tab_text != "Welcome"` (core/ide_window.py:973); no setTabButton call removes the close button, and setTabsClosable(True) (line 951) puts an 'x' on the Welcome tab. The shipped translations/pygm2_fr.qm translates 'Welcome' to 'Bienvenue' in the exact tr() context (PyGameMakerIDE), and translators are installed at startup (main.py:291-293, core/language_manager.py). A live offscreen-Qt reproduction with the real .qm confirmed the full chain: in a French UI the guard fails, the Welcome tab is removed and deleteLater()'d, the line-1022 re-add does not cancel the pending deletion, and once DeferredDelete is processed any further addTab(self.welcome_tab) raises RuntimeError('Internal C++ object already deleted'). on_project_loaded (line 3847) does that addTab unconditionally and is invoked via project_loaded.emit at project_manager.py:285 inside load_project's try, whose broad except at line 290 returns False — so every subsequent project load shows 'Failed to load project' until restart. No test pins this path and git history (incl. 85cec7c, which added the French Welcome translation) shows no fix. This is IDE-UI robustness for the project's primary French-speaking audience, not a foreign-format/trust issue, so the trusted-project-files caveat does not apply.

- [x] **H2 — `core/project_manager.py:444` — Zip-save state survives project switches: saving a new project silently rewrites the previously opened zip and misreports save success** *(corroborated by 1 other finder)* *(fixed `64c47da`, tests/test_zip_save_state.py)*
  - *Found by:* cross-lifecycle; *category:* data-integrity
  - When a project is opened from a .zip, load_project_from_zip() sets _original_zip_path, _temp_extraction_dir and _auto_save_as_zip=True. The only place this state is cleared is close_project() — which has ZERO callers in the entire codebase (verified: `grep -rn close_project` matches only its definition at core/project_manager.py:822). Neither load_project() (line 235), create_new_project() (line 184), save_project_as() (line 892), nor the IDE's load_project/closeEvent ever reset it. So after the user switches from a zip project to ANY other project, save_project() still takes the zip branch: `if self._auto_save_as_zip and self._original_zip_path and not project_path: return self._save_to_zip()`. _save_to_zip() then (a) saves the CURRENT project to its folder via _save_to_folder(), but (b) recompresses the STALE temp extraction of the OLD zip project (`compress_project(self._temp_extraction_dir, self._original_zip_path)`, line 805) over the old zip file, and (c) returns the zip-compression result as the save result for the current project. is_project_from_zip() (line 1474) also keeps returning True for the folder project.
  - **Failure:** A teacher opens a student's maze.zip, looks at it, then File→Open their own folder project (or File→New Project) and works there. (1) Every Ctrl+S and every 30s auto-save of the folder project rewrites maze.zip from a stale /tmp extraction — if maze.zip was meanwhile replaced (Dropbox sync, copied a newer version from the student), it is silently reverted to the old content: real data loss. (2) If maze.zip is now missing or locked (USB stick removed, file deleted), `shutil.copy2(self._original_zip_path, backup_path)` (line 802) raises, _save_to_zip returns False, and the IDE shows 'Failed to save project' on every save — even though the folder save actually succeeded — and closeEvent's save-before-exit then refuses to proceed cleanly. (3) After Save-As from a zip to a folder, subsequent edits go to the folder while the original zip keeps being rewritten with the pre-Save-As state on every save.
  - **Suggested fix:** Reset the zip-mode state at the top of load_project() and create_new_project() (and in save_project_as when retargeting): clean up the old _temp_extraction_dir (shutil.rmtree, ignore_errors=True) and set _original_zip_path=None, _temp_extraction_dir=None, _auto_save_as_zip=False — except when the call comes from load_project_from_zip for the new zip itself (e.g., reset before load_project_from_zip assigns the new values). Alternatively, have the IDE call project_manager.close_project() before loading/creating any project.
  - **Verifier:** Reproduced on HEAD: after load_project_from_zip then load_project of a folder project, save_project() still takes the zip branch (project_manager.py:444) because _auto_save_as_zip/_original_zip_path are reset only in close_project (line 822), which grep confirms has zero callers, and load_project (235)/create_new_project (184)/save_project_as (892) never touch them. The repro showed the old projA.zip rewritten from the stale temp extraction on every save of the unrelated folder project, and after deleting the zip, save_project returned False (shutil.copy2 at :802 raised FileNotFoundError) even though the folder save succeeded — the IDE would show 'Failed to save project' on every Ctrl+S. High: silently rewrites/reverts a user file the user believes is closed (real loss if the zip changed externally, e.g. Dropbox sync — the user's documented environment) plus persistent misreported save failures.
  - **Vote:** confirmed: Reproduced line by line. (1) load_project_from_zip (core/project_manager.py:1450-1452) sets _original_zip_path/_temp_extraction_dir/_auto_save_as_zip=True; the only reset is close_project (:822-835), which a repo-wide grep confirms has zero callers; load_project (:235-292), create_new_project (:184-229), and save_project_as (:892-928) never touch these fields; the IDE uses one ProjectManager instance for all opens (ide_window.py:64). (2) After switching to a folder project, Ctrl+S (ide_window.py:1384) and the 30s auto_save (:930-933) call save_project() with no path, satisfying the gate at :444 and entering _save_to_zip. (3) _save_to_zip saves the CURRENT folder project via _save_to_folder() (default save_path=current_project_path, :527), then copy2's the old zip to .bak (:802) and recompresses the stale, never-cleaned temp extraction over the old zip (:805), returning that result as the save result — so the old zip is silently rewritten/reverted on every save, and if the zip is missing, copy2 raises (caught at :816, returns False) making the IDE report 'Failed to save project' despite the folder save having succeeded. is_project_from_zip (:1474) also stays True. All three failure scenarios (stale-zip overwrite/revert, false save-failure on missing zip, post-Save-As zip rewrites) follow with no broken step. Severity stands under the project's trust model: this corrupts the user's own files through the normal save path, no untrusted input needed.
  - **Vote:** confirmed: Every element of the finding checks out against current code with no existing guard. close_project (core/project_manager.py:822), the only place that clears _original_zip_path/_temp_extraction_dir/_auto_save_as_zip outside __init__ and the zip-load failure path, has zero callers (grep matches only its definition). The IDE holds a single ProjectManager created once at startup (ide_window.py:64), and neither ProjectManager.load_project (:235), create_new_project (:184), save_project_as (:892), nor any IDE-side switch/close path resets the zip state. The zip-open entry points are live (ide_window.py:1264, ide_exporters.py:166, welcome tab), and Ctrl+S plus the 30s auto_save (:930-933) call save_project() with no path, hitting the stale gate at :444 -> _save_to_zip (:788), which saves the current folder project but recompresses the stale temp extraction of the OLD zip over the old zip file (:805) and returns the zip result as the save verdict; if the old zip is missing, shutil.copy2 at :802 raises and save reports failure despite the folder save succeeding. is_project_from_zip (:1474) also stays True. No test references load_project_from_zip and no recent commit addresses this lifecycle. The trusted-project-files scoping does not apply: this is a stale-state data-loss bug triggered by ordinary IDE use (open zip, then open/new folder project), not by untrusted input.

- [x] **H3 — `core/project_manager.py:631` — Emptying a room still resurrects deleted instances whenever the room was never registered in _rooms_loaded_this_session (new projects, recreated rooms, legacy projects)** *(corroborated by 2 other finders)* *(fixed `4e4422d`, tests/test_room_resurrection.py)*
  - *Found by:* core-managers-utils; *category:* data-integrity
  - The bug-#15 fix gates the 'preserve existing file instances' heuristic on `_rooms_loaded_this_session`, but that set is ONLY populated in `_load_rooms_from_files` (lines 328/340), and only when a room file exists or embedded instances are non-empty at load time. Rooms that acquire their on-disk file mid-session are never registered, so an intentionally emptied room hits the preservation branch and the old instances are written back. Affected sessions: (a) a freshly created project (create_new_project never touches the set; room0.json is first written by save #1); (b) a room deleted or renamed and later recreated with the same name — AssetManager.delete_asset/rename_asset never remove the stale rooms/<name>.json, so the orphan file's instances are resurrected into the new room; (c) legacy projects with no rooms/ dir at load (early return at line 301 leaves the set empty for every room). Empirically reproduced against HEAD: create project -> add instance to room0 -> save -> set instances=[] -> save -> rooms/room0.json still contains 1 instance, and reloading the project brings the deleted instance back into memory. The IDE room editor routes through this exact path (save_requested -> update_asset('rooms',...) -> save_project).
  - **Failure:** A teacher creates a new project, places a test instance in room0, saves, then deletes the instance (empties the room) and saves again. The save log even says 'Preserving 1 instances from existing file'. On the next project open the deleted instance is back in the room. Same resurrection happens if a room is deleted/renamed and a new room is later created with the old name: the stale rooms/<name>.json injects the dead room's instances into the new room.
  - **Suggested fix:** Register rooms in `_rooms_loaded_this_session` at every point the in-memory instance list becomes authoritative: in create_new_project/_apply_project_template for all created rooms, in update_asset/create_asset when 'instances' is supplied for a room, and after `_save_rooms_to_files` successfully writes a room file. Additionally, delete/rename rooms' and objects' external JSON files in AssetManager.delete_asset/rename_asset so orphan files cannot be resurrected.
  - **Verifier:** Reproduced on HEAD with a headless harness: create_new_project → add instance to room0 → save → set instances=[] → save logs 'Preserving 1 instances from existing file for room0', rooms/room0.json keeps the instance, and reloading the project resurrects it in memory. _rooms_loaded_this_session is populated only in _load_rooms_from_files (project_manager.py:328/340), so any room whose file is first written mid-session (new projects, recreated/renamed rooms with orphan files, legacy projects hitting the early return at :301) hits the preservation branch at :631 when emptied. This is a residual gap in the closed audit item #15, not a duplicate of it. High: silent data loss (deleted instances return) in completely normal new-project use.
  - **Vote:** confirmed: Reproduced exactly against HEAD with an offscreen-Qt script: create_new_project → add instance to room0 → save (file has 1 instance) → set instances=[] → save again. The guard at core/project_manager.py:631 fires because _rooms_loaded_this_session is only ever populated in _load_rooms_from_files (lines 328/340, called only from load_project) — create_new_project and the save path never add to it (repo-wide grep shows only lines 138/299/328/340/632 touch it). Observed the predicted log '⚠️ Preserving 1 instances from existing file for room0'; rooms/room0.json retained the deleted instance after the empty-room save, and reloading the project resurrected it into memory (1 instance). The within-session emptying of a room in a freshly created project (the finding's primary scenario, matching the IDE's update_asset → save_project path) demonstrably loses the user's deletion. The (b)/(c) variants (stale orphan room file with same name; legacy project with no rooms/ dir early-returning at line 301 with the set empty) follow from the same unpopulated-set condition verified in code.
  - **Vote:** confirmed: Empirically reproduced against HEAD with a live ProjectManager (offscreen Qt): create_new_project -> add instance to room0 in memory -> save_project (rooms/room0.json written with 1 instance) -> set instances=[] -> save_project again logs "Preserving 1 instances from existing file for room0", the on-disk file still contains 1 instance, and a fresh load_project brings the deleted instance back into memory. The guard at core/project_manager.py:631-632 depends on _rooms_loaded_this_session, but a whole-codebase grep shows that set is only populated in _load_rooms_from_files (lines 328/340) and reset at 138/299 — create_new_project, save_project, _save_rooms_to_files, and update_asset never register rooms, so any room whose on-disk file first appears mid-session (new projects, recreated rooms, legacy projects hitting the early return at line 301) permanently hits the preservation branch when emptied. No other guard exists: AssetManager.delete_asset only unlinks asset_data["file_path"] and the thumbnail, so a stale rooms/<name>.json from a deleted/renamed room would also survive and inject its instances into a recreated room. This affects trusted user-authored projects via the normal IDE save path, so the trusted-input scoping does not refute it; it is a real data-loss/resurrection bug, not a duplicate of the previously-fixed bug #15 (it is a gap in that fix's gating condition).

- [x] **H4 — `editors/object_editor/python_code_parser.py:23` — Assigning one half of a paired attribute (self.x, self.direction, self.gravity_direction...) produces an action with a missing required parameter: runtime teleports the object and the statement is deleted on the next code regeneration** *(fixed `9274c1f`, tests/test_paired_attribute_actions.py)*
  - *Found by:* object-editor; *category:* data-integrity
  - ATTRIBUTE_TO_ACTION maps single attribute assignments onto multi-parameter actions: ('self','x')->('jump_to_position','x'), ('self','y')->('jump_to_position','y'), ('self','direction')/('self','speed')->set_direction_speed, ('self','gravity_direction')->set_gravity. Parsing `self.x = 100` yields {"action":"jump_to_position","parameters":{"x":100,"relative":false}} with no 'y'. Two failures follow: (a) at runtime, execute_jump_to_position_action (runtime/action_executor.py:863-864) reads parameters.get("y","0"), so the instance is moved to y=0 — the user only asked to change x; (b) when the code view is regenerated, the template 'self.x = {x}\nself.y = {y}' raises KeyError and _generate_action_code returns the literal string "# Missing parameter 'y' for jump_to_position" — the user's statement is replaced by a comment, and since comments are not AST statements, the next auto-apply parse drops the action entirely (permanent loss). Verified empirically: python_to_events('class obj:\n def on_create(self):\n  self.x = 100') -> jump_to_position{x:100}; events_to_python of that -> "# Missing parameter 'y' for jump_to_position". Same failure for self.y, self.direction (missing 'speed'), self.speed (missing 'direction'), and self.gravity_direction (set_gravity template only uses {gravity}).
  - **Failure:** A student opens 'Edit Custom Code' and types `self.x = 100` in on_create. The 1.5s auto-apply (object_editor_main._auto_apply_code) replaces the events with the parsed result and the object is saved. At runtime the object appears at (100, 0) — top of the screen — instead of keeping its y. When the student later opens View Generated Code (or re-enters Edit mode, which seeds from events_to_python), the line is gone, replaced by '# Missing parameter' — and one more auto-apply erases the action from the project.
  - **Suggested fix:** When parsing a single assignment to one half of a paired action, fill the other parameter with the identity expression (e.g. self.x=100 -> jump_to_position{x:100, y:'self.y'}; self.direction=d -> set_direction_speed{direction:d, speed:'self.speed'}; gravity_direction -> set_gravity{direction:v, gravity:'self.gravity'}). Also make the templates tolerant: use params.setdefault for the missing key instead of emitting a comment.
  - **Verifier:** Reproduced end-to-end: parsing 'self.x = 100' yields jump_to_position with only x (python_code_parser.py:516-522); runtime execute_jump_to_position_action defaults y to "0" (action_executor.py:863-864) so the object teleports to y=0; regeneration hits KeyError and emits "# Missing parameter 'y' for jump_to_position" (line 1133-1135), and _auto_apply_code's replace-not-merge (object_editor_main.py:1547-1550) silently drops the action on the next round trip. Same failure verified for self.direction (missing 'speed'). Wrong runtime position plus silent loss of user-typed code in the flagship code-editor flow justifies high.
  - **Vote:** confirmed: Reproduced end-to-end. (1) ATTRIBUTE_TO_ACTION (editors/object_editor/python_code_parser.py:23-29) maps single-attribute assignments onto multi-parameter actions; _try_parse_assignment builds parameters containing only the one mapped key plus 'relative', so `self.x = 100` yields jump_to_position{x:100, relative:False} with no 'y' (verified by running the parser; no parse errors, so object_editor_main._auto_apply_code line 1506 replace-applies it). (2) Runtime: execute_jump_to_position_action (runtime/action_executor.py:863-864) defaults the missing y to "0" with relative=False, teleporting the instance to (100, 0). (3) Regeneration: ACTION_TO_PYTHON['jump_to_position'] = 'self.x = {x}\nself.y = {y}' raises KeyError and _generate_action_code (lines 1132-1135) returns "# Missing parameter 'y' for jump_to_position" — verified empirically; the user's statement is replaced by a comment in the regenerated code, and a comment is not an AST statement so the action cannot round-trip. Same defect for self.y, self.direction, self.speed (set_direction_speed needs both), and self.gravity_direction (maps to param 'direction' but the set_gravity template uses {gravity}). Minor nuance only: if the comment is the sole body statement the regenerated method is a SyntaxError, which blocks (rather than silently completes) the final auto-apply erasure; with any sibling statement the action is silently dropped as described. The runtime teleport and code-view data loss are real in all cases.
  - **Vote:** confirmed: Reproduced end-to-end on current main with no guard found anywhere in the chain. (1) Parse: ATTRIBUTE_TO_ACTION in editors/object_editor/python_code_parser.py:23-29 maps single-attribute assignments onto multi-parameter actions; running python_to_events on 'self.x = 100' in on_create yields {'action': 'jump_to_position', 'parameters': {'x': 100, 'relative': False}} with no 'y' and zero errors — so _auto_apply_code (editors/object_editor/object_editor_main.py:1506) sails past its result.errors guard and commits via events_panel.load_events_data + mark_modified; _flush_pending_auto_applies ensures it persists on save. (2) Runtime: runtime/action_executor.py execute_jump_to_position_action reads y_expr = str(parameters.get("y", "0")) with relative=False, so the instance is teleported to y=0 — verified at lines ~863-864. (3) Regeneration: events_to_python of the parsed result produces literally '# Missing parameter 'y' for jump_to_position' (the template 'self.x = {x}\nself.y = {y}' KeyErrors and _generate_action_code returns the comment, line 1135). (4) Loss: re-parsing the regenerated code returns events == {} — the action is gone (in an object with other events, the events dict is non-empty so the auto-apply replace proceeds and silently drops the action). Same defect class applies to self.y, self.direction (missing 'speed'), self.speed (missing 'direction'), and self.gravity_direction (set_gravity template only uses {gravity}, so the assignment is parsed but renders as a wrong/garbage statement). The parameter-normalization shims in _generate_action_code (set_hspeed/set_vspeed/variable_name aliases) show the codebase patches such gaps case-by-case but has no shim for these pairs; no test covers this roundtrip. This is editor-authored data (trusted-input caveat irrelevant): a user typing one of the most natural lines possible in Edit Custom Code gets a wrong runtime position and eventual silent deletion of their statement.

- [x] **H5 — `editors/object_editor/python_code_parser.py:744` — Thymio if/else: the else branch is silently deleted by the parser — and ObjectEventsPanel re-parses Thymio execute_code on every load, so saved projects lose their else branches just by being opened** *(fixed `507916b`, tests/test_thymio_else_preserved.py)*
  - *Found by:* object-editor; *category:* data-integrity
  - _try_parse_thymio_conditional (and its helpers _try_parse_thymio_compare / _try_parse_thymio_button_check) parses stmt.test and stmt.body into a thymio_if_* action with sub_actions, but never reads stmt.orelse. An `if thymio...: ... else: ...` statement therefore loses its entire else branch. Verified empirically: parsing 'if thymio.read_button("center"): thymio.set_led_top(32,0,0) else: thymio.leds_off()' returns thymio_if_button_pressed with only the LED-on sub_action; thymio.leds_off() is gone. This is amplified by ObjectEventsPanel._parse_execute_code_actions (editors/object_editor/object_events_panel.py:1588-1630), which runs on EVERY load_events_data call: any stored execute_code action whose code contains 'thymio.' is re-parsed, and if the result contains a thymio_ action (a thymio_if_* counts) the original execute_code is replaced by the lossy parse. So an existing, saved, working project loses the else logic when the object editor merely opens the object and auto-save persists it.
  - **Failure:** A teacher writes robot logic in the code editor: 'if thymio.read_button("center"): set red LED, else: LEDs off'. Auto-apply parses it; the else branch vanishes while the status pill says 'Saved'. Alternatively, a project saved earlier with that code inside an execute_code action loses the else branch the next time the object is opened in the editor and saved. The robot's LED never turns off; the user has no error or warning.
  - **Suggested fix:** In _try_parse_thymio_conditional, when stmt.orelse is non-empty either (a) parse it into the action's 'else_actions' (execute_action in runtime/action_executor.py:430-437 already executes parameters['else_actions'] for boolean-returning handlers), or (b) refuse to convert and fall back to execute_code so the statement is preserved verbatim. Also gate _parse_execute_code_actions on a lossless round-trip check.
  - **Verifier:** Reproduced: _try_parse_thymio_conditional and its helpers (python_code_parser.py:744-855) never reference stmt.orelse — parsing the if/else button example returns thymio_if_button_pressed with only the LED-on sub_action, thymio.leds_off() gone. The amplifier is real: load_events_data unconditionally calls _parse_execute_code_actions (object_events_panel.py:1572, 1588-1630), which replaces any execute_code containing 'thymio.' with the lossy parse whenever it yields any thymio_ action, so a saved project's else logic is destroyed just by opening the object and saving. Silent data loss of authored logic in normal Thymio use → high.
  - **Vote:** confirmed: Reproduced end to end. (1) Parser drops the else branch: editors/object_editor/python_code_parser.py:744-767 `_try_parse_thymio_conditional` and its helpers `_try_parse_thymio_compare` (:769, reads only `test` + `body` at :792) and `_try_parse_thymio_button_check` (:830, reads only `call` + `body` at :842) never reference `stmt.orelse` — grep confirms zero occurrences of "orelse" in the entire file. Empirically, parsing `if thymio.read_button("center"): thymio.set_led_top(32,0,0) / else: thymio.leds_off()` returns a `thymio_if_button_pressed` action whose `sub_actions` contain only the LED-on call; `leds_off` is absent from the output, with no error or fallback to execute_code (the if-statement matched, so it never reaches the unrecognized-statements preservation path in `_extract_actions_from_body` at :428-440). (2) Amplification on load: editors/object_editor/object_events_panel.py:1572 — `load_events_data` unconditionally calls `_parse_execute_code_actions` (:1588-1630), which re-parses any stored `execute_code` whose code contains 'thymio.' and replaces it (`new_actions.extend(parsed_actions); continue` at :1619-1620) whenever any parsed action name starts with `thymio_` — which a lossy `thymio_if_*` satisfies. Simulating that exact loop against a saved events dict containing the if/else code confirms the resulting `current_events_data` no longer contains `leds_off`; `get_events_data` (:1632) returns this mutated dict, so the next save persists silent data loss in a previously-working trusted project. Every step of the failure scenario follows from the code.
  - **Vote:** confirmed: Verified empirically and by code inspection; no existing guard found anywhere. (1) The parser bug is real: in /home/edu-thulleng/USR/Documents/pygm2/editors/object_editor/python_code_parser.py, `_try_parse_thymio_conditional` (line 744) and helpers `_try_parse_thymio_compare` (769) / `_try_parse_thymio_button_check` (830) only consume `stmt.test` and `stmt.body`; `stmt.orelse` is never referenced anywhere in the file (grep for "orelse" returns zero hits). Running the parser live (Python 3.11, repo root) on `if thymio.read_button("center"): set_led_top(32,0,0) else: leds_off()` returns a single `thymio_if_button_pressed` action whose `sub_actions` contain only the LED-on call — `thymio.leds_off()` is silently gone. Non-thymio if/else statements are safe because `_try_parse_statement` returns None for them and they round-trip losslessly via `_statements_to_code`/`ast.unparse`; only thymio conditionals are matched-and-truncated. (2) The amplification is real: `ObjectEventsPanel._parse_execute_code_actions` (object_events_panel.py:1588-1630), called unconditionally from `load_events_data` (line 1572), re-parses every stored `execute_code` action whose code contains "thymio." and replaces it with the parse result whenever any resulting action name starts with "thymio_" — which a lossy `thymio_if_*` does — so a previously saved working project loses its else branch in memory just by opening the object, and `get_events_data` returns the mutated data for save. (3) Existing-guards hunt came up empty: no orelse bail-out, no roundtrip-fidelity check before replacement, no test covering thymio if/else parsing (the only related test, TestThymioPresetEnablesElseAction in tests/test_audit_regressions.py, covers a different Blockly-toolbox bug — and incidentally proves else semantics are a supported user-facing feature via the `else_action` block). The trusted-project lens does not apply: this is silent data loss of user-authored content, not an untrusted-input issue.

- [x] **H6 — `export/Kivy/code_generator.py:89` — Conditional blocks are never closed after the guarded action — all subsequent event actions are wrongly swallowed into the if/else branch** *(fixed `94a7ba0`, tests/test_kivy_codegen_regressions.py)*
  - *Found by:* export-mobile; *category:* correctness
  - In GameMaker (and this IDE's runtime executor), a conditional action guards exactly the next action or the next start_block..end_block group. The Kivy generator instead leaves the conditional's indent open 'for sequential action handling' (comment at line 214) and only else_action/end_block ever pop it — so every remaining action of the event stays inside the branch. Empirically confirmed against shipped sample data: running ActionCodeGenerator on samples/plateforme_2 obj_personnage's step event (`comment, if_collision, set_gravity, else_action, set_gravity, comment, test_variable, set_vspeed`) generates `else:` containing set_gravity AND the trailing comment/test_variable/`self.vspeed = -(24)` — i.e. the jump impulse executes unconditionally every airborne frame, instead of only when the (separate) test_variable condition holds and regardless of the if/else. A second manifestation: the canonical flat pattern `if_condition; start_block; A; end_block; B` emits B one indent level above A but still below the `if` line (end_block at lines 82-87 pops only one level), producing 'unindent does not match any outer indentation level' — an import-time crash.
  - **Failure:** A teacher exports the bundled plateforme_2 platformer (or any project authored with flat conditional+action sequences, which is what the IDE runtime executes correctly) to Android/EXE. The exported game's physics are wrong: gravity/jump logic collapses into the else branch, the character behaves nothing like in the IDE; other action layouts crash the exported game with IndentationError.
  - **Suggested fix:** Implement GM scoping: after a conditional opens a block, close it (pop_indent + pop block_stack) once exactly one following action has been emitted, unless that action is start_block, in which case close when the matching end_block is processed. end_block must also pop the pending conditional level(s) it closes.
  - **Verifier:** Reproduced both manifestations by running ActionCodeGenerator on the shipped samples/plateforme_2 obj_personnage step event: the trailing comment/test_variable/set_vspeed actions are generated inside the else: branch (self.vspeed = -(24) every airborne frame), and the flat if_condition;start_block;A;end_block;B pattern compiles to a real IndentationError ('unindent does not match any outer indentation level'). The IDE runtime (_execute_action_list_inner, action_executor.py:208-270) uses GM80 skip-next semantics for flat conditionals, and this generator is on the live KivyExporter/AndroidExporter path, so exports of correctly-authored projects are wrong or crash.
  - **Vote:** confirmed: Reproduced both manifestations line-by-line and empirically. (1) The generator (export/Kivy/code_generator.py) opens an indent for every conditional and, with no nested then_actions, deliberately leaves it open (comment at line 214); only else_action (line 89) and end_block (line 82) pop. Running ActionCodeGenerator on the shipped samples/plateforme_2 obj_personnage step event (exact sequence as claimed: comment, if_collision, set_gravity, else_action, set_gravity, comment, test_variable, set_vspeed) yields an else: block containing set_gravity AND the trailing comment/test_variable stub/`self.vspeed = -(24)` — i.e. the vspeed write runs unconditionally inside the else branch, not guarded by test_variable nor outside the if/else. The IDE runtime (runtime/action_executor.py:196-243) implements GM80 semantics where a conditional guards exactly the next action or start_block..end_block group, so the exported physics diverge from in-IDE behavior. (2) The flat pattern if_condition; start_block; A; end_block; B generates B at one indent level between the if line and A; compile() fails with exactly "unindent does not match any outer indentation level" — an import-time crash. The exporter feeds raw event actions sequentially through process_action (kivy_exporter.py:2565-2575), so this is the live export path. Every step of the failure scenario follows.
  - **Vote:** confirmed: Empirically reproduced both manifestations with no existing guard anywhere in the pipeline. (1) Runtime semantics verified: runtime/action_executor.py `_execute_action_list_inner` (lines ~207-273) implements GM-style skip-next — a False conditional sets `skip_next` which skips exactly one action or one start_block..end_block group, and `end_block` resets state; so flat sequences like plateforme_2's step event are correct in the IDE. (2) The Kivy generator diverges: running ActionCodeGenerator(base_indent=2) on the shipped samples/plateforme_2/objects/obj_personnage.json step event (comment, if_collision[not_flag], set_gravity, else_action, set_gravity, comment, test_variable, set_vspeed) produces an `else:` branch containing set_gravity AND the trailing comment/test_variable stub/`self.vspeed = -(24)` — the trailing actions are swallowed into the else exactly as claimed (additionally, not_flag is ignored and test_variable emits `pass # TODO`, so vspeed=-24 fires unconditionally inside else — physics are badly wrong in export while correct in IDE). (3) The flat pattern if_condition; start_block; A; end_block; B generates `if:` / A at +2 indents / B at +1 indent → compile() raises IndentationError "unindent does not match any outer indentation level" — an import-time crash in the exported game. (4) No guard exists: export/Kivy/kivy_exporter.py `_generate_action_code` (line ~2557) feeds event action lists flat into the generator with no normalization into nested then_actions (the only then_actions reference in the exporter, line 2337, is a read for speed extraction); tests/test_exporters.py block tests cover isolated patterns but not trailing-action-after-conditional; recent commits (aaa3bb4 etc.) touched other generator paths but not this. The exporter is live, reachable code — it is the shared backend for EXE and Android exports via export/base_exporter.py BaseKivyExporter (line 232). Sample data is shipped first-party content, so the trusted-input defense does not apply; this is a plain correctness bug, not a threat-model question.

- [x] **H7 — `export/Kivy/code_generator.py:707` — Unknown conditional actions (test_instance_count, test_variable) emit a flat 'pass' — generated code for bundled maze_3 is an IndentationError, and guarded actions run unconditionally** *(fixed `94a7ba0`, tests/test_kivy_codegen_regressions.py)*
  - *Found by:* export-mobile; *category:* correctness
  - _convert_simple_action's default branch returns `pass  # TODO: <action>` for any unhandled action. For conditional actions this is doubly wrong: (1) no block is opened, so the following start_block pushes an indent under a non-compound statement; (2) the action it guards executes unconditionally. Empirically confirmed with the bundled sample: samples/maze_3 obj_door's step event is `[test_instance_count, start_block, play_sound, destroy_instance, end_block]`; the generator produces:
        pass  # TODO: test_instance_count
            pass  # TODO: play_sound
            self.destroy()
and compile() raises 'SyntaxError: unexpected indent'. So exporting maze_3 yields a game that crashes at import. Where the guarded action isn't inside a block (plateforme_2's `test_variable` then `set_vspeed`), the jump-condition is dropped and set_vspeed runs every step. Other actions used by every bundled sample (play_sound, set_sprite, move_to_contact, comment) also degrade to pass stubs — silent loss of sound/sprite behavior.
  - **Failure:** A teacher exports the bundled maze_3 sample to Android: the APK builds, but the game crashes on launch with IndentationError in objects/obj_door.py. In plateforme_2 exports, the variable-gated jump fires unconditionally every frame.
  - **Suggested fix:** Add branches for test_instance_count / test_variable (and the other runtime conditionals) that emit a real `if ...:` and push a block, mirroring if_condition. At minimum, make the unknown-action fallback detect that the next action is start_block and emit `if True:` (or refuse export with a clear per-action error) so the generated file stays syntactically valid.
  - **Verifier:** Empirically reproduced: running ActionCodeGenerator on bundled maze_3 obj_door's step event ([test_instance_count, start_block, play_sound, destroy_instance, end_block]) produces 'pass  # TODO: test_instance_count' followed by deeper-indented lines, and compile() raises 'SyntaxError: unexpected indent' — every maze_3 export crashes at import. plateforme_2's test_variable→set_vspeed (no block) verified to make the guarded action unconditional. Same bug class as registry item #18 (if_object_exists), which was treated as real and fixed.
  - **Vote:** confirmed: Reproduced end-to-end. (1) Code path: in export/Kivy/code_generator.py, ActionCodeGenerator.process_action handles a fixed list of conditional actions (if_on_grid, test_expression, if_collision*, check_*, if_key_pressed, if_condition, if_can_push, if_*_room_exists, repeat, while, stop_if_no_keys) — test_instance_count and test_variable are not among them, so they fall through to _convert_simple_action, whose default branch (lines 707-709) returns a flat "pass  # TODO: <action>" without opening a block. The subsequent start_block (line 76) unconditionally push_indent()s. (2) Empirical reproduction: ran the generator on the bundled samples/maze_3/objects/obj_door.json step event ([test_instance_count, start_block, play_sound, destroy_instance, end_block] — exactly as the finding states); output is literally "        pass  # TODO: test_instance_count" followed by "            pass  # TODO: play_sound" / "            self.destroy()", and compile() of the wrapped method raises SyntaxError: unexpected indent. (3) The exporter wiring is real: kivy_exporter.py _generate_action_code (line ~2555) uses ActionCodeGenerator for event method bodies and performs no compile/AST validation before writing, so the broken code ships in the exported game and fails at import. (4) The secondary claim also holds: test_variable is unhandled (used in samples/plateforme_2/objects/obj_personnage.json) while set_vspeed IS handled (code_generator.py:418), so the condition degrades to a no-op pass and the guarded set_vspeed executes unconditionally every step. play_sound, set_sprite, move_to_contact, comment likewise have no handler (grep confirms) and degrade to pass stubs. Note: project files are trusted, but this is not an untrusted-input issue — it is the exporter mis-compiling its own bundled samples into syntactically invalid Python.
  - **Vote:** confirmed: Reproduced end-to-end with the bundled sample. samples/maze_3/objects/obj_door.json step event is exactly [test_instance_count, start_block, play_sound, destroy_instance, end_block]. Neither `test_instance_count` nor `test_variable` (nor play_sound/set_sprite/move_to_contact/comment) appears anywhere in export/Kivy/code_generator.py; they fall into the default branch at export/Kivy/code_generator.py:707-709 which returns `pass  # TODO: <action>` without opening a block, so the following start_block pushes an indent under a non-compound statement. Running ActionCodeGenerator on the real obj_door actions produces:
        pass  # TODO: test_instance_count
            pass  # TODO: play_sound
            self.destroy()
and compile() raises "SyntaxError: unexpected indent" — exported maze_3 crashes at import of objects/obj_door.py. Guard hunt (all negative): (1) export/Kivy/project_adapter.py does not translate action types — it passes events through verbatim; (2) kivy_exporter.py `_generate_action_code` feeds raw action dicts straight to process_action and has no compile/py_compile/ast validation of generated files (its only "guard" is substituting `pass` for *empty* code, not invalid code); (3) export/android/android_exporter.py inherits BaseKivyExporter and routes through the same KivyExporter, so the Android failure scenario is the real path; (4) no tests cover test_instance_count export; (5) recent commits (aaa3bb4 "Kivy if_object_exists", 55080b5 "if_condition with instance_count") fixed sibling actions in exactly this bug class but left test_instance_count/test_variable unhandled — `if_condition` with condition_type='instance_count' exists in the generator, but nothing maps the sample's `test_instance_count` onto it. Trust framing doesn't apply: this is correctness on the project's own bundled samples, not an untrusted-input issue. Secondary claim also holds: where the unknown conditional isn't followed by start_block (plateforme_2's test_variable then set_vspeed), the guard degrades to a flat `pass` and the gated action runs unconditionally every step.

- [x] **H8 — `export/Kivy/kivy_exporter.py:2298` — Keyboard handler generator emits 'i'/'eli' instead of 'if'/'elif' — generated game is a SyntaxError** *(fixed `120124c`, tests/test_kivy_codegen_regressions.py)*
  - *Found by:* export-mobile; *category:* correctness
  - _generate_keyboard_handler builds the per-key dispatch with `if_keyword = "i" if i == 0 else "eli"`, producing lines like `i key == 275:  # right`. The sibling _generate_keyboard_press_handler (line 2487) has the correct `"if"`/`"elif"`, so this is a plain typo/regression in the non-grid keyboard path. The broken path runs whenever an object has 'keyboard' events and none of them contain if_on_grid/set_hspeed/set_vspeed (the uses_grid_movement check at 2264-2279), e.g. arrow/space key -> create_instance (shooting), jump_to_position, or move_fixed. Empirically confirmed: generating a handler for a keyboard event with create_instance yields `i key == 275:` and `compile()` raises SyntaxError: invalid syntax. Since every scene module imports every object module, the entire exported game fails at startup. (Secondary gap in the same function: key_map only contains the 4 arrow keys; any other key gets code '0' and its actions can never fire.)
  - **Failure:** A student adds a keyboard event 'press right -> create bullet' to an object and exports to Android/EXE/Linux. The generated objects/<name>.py contains `i key == 275:`; the exported game (or APK) crashes immediately at launch with SyntaxError before any window appears.
  - **Suggested fix:** Change line 2298 to `if_keyword = "if" if i == 0 else "elif"` (match line 2487). Also consider extending key_map beyond the 4 arrows or warning on unmapped keys.
  - **Verifier:** Empirically reproduced: kivy_exporter.py:2297 has `if_keyword = "i" if i == 0 else "eli"`; generating a handler for a keyboard event with create_instance yields `i key == 275:` and compile() raises SyntaxError. The non-grid path runs whenever no keyboard action is if_on_grid/set_hspeed/set_vspeed, and since every generated scene imports every object module (line 1143) the exported game (used by the Mobile-Kivy, Android, EXE, Linux, macOS paths, which all reuse this KivyExporter) dies at import before any window appears.
  - **Vote:** confirmed: Reproduced end-to-end. Line 2298 of export/Kivy/kivy_exporter.py contains the literal typo `if_keyword = "i" if i == 0 else "eli"` (sibling _generate_keyboard_press_handler at 2487 has the correct "if"/"elif"). The path is reachable exactly as described: _generate_event_methods (line 2196) sends all 'keyboard' events to _generate_keyboard_handler, and the uses_grid_movement check (2264-2279) only diverts when actions include if_on_grid/set_hspeed/set_vspeed — so e.g. 'right -> create_instance' hits the broken branch. Calling the generator directly with such an event produced `        i key == 275:  # right`, and compile() of the resulting class raised SyntaxError: invalid syntax. The broken method is embedded in the generated objects/<name>.py (template at 2126-2151), which scene modules import unconditionally (`from objects.{obj} import ...`, line 1143), so the exported game (Kivy underlies the android/exe/linux/ios/macos exporters) crashes at import time before any window appears. The secondary key_map gap is also accurate: only the 4 arrow keys are mapped; other keys get code '0'.
  - **Vote:** confirmed: The typo is real and unguarded. export/Kivy/kivy_exporter.py:2298 reads `if_keyword = "i" if i == 0 else "eli"`, while the sibling press-handler at line 2487 has the correct `"if"/"elif"` — clearly a typo, not intent. The path is live: _generate_event_methods (line 2196-2199) calls _generate_keyboard_handler for any object with continuous 'keyboard' events (key_name set, not 'nokey'), and the broken branch runs whenever none of those events contain if_on_grid/set_hspeed/set_vspeed (the grid check at 2264-2279 diverts to a separate generator). Empirically reproduced: generating a handler for 'press right -> create_instance' emits `i key == 275:  # right`, and compile() raises SyntaxError. Hunted for existing guards and found none: no py_compile/compile validation anywhere in export/, no post-processing of generated object files, no tests covering on_keyboard generation (tests/test_exporters.py has zero keyboard-handler coverage), and git log shows no fix landed (last touches a051183 and earlier predate or preserve the typo). The Kivy exporter is the shared backend for android/exe/linux/macos/ios exporters, so an exported game whose object uses a non-grid keyboard event ships a module that is a SyntaxError at import. This is a deterministic exporter bug on trusted user-authored input, not a threat-model question. (Secondary key_map-only-4-arrows gap also verified present at lines 2282-2287, defaulting unknown keys to code '0'.)

- [x] **H9 — `export/Kivy/project_adapter.py:79` — Adapter converts background_color to an RGBA float list, but KivyExporter expects a hex string — the IDE's 'Mobile (Kivy)' export always fails** *(fixed `15b1f79`, tests/test_kivy_codegen_regressions.py)*
  - *Found by:* export-mobile; *category:* correctness
  - adapt_project_for_kivy_export rewrites every room's background_color via parse_color -> utils.color.to_kivy_rgba, which returns a List[float] ([r,g,b,a] in 0-1). KivyExporter._generate_main_app (kivy_exporter.py:222-224) then does `bgc = rd.get('background_color', '#808080'); if bgc.startswith('#'):` — a list has no .startswith, so AttributeError is raised for every project with at least one room; export()'s blanket except catches it and returns False. Real projects store background_color as a hex string (e.g. samples/maze_3: "#c0c0c0"), and the adapter sets the key unconditionally, so the failure is universal on this path. This is exactly the path wired to the export dialog: dialogs/project_dialogs.py:457 calls export_with_adapter for platform 'Mobile (Kivy)'/'Mobile (APK)'. (_generate_scene at kivy_exporter.py:1106 has the same .startswith crash per room.) The Android/EXE/Linux/macOS exporters bypass the adapter (BaseKivyExporter passes raw project.json data with hex strings) and are unaffected.
  - **Failure:** A teacher opens any project (e.g. bundled maze_3), chooses Export -> 'Mobile (Kivy)', picks an output folder, and always gets 'Export Failed. Check console for errors.' — the feature is 100% broken; the console shows AttributeError: 'list' object has no attribute 'startswith'.
  - **Suggested fix:** In the adapter, pass background_color through unchanged (keep the hex string), or teach KivyExporter's two color-parsing sites to accept both hex strings and [r,g,b,a] lists. Also note the adapter drops 'background'/'tile_horizontal'/'tile_vertical', so background images would be silently lost on this path even after the crash is fixed.
  - **Verifier:** Empirically reproduced: project_adapter.py:79 unconditionally rewrites background_color via to_kivy_rgba (utils/color.py:57 returns List[float]); KivyExporter._generate_main_app:223 then calls bgc.startswith('#') and export() fails with AttributeError 'list' object has no attribute 'startswith' for any project with at least one room. This is exactly the IDE dialog path (dialogs/project_dialogs.py:429/457 routes 'Mobile (Kivy)'/'Mobile (APK)' through export_with_adapter), so the feature is unconditionally broken; the other exporters bypass the adapter and pass raw hex strings.
  - **Vote:** confirmed: Reproduced the crash live, step by step. (1) project_adapter.py:79 unconditionally rewrites every room's background_color via parse_color -> utils.color.to_kivy_rgba (utils/color.py:57), which returns List[float]; reproduction with maze_3-style data ("#c0c0c0") yields [0.7529, 0.7529, 0.7529, 1.0]. (2) KivyExporter._generate_main_app (export/Kivy/kivy_exporter.py:222-223) does `bgc = rd.get('background_color', '#808080'); if bgc.startswith('#')` — running export_with_adapter against a one-room project raises AttributeError: 'list' object has no attribute 'startswith' at line 223, caught by export()'s blanket except (kivy_exporter.py:103-107), which returns False; export_with_adapter then returns False. (3) The wiring matches: dialogs/project_dialogs.py:429 routes both 'Mobile (Kivy)' and 'Mobile (APK)' to _export_kivy, which calls export_with_adapter (line 457) and only shows success on True. (4) The failure is universal on this path: the adapter sets the key for every room (even the room-level default [0,0,0,1] becomes a float list), all bundled samples store hex strings (e.g. samples/maze_3/rooms/*.json: "#c0c0c0"), and any project with at least one room hits the crash before any room-specific branch (_generate_scene at line 1106 has the identical bug as a second kill site). Only a zero-room project would survive _generate_main_app's loop, and such a project isn't a realistic export target. The claim that other exporters bypass the adapter is consistent with the code (only project_adapter.export_with_adapter applies parse_color).
  - **Vote:** confirmed: Empirically reproduced end-to-end: project_adapter.py:79 unconditionally converts each room's background_color via to_kivy_rgba, which always returns a List[float] (utils/color.py:57-92, all branches), while kivy_exporter.py:223 (_generate_main_app) and :1106 (_generate_scene) call bgc.startswith('#') with no type guard. Running export_with_adapter on a representative one-room project with a hex "#c0c0c0" color raised AttributeError: 'list' object has no attribute 'startswith' at kivy_exporter.py:223; export()'s blanket except (lines 103-107) caught it and returned False, which dialogs/project_dialogs.py:457 (the 'Mobile (Kivy)'/'Mobile (APK)' path via line 429) surfaces as a failed export. Guard hunt found nothing: tests/test_exporters.py never exercises the adapter→exporter path, git log on export/Kivy/ shows no fix, and the only non-failing case is a project with zero rooms. The threat model is irrelevant here — this breaks trusted, user-authored projects.

- [x] **H10 — `importers/gmk_converter.py:690` — Path traversal / arbitrary file overwrite via untrusted GMK resource names** *(fixed `742f886`, tests/test_gmk_name_traversal.py)*
  - *Found by:* importers; *category:* security
  - GmkConverter builds every output file path by interpolating the resource name read verbatim from the .gmk binary, with no sanitization (no _safe_join / _safe_asset_path like core/project_manager.py and utils/resource_packager.py already use). GmkParser.read_string returns whatever bytes the file contains, so a hand-crafted .gmk can name an object, sprite, sound, background, or room with '../' components and escape the output project directory. Affected write sites all use the raw name: objects '_write_object_file' (self.output_dir / "objects" / f"{name}.json"), rooms '_convert_single_room' (self.output_dir / "rooms" / f"{name}.json", line 828), sprites (self.output_dir / "sprites" / f"{name}.png", lines 237/269), sounds (self.output_dir / "sounds" / file_name, line 326/337), backgrounds (line 365). Path's '/' operator does not collapse '..', so 'name' = '../../../.config/autostart/x' writes attacker-controlled JSON/PNG/audio bytes into any directory that already exists relative to the project folder.
  - **Failure:** A teacher downloads a student's (or a forum) .gmk and uses File > Import GameMaker .gmk File. The malicious .gmk contains an object named '../../../../home/teacher/.bashrc' (or a sound named '../../.config/...'). During import the converter writes attacker-controlled file contents outside the chosen output folder, overwriting/creating files on the teacher's machine.
  - **Suggested fix:** Add a shared _safe_join/_safe_asset_path guard (mirroring core/project_manager.py._safe_asset_path) and route all converter write paths (objects, rooms, sprites, sounds, backgrounds) through it; skip with a warning any resource name that resolves outside its asset subdirectory.
  - **Verifier:** GmkConverter builds every write path by interpolating the resource name read verbatim via read_string (gmk_reader.py:153, decodes arbitrary bytes incl. '/' and '..') with no _safe_join/_safe_asset_path guard: _write_object_file output_dir/'objects'/f'{name}.json' (:690), rooms (:828), sprites _bgra_to_png(... output_dir/'sprites'/file_name) (:237/269), sounds out_path.write_bytes (:326), backgrounds (:367). Path's '/' does not collapse '..', so a name like '../../../.bashrc' in an untrusted .gmk escapes the output dir — same exploitable class the repo already hardened in resource_packager.py and project_manager.py.
  - **Vote:** confirmed: Reproduction holds end to end. GMK is an untrusted foreign format. Resource names are read verbatim by gmk_reader.py:153 read_string() (raw bytes decoded latin-1, no filtering of '/' or '..'), assigned directly to GmkObject/GmkSprite/etc .name in gmk_parser.py, and interpolated unsanitized into every output path: _write_object_file (gmk_converter.py:690 output_dir/"objects"/f"{name}.json"), rooms (828), sprites (237/269), sounds (326), backgrounds (367). I confirmed there is NO _safe_join / _safe_asset_path / is_relative_to / relative_to guard anywhere in gmk_converter.py — unlike core/project_manager.py and utils/resource_packager.py which were already hardened. Path's '/' operator does not collapse '..', the subdirs exist (created at line 151), and open(path,"w") resolves '..' at the OS level, so a name like '../../../../home/teacher/.bashrc' escapes the chosen output dir and writes attacker-controlled bytes. Reachable from File > Import GameMaker .gmk File (ide_window.py:739 -> gmk_importer.import_gmk_detailed -> GmkConverter). Real exploitable path-traversal/arbitrary-file-write of the same class previously fixed elsewhere in the repo.
  - **Vote:** confirmed: The finding holds up. GMK is an untrusted foreign format per the threat model, and the .gmk binary's resource names flow verbatim into output paths with zero sanitization.

Trace: GmkReader.read_string (gmk_reader.py:153) returns whatever bytes the file contains, decoded latin-1, no filtering. The converter uses these names raw at every write site I checked: objects `self.output_dir / "objects" / f"{name}.json"` (_write_object_file, line 690), rooms `f"{name}.json"` (line 828), sprites `file_name = f"{name}.png"` (line 235-236), sounds `name = gmk_snd.name; file_name = f"{name}.{ext}"` (lines 317/325-326), backgrounds (line 365). None of these route through any _safe_join/_safe_asset_path/is_relative_to guard.

I grepped gmk_converter.py and all importers/*.py for "safe", "sanitiz", "relative_to", "../" — no matches. The caller chain (gmk_importer.import_gmk_detailed → GmkConverter.convert) adds no name validation; it only checks the .gmk file exists and has the right suffix.

Notably, the same path-traversal class was explicitly fixed in core/project_manager.py (_safe_asset_path) and utils/resource_packager.py (_safe_join) per the CLAUDE.md security notes (2026-06-09), but gmk_converter.py was never given the equivalent guard — this is a genuine gap left out of that hardening pass, not a false positive already covered.

I confirmed the mechanics: `Path('/tmp/projtest') / 'objects' / '../../../../tmp/EVIL.json'` resolves to `/tmp/EVIL.json`, escaping the base dir (Python's `/` operator does not collapse `..`, and open() resolves it at the OS level). A hand-crafted .gmk with an object/sound/sprite named with `../` components writes attacker-controlled JSON/PNG/audio bytes into any existing directory relative to the project folder (e.g. overwriting ~/.bashrc, creating ~/.config/autostart/x). No existing test (tests/) covers GMK name traversal. The failure scenario (teacher imports a student/forum .gmk via File > Import) is realistic for this educational tool.

- [x] **H11 — `runtime/game_runner.py:2628` — Mouse events authored in the IDE (and imported from GMK) never fire: writers use flat 'mouse_<button>_<type>' keys, runtime reads only nested events['mouse'][button]** *(fixed `92d4216`, tests/test_mouse_events.py)*
  - *Found by:* cross-data-integrity; *category:* correctness
  - The object editor's 'Add Mouse Event' selector stores top-level event keys like 'mouse_left_press' / 'mouse_left_release' / 'mouse_left_down' / 'mouse_wheel_up' (editors/object_editor/object_events_panel.py:437: event_key = f"mouse_{button.lower()}_{event_type}"). The GMK importer likewise emits top-level keys 'mouse_left_button', 'mouse_left_press', etc. (importers/gmk_mappings.py:169-171, resolve_event returns (name, None, {}) for event_type 6). But the live runtime dispatch (GameRunner.handle_mouse_press/release/motion, game_runner.py:2628-2637, 2695-2698, 2718-2721) reads ONLY a nested format: events['mouse']['left_button'|'right_button'|'middle_button'|'mouse_move']. No code anywhere in the runtime looks up any 'mouse_*' top-level key (grep over runtime/ finds none), and no migration converts the flat keys to the nested form. Conversely, no current writer (events panel, Blockly, GMK importer) produces the nested 'mouse' format — only the hand-built fixtures in tests/test_game_runner.py:1046 do, which is why the tests pass while real authored data is dead. (runtime/input_handler.py does read 'mouse_press'/'mouse_button' style keys, but InputMixin is dead code — GameRunner does not inherit it and defines its own same-named handlers.)
  - **Failure:** A student adds a 'Mouse Left Button' event to an object in the object editor (the dialog exists and saves successfully, the event shows in the events panel with its actions), then runs the game and clicks the object: nothing happens, no warning, no log. Same for every mouse event in a GMK-imported game (e.g., a point-and-click game imported from GM8 is completely unplayable).
  - **Suggested fix:** In GameRunner.handle_mouse_press/release/motion, additionally look up the flat keys the writers actually produce ('mouse_left_button', 'mouse_left_press', 'mouse_left_release', 'mouse_left_down', wheel variants, etc.), or normalize flat keys into the nested events['mouse'] structure at project load. Add a regression test that round-trips an event created via the editor's key format through the runtime dispatcher.
  - **Verifier:** Traced both ends: object_events_panel.py:437 and gmk_mappings.py (GM_MOUSE_SUBEVENT) write only flat top-level mouse_* keys, while the only live dispatchers (handle_mouse_press/release/motion, game_runner.py:2594-2725) read exclusively nested events['mouse']['left_button'/'left_button_released'/'mouse_move']; repo-wide grep finds no flat-key reader, no migration, and InputMixin is dead (GameRunner at :1735 inherits nothing). Every IDE-authored and GMK-imported mouse event is silently inert in normal use — a whole event category dead, hence high.
  - **Vote:** confirmed: Reproduction verified line by line. Writers emit flat top-level keys: the IDE events panel builds event_key = f"mouse_{button.lower()}_{event_type}" and stores it top-level (object_events_panel.py:437-454), and the GMK importer's resolve_event returns (name, None, {}) for event_type 6 with names like mouse_left_button (gmk_mappings.py:92-116, 169-171), which _convert_events writes as events[name] = event_data with no nesting (gmk_converter.py:515-521). The live runtime dispatch reads ONLY the nested form: handle_mouse_press/release/motion check 'if "mouse" in events' then events["mouse"][button_name] with button_name in {left_button, right_button, middle_button, *_released, mouse_move} (game_runner.py:2628-2632, 2695-2698, 2718-2721). Repo-wide grep confirms no migration converts flat to nested, and the only producer of the nested 'mouse' dict is hand-built test fixtures (tests/test_game_runner.py:1046 etc.), explaining green tests with dead real-world data. InputMixin (runtime/input_handler.py) is indeed dead: class GameRunner (game_runner.py:1735) has no bases, and even InputMixin reads events["mouse_press"][button] nested-by-type keys, not the flat mouse_left_press keys writers produce. So an authored or GMK-imported mouse event silently never fires — the scenario reproduces exactly.
  - **Vote:** confirmed: Verified end-to-end: the runtime's only mouse dispatch (game_runner.py:2628/2695/2718) reads the nested events['mouse'][button_name] form exclusively, while both real writers produce flat top-level keys — the IDE events panel writes f"mouse_{button}_{event_type}" (object_events_panel.py:437-454) and the GMK importer writes mouse_left_button/mouse_left_press/etc. flat because resolve_event returns sub_key=None for event_type 6 (gmk_mappings.py:169-171, gmk_converter.py:515-521). Hunted for existing guards and found none: no migration/normalization anywhere converts flat to nested (codebase-wide grep for code constructing events['mouse'] finds only the runtime readers and hand-built test fixtures at tests/test_game_runner.py:1046); no other dispatch loop fires flat mouse_* keys (event iterations handle only collision_with_*, not_collision_with_*, keyboard, Thymio); GameRunner does not inherit InputMixin (class GameRunner: at line 1735), so input_handler.py's scheme is dead code as the finding states; no sample project uses mouse events, explaining why the gap is unobserved. Authored and GMK-imported mouse events silently never fire.

- [ ] **H12 — `utils/project_file_merge.py:21` — Room-file merge drops tiles/backgrounds/views on load; next project save permanently destroys them in rooms/<name>.json**
  - *Found by:* cross-data-integrity; *category:* data-integrity
  - The GMK converter writes a room's 'tiles', 'backgrounds' (8 parallax layers), 'views_enabled'/'views' and 'creation_code' ONLY to rooms/<name>.json; its project.json entry is metadata-only (importers/gmk_converter.py:796-848 writes the full room_file_data to the room file, then returns an entry without those keys). But all three loaders (editor core/project_manager.py:325, runtime runtime/game_runner.py:1901, export export/base_exporter.py:98) merge the room file through merge_room_file, whose _ROOM_FILE_KEYS whitelist copies only 'width', 'height', 'background_color', 'background_image', 'tile_horizontal', 'tile_vertical' plus 'instances'. So tiles/backgrounds/views never reach memory: the runtime renders no tile layer, no layered backgrounds, views stay off (GameRoom reads room_data.get('tiles', []) / .get('backgrounds', []) / .get('views_enabled', ...) from the merged dict). Worse, ProjectManager._save_rooms_to_files (core/project_manager.py:654) then rewrites rooms/<name>.json from the merged in-memory dict via _atomic_write_json(room_file, room_data), erasing those keys from the only file that had them. Verified by direct reproduction: merge_room_file on a converter-shaped entry + file leaves 'tiles'/'backgrounds'/'views_enabled' absent from the merged dict. Repo history confirms the bug in the wild: commit a3d981e had to manually 'sync project.json's embedded asset cache so the runtime loads the ... tiles' for samples/plateforme_2 — a hand-applied workaround for exactly this drop (the original import 37c3e9a had 147 tiles in the room file and zero in project.json).
  - **Failure:** A teacher imports an old GameMaker .gmk platformer that uses tile decorations and background layers (e.g., the shipped plateforme_*.gmk files). On Test Game, all tile graphics and parallax backgrounds are missing and room views are disabled. The teacher then presses Ctrl+S to save the project: every rooms/<name>.json is rewritten without its 'tiles'/'backgrounds'/'views'/'creation_code' keys, permanently destroying the imported level art with no warning and no undo (the only remaining copy is the original .gmk).
  - **Suggested fix:** Add 'tiles', 'backgrounds', 'views', 'views_enabled', 'bg_hspeed', 'bg_vspeed', 'bg_stretch', 'persistent' (and 'creation_code' for round-trip fidelity) to _ROOM_FILE_KEYS in utils/project_file_merge.py so the single-sourced kernel propagates them to the editor, runtime, and exporter loaders alike. Alternatively (or additionally), make the GMK converter embed these keys in the project.json room entry like editor saves do.
  - **Verifier:** Reproduced: merge_room_file's _ROOM_FILE_KEYS whitelist (project_file_merge.py:21) drops tiles/backgrounds/views_enabled/views/creation_code, which the GMK converter writes only to rooms/<name>.json (gmk_converter.py:797-848, project entry omits them); GameRoom reads them from the merged dict (game_runner.py:1158/1165/1198) so imported tile art never renders, and _save_rooms_to_files (project_manager.py:654) rewrites the room file from the merged dict, permanently erasing the only copy. Commit a3d981e's manual 'sync project.json embedded cache so the runtime loads the tiles' confirms the bug in the wild.
  - **Vote:** confirmed: Reproduced end to end. (1) importers/gmk_converter.py writes 'tiles', 'backgrounds' (8 padded layers), 'creation_code', 'views_enabled'/'views' ONLY into rooms/<name>.json (room_file_data), while the returned project.json entry carries just metadata (width/height/colors/instance_count, instances=[]). (2) utils/project_file_merge.py:21 _ROOM_FILE_KEYS whitelists only width/height/background_color/background_image/tile_horizontal/tile_vertical (+instances), and all three loaders (core/project_manager.py:325, runtime/game_runner.py:1901, export/base_exporter.py:98) merge exclusively through merge_room_file — direct execution of merge_room_file on converter-shaped entry+file confirms 'tiles'/'backgrounds'/'views_enabled'/'views'/'creation_code' are all absent from the merged dict. (3) The runtime consumes the merged dict: game_runner.py reads room_data.get('backgrounds', []) (line 1158), .get('tiles', []) (1165), .get('views_enabled', ...) (1198), so imported tile art, parallax layers, and views are silently missing on Test Game; the room editor likewise reads tiles from the merged in-memory asset (editors/room_editor/__init__.py:386), so it can't re-add them. (4) The save is destructive: ProjectManager._save_rooms_to_files writes _atomic_write_json(room_file, room_data) from the merged in-memory dict (core/project_manager.py:~654); its only preservation guard re-reads the existing file solely for 'instances' (and only when the room wasn't loaded this session), so for a normally loaded imported project the rewrite erases tiles/backgrounds/views/creation_code from the one file that had them. (5) In-the-wild confirmation: commit a3d981e ("Add a 127-tile visual layer to niveau_01 ... sync project.json's embedded asset cache so the runtime loads the ... tiles") is a hand-applied workaround embedding tiles into project.json because room-file tiles never reach the runtime — exactly this bug. Untrusted-input framing is irrelevant; this is data loss on the trusted editor/runtime/save path for any GMK-imported (or future tile-bearing) project.
  - **Vote:** confirmed: Verified end-to-end with the real code. (1) gmk_converter.py writes tiles/backgrounds/views_enabled/views/creation_code ONLY to rooms/<name>.json; its returned project.json entry omits them. (2) merge_room_file's _ROOM_FILE_KEYS (utils/project_file_merge.py:21) copies only width/height/background_color/background_image/tile_horizontal/tile_vertical + instances, so those keys never reach memory — reproduced directly, and GameRoom (runtime/game_runner.py:1158/1165/1198) reads them from the merged dict, so tiles/parallax/views are silently missing at runtime. (3) A live ProjectManager.load_project + save_project run on a converter-shaped temp project showed rooms/<name>.json rewritten WITHOUT tiles/backgrounds/views/creation_code (instances kept) — permanent destruction; the empty-instances preservation branch in _save_rooms_to_files doesn't apply since GMK rooms have instances. (4) No existing guards anywhere: the IDE import flow ends in load_project (the lossy path), gmk_importer.py does no project.json enrichment, no tests pin these keys, and commit a3d981e is a hand-applied workaround ('sync project.json's embedded asset cache so the runtime loads the... tiles' for plateforme_2) proving the bug occurred in the wild — hand-synced samples survive only because merge_room_file doesn't delete keys already embedded in project.json, which fresh imports lack. This is a data-loss correctness bug on a legitimately imported (and explicitly untrusted-format) .gmk, independent of the trust model.

- [x] **H13 — `widgets/asset_tree/asset_operations.py:156` — Deleting any asset reloads the project from stale disk state — silently discarding unsaved imports/reorders and closing every open editor without a save prompt** *(fixed `a84887e`, tests/test_asset_tree_state_sync.py)*
  - *Found by:* cross-state-sync; *category:* data-integrity
  - AssetOperations.delete_asset → remove_asset_from_project() loads project.json FROM DISK (load_project_data, line 367), removes the asset, and writes that disk copy back (line 439). Disk is stale relative to memory by design: sprite imports only update assets_cache/current_project_data until the next save (commit e950e4c note), drag-reorders only mark_dirty (asset_tree_widget.py dropEvent comment: 'We don't save eagerly here'). Then step 3 calls self.tree.force_project_refresh() (line 156) → project_manager.load_project() → in-memory state (cache, current_project_data, dirty flag) is replaced by the stale disk state, and ide_window.on_project_loaded (ide_window.py:3838-3846) force-closes ALL open editor tabs and detached windows via removeTab()+deleteLater(), bypassing the unsaved-changes prompt that close_editor_tab (ide_window.py:976-989) normally shows. The project-level 30s auto-save and the editors' 3s auto-save only shrink the window; both are user-toggleable and the loss is silent (dirty flag is even reset to False, so no warning on exit). The same force_project_refresh() reload also runs after import_package (asset_tree_widget.py:751) without a prior save.
  - **Failure:** A student imports three new sprite PNGs (memory-only until save), then within the same half-minute right-clicks the obsolete old sprite → Delete → Yes. remove_asset_from_project rewrites project.json from the stale disk copy (which never contained the three imports) and force_project_refresh reloads it: the three imported sprites vanish from the tree and the project (their PNGs sit orphaned in sprites/), any drag-reordered room order reverts, and every open editor — including one with un-flushed object-event edits — is closed with no 'Save changes?' prompt.
  - **Suggested fix:** Make deletion operate on the live in-memory model instead of disk: route it through asset_manager.delete_asset / project_manager (which already keep cache + project data aligned) followed by project_manager.save_project(), and replace force_project_refresh()'s load_project() with asset_tree.refresh_from_project(ide.current_project_data). If a disk reload must stay, save_project() first and make on_project_loaded respect editors' unsaved state.
  - **Verifier:** remove_asset_from_project (asset_operations.py:367/439) round-trips the stale on-disk project.json (sprite imports never write it: asset_manager.import_asset only updates assets_cache, ide_window.on_asset_imported only updates current_project_data), then delete_asset step 3 calls force_project_refresh → load_project, which resets is_dirty_flag=False (project_manager.py:278) and emits project_loaded → on_project_loaded (ide_window.py:3838-3846) force-closes all tabs and detached editors with no unsaved-changes prompt (unlike close_editor_tab:976-989). Unsaved imports and editor edits are silently discarded; default 5-min auto-save only shrinks the window.
  - **Vote:** confirmed: Reproduction verified line by line. (1) Sprite imports are memory-only: AssetManager.import_asset (core/asset_manager.py:136-171) copies the PNG and updates assets_cache; on_asset_imported (core/ide_window.py:3199-3210) updates current_project_data in memory; no project.json write — corroborated by commit e950e4c's own message ('a freshly imported sprite lives only in the IDE's in-memory project data until the project is saved'). (2) delete_asset → remove_asset_from_project (widgets/asset_tree/asset_operations.py:367/439) loads the stale project.json from disk and writes it back minus the deleted asset. (3) Step 3 (asset_operations.py:156) calls force_project_refresh (asset_tree_widget.py:1073-1088), whose parent walk reaches the IDE window (asset tree is AssetTreeWidget(self), ide_window.py:901; ide has project_manager, line 64) and calls project_manager.load_project(), which wholesale-replaces the cache (load_assets_from_project_data does self.assets_cache = {}, asset_manager.py:722), replaces current_project_data, and resets is_dirty_flag=False (project_manager.py:278) — unsaved imports and drag-reorders are discarded and the dirty flag is cleared, so no exit warning. (4) load_project emits project_loaded (line 285) → on_project_loaded (ide_window.py:3834-3846), which removeTab()+deleteLater()s every tab and destroys detached editors with no is_modified check, bypassing the unsaved-changes prompt in close_editor_tab (ide_window.py:976-989). _load_sprites/objects/rooms_from_files only merge side files for assets already listed in project.json, so they cannot recover memory-only imports. The trusted-project-files threat model is irrelevant: this is an IDE data-loss logic bug triggered by a routine delete, and the finding's auto-save caveats are accurately stated.
  - **Vote:** confirmed: Every link in the chain verified in code with no existing guard. delete_asset (asset_operations.py:100, wired at asset_tree_widget.py:414) calls remove_asset_from_project, which reads project.json from disk (line 367) and writes it back (line 439) without merging unsaved in-memory state; imports are memory-only (asset_manager.py:166 + ide_window.py on_asset_imported updates only cache/current_project_data, confirmed by commit e950e4c's own rationale) and drag-reorders only mark_dirty (dropEvent comment 'We don't save eagerly here', asset_tree_widget.py:171-176). Step 3 force_project_refresh (asset_operations.py:156 → asset_tree_widget.py:1083) calls project_manager.load_project, which replaces current_project_data and assets_cache from the stale disk copy and resets is_dirty_flag=False (project_manager.py:278), so the loss is silent. _load_sprites_from_files iterates only project.json keys, so orphaned imported PNGs are not re-discovered. The emitted project_loaded signal reaches on_project_loaded (ide_window.py:3838-3846), which removeTab+deleteLater's all tabs and destroys detached editors with no unsaved-changes prompt. Guards hunted and absent: no save-before-delete, no dirty check before reload, auto-saves are toggleable and only shrink the window, and no recent commit fixes this path. The trusted-project-files scoping doesn't refute it: this is silent data loss from normal user actions, not an untrusted-input issue.

- [x] **H14 — `widgets/asset_tree/asset_tree_widget.py:645` — Configure Sprite Animation is immediately reverted by a disk reload — frame settings never stick** *(fixed `a84887e`, tests/test_asset_tree_state_sync.py)*
  - *Found by:* cross-state-sync; *category:* data-integrity
  - configure_sprite_animation() calls asset_manager.update_sprite_animation(), which mutates ONLY the in-memory assets_cache (core/asset_manager.py:267-299; the asset_updated signal merely mark_dirty()s the project). On the very next line it calls self.force_project_refresh(), which runs project_manager.load_project(current_path) (asset_tree_widget.py:1083) — a full reload of project.json + sprites/<name>.json FROM DISK that rebuilds assets_cache and current_project_data and resets is_dirty_flag. Nothing wrote the new frames/frame_width/frame_height/speed/animation_type to disk in between (both calls happen in the same synchronous stack, so neither the 30s project auto-save timer nor any other event can fire). The just-configured animation metadata is therefore deterministically discarded, after which a success dialog reports 'Sprite configured with N frames'. Misleadingly, the thumbnail DOES update (update_sprite_animation regenerates the thumbnail PNG file on disk, cropping the first frame), so the tree looks right while the frames metadata is gone.
  - **Failure:** A teacher imports a 4-frame walk-cycle strip, right-clicks the sprite → 'Configure Animation...', sets 4 frames / frame size / FPS, clicks OK, and sees 'Sprite configured with 4 frames at 10 FPS'. The settings were already wiped by the reload: the sprite editor and the runtime still treat the image as a single wide frame, so the game draws the whole strip as one sprite. Re-doing the dialog fails the same way every time.
  - **Suggested fix:** Persist before (or instead of) reloading: after update_sprite_animation() call self.project_manager.save_project(), or drop force_project_refresh() entirely and update the tree item from the live cache (e.g. refresh_from_project(ide.current_project_data)) — reloading the whole project from disk to refresh one icon is the root problem.
  - **Verifier:** Traced fully: update_sprite_animation (core/asset_manager.py:277-286) mutates only assets_cache; the asset_updated signal reaches on_asset_changed which only mark_dirty()s (project_manager.py:1007-1009). The very next synchronous call force_project_refresh (asset_tree_widget.py:1083) runs load_project, whose load_assets_from_project_data does `self.assets_cache = {}` and rebuilds from the disk read (asset_manager.py:722-728), and resets is_dirty_flag=False (project_manager.py:278). No disk write of frames/frame_width/speed happens in between, so the animation config is deterministically discarded before the 'Sprite configured with N frames' success dialog — the feature can never persist via this path.
  - **Vote:** confirmed: Walked the exact path; every step holds. (1) widgets/asset_tree/asset_tree_widget.py:628-645: after SpriteStripDialog accepts, configure_sprite_animation calls asset_manager.update_sprite_animation(...) and then immediately self.force_project_refresh() in the same synchronous stack — no save call in between. (2) core/asset_manager.py:267-299: update_sprite_animation mutates only assets_cache[sprite_name] (frames/frame_width/frame_height/speed/animation_type/origins) and writes nothing to disk except regenerating the thumbnail PNG (lines 288-294), which explains the misleadingly-correct tree icon; its asset_updated signal is connected only to ProjectManager.on_asset_changed (project_manager.py:129), which is just mark_dirty() (line 1007-1009) — no persistence. (3) force_project_refresh (asset_tree_widget.py:1073-1088) walks parents for 'project_manager'; the tree is constructed as AssetTreeWidget(self) in core/ide_window.py:901 and IDEWindow has self.project_manager (ide_window.py:64), so the walk succeeds and project_manager.load_project(current_path) runs. (4) load_project (project_manager.py:235-292) re-reads project.json + sprites/<name>.json from disk into a brand-new project_data, then asset_manager.load_assets_from_project_data resets assets_cache = {} and rebuilds it from the disk data (asset_manager.py:718-728), replaces current_project_data, and sets is_dirty_flag = False — wiping both the in-memory animation settings and the dirty flag that would have let the 30s auto-save persist them later. Since both calls are in one synchronous Qt slot, no timer or event can save in between; the loss is deterministic, after which the success QMessageBox (lines 647-653) reports the configuration as applied. Re-doing the dialog repeats the same cycle. The only escape hatches fail: aliasing between cache and current_project_data doesn't help because load_project replaces both from disk, and the thumbnail write persists no metadata.
  - **Vote:** confirmed: Verified end to end with no existing guard. update_sprite_animation (core/asset_manager.py:267-299) mutates only assets_cache; its only disk write is the thumbnail PNG, and its asset_updated signal reaches only project_manager.on_asset_changed (project_manager.py:1007-1009) which merely mark_dirty()s (dirty_changed's sole consumer is the IDE window-title asterisk, ide_window.py:1137). configure_sprite_animation (asset_tree_widget.py:628-653) then synchronously calls force_project_refresh (line 1073-1088), which finds the IDE window's project_manager and calls load_project; load_project (project_manager.py:235-292) re-reads project.json and sprites/*.json from disk and load_assets_from_project_data (asset_manager.py:718-728) does assets_cache = {} and rebuilds from the disk read, resetting is_dirty_flag — so the new frames/frame_width/frame_height/speed/animation_type are deterministically wiped before the 'Sprite configured with N frames' success dialog shows. Same synchronous stack means the 30s auto-save can't intervene, and the post-reload clean dirty flag means even a later auto-save persists nothing. Guard hunt came up empty: no other definition of update_sprite_animation, zero tests reference configure_sprite_animation/update_sprite_animation, no fixing commit in git log for either file, and the e950e4c 'prefer live in-memory data' fix is the object editor's read path — it can't help because the in-memory data itself is replaced. The misleading-thumbnail detail also checks out (generate_thumbnail writes the cropped first-frame PNG to disk before the reload).

- [x] **H15 — `widgets/thymio_playground.py:664` — Zooming out below 100% in the Thymio Playground raises ValueError every frame, freezing the simulator view** *(fixed `4d5be10`, tests/test_thymio_playground_zoom.py)*
  - *Found by:* widgets-misc-config-main; *category:* correctness
  - _apply_zoom_and_pan computes view_width = playground_width / zoom_level and view_height = playground_height / zoom_level. For any zoom_level < 1.0 these exceed the world surface size. The clamp `src_x = max(0, min(src_x, self.playground_width - view_width))` clamps a negative bound to 0, producing src_rect = Rect(0, 0, int(view_width), int(view_height)) which is LARGER than the world surface. `world_surface.subsurface(src_rect)` then raises `ValueError: subsurface rectangle outside surface area`. Empirically reproduced with pygame 2.6.1 at zoom 0.9 on an 800x600 surface (src_rect <rect(0, 0, 888, 666)> -> ValueError). The exception is raised inside update_simulation, which is driven by a 60 FPS QTimer with no try/except, so it re-raises on every tick: the pygame display and status panel stop updating (the calls after _apply_zoom_and_pan never run) and stderr floods with tracebacks. Zoom levels below 100% are fully exposed in the UI: the zoom slider range is (25, 400), the toolbar has a 'Zoom Out (-)' action, and the '-' key calls zoom_out(); a single '-' press from the default 100% gives zoom 0.9 and triggers the bug.
  - **Failure:** A student opens the Thymio Playground and presses '-' once (or drags the zoom slider below 100%) to see more of the playground. The simulation view instantly freezes on the last rendered frame, the robot appears stuck, keyboard driving seems dead (the model still updates but nothing repaints), and the console spews a ValueError traceback 60 times per second until the user happens to press Home or zoom back to >= 100%.
  - **Suggested fix:** In _apply_zoom_and_pan (and screen_to_world/world_to_screen), clamp the source rect to the world surface: view_width = min(view_width, self.playground_width), view_height = min(view_height, self.playground_height) before building src_rect — or treat zoom_level < 1.0 as a scale-down of the entire world surface (smoothscale the full world_surface to (playground_width*zoom, playground_height*zoom) and blit centered). Alternatively set zoom_min = 1.0 and slider minimum 100 until shrink rendering is implemented.
  - **Verifier:** Reproduced on this box (pygame 2.6.1): zoom 0.9 on the 800x600 world gives view 888x666; the max(0, min(...)) clamp at thymio_playground.py:656-657 collapses the negative bound to 0, and subsurface(Rect(0,0,888,666)) raises 'ValueError: subsurface rectangle outside surface area'. zoom_min=0.25, slider range (25,400), toolbar 'Zoom Out (-)' and Key_Minus all reach zoom<1.0, and update_simulation (QTimer-driven, line 570) has no try/except, so the exception re-raises every tick and update_display/_update_status_panel never run — the view freezes after one '-' press.
  - **Vote:** confirmed: Reproduced line by line and empirically. widgets/thymio_playground.py: zoom < 1.0 is reachable from the UI (zoom_min=0.25 at line 217, slider range 25-400 at line 345, toolbar 'Zoom Out (-)' with '-' shortcut at lines 516-518, Key_Minus at line 918); one '-' press gives zoom 0.9. _world_surface is exactly playground_width x playground_height (800x600, line 213). In _apply_zoom_and_pan, view_width = 800/0.9 = 888.9 exceeds the surface; the max(0, min(src_x, width - view_width)) clamp at line 656 clamps the negative bound to 0, yielding src_rect Rect(0,0,888,666), and world_surface.subsurface(src_rect) at line 664 raises ValueError — verified live on this machine (pygame 2.6.1, Python 3.11.2): 'ValueError: subsurface rectangle outside surface area' with the exact rect from the finding. The file contains zero try: blocks; update_simulation is driven by a QTimer at ~60 FPS (lines 232-234), so the exception re-raises every tick, and pygame_widget.update_display() (line 631) and _update_status_panel() (line 634) are skipped, freezing the display until the user zooms back to >=100% or resets the view. This is local UI triggered by a single ordinary keypress, so the trusted-project-files caveat does not mitigate it.
  - **Vote:** confirmed: Reproduced against the real code: calling ThymioPlaygroundWindow._apply_zoom_and_pan (widgets/thymio_playground.py:636-668) with zoom_level=0.9 on an 800x600 world surface raises "ValueError: subsurface rectangle outside surface area" at line 664, exactly as claimed. For zoom < 1.0, view_width/view_height (playground / zoom) exceed the world surface; the clamp at lines 656-657 takes max(0, negative) = 0, yielding src_rect (0, 0, 888, 666) larger than the surface, and pygame.Surface.subsurface rejects it. No existing guard anywhere: zoom_min is 0.25 (line 217) and the slider range is (25, 400) (line 345), so sub-100% zoom is fully reachable via the '-' shortcut/toolbar zoom_out (one press from 100% gives 0.9) or the slider; set_zoom only clamps to [0.25, 4.0]; the world surface is allocated at exactly playground size (lines 213, 1047); the 60 FPS QTimer connects directly to update_simulation (lines 232-233) with no try/except on the path, so the exception recurs every tick and the display/status updates after line 628 never run. No tests cover thymio zoom and no commits since 'Layer 1 audit fixes' touch this file. This is first-party IDE code, so the trusted-input lens does not apply — it is a plain UI crash bug triggered by normal user interaction.


## Medium (61) — M1–M40 FIXED (M1–M20 2026-06-13, M21–M40 2026-06-14)

The first 20 medium findings were re-verified and fixed with regression
tests across commits `77974b2`..`5c9418b` (one commit per finding;
M3/M4/M5 share a commit — same rename path; M9/M10 share one — duplicate
findings; M17 was already resolved by H11). Suite 808 → 890 passed, 0 failed.

M21–M40 were then re-verified and fixed (2026-06-14), grouped into commits
by shared file/root-cause (M21+M22 playground, M23+M24 room undo, M25+M26
sprite editor, M30+M31 input conditions, M32+M33 aseba, M35+M36+M37 kivy
keyboard, M38+M39 desktop export). Each finding has a dedicated regression
test file. Suite 890 → 958 passed, 0 failed. **M41–M61 remain open** —
pick up from M41 below.

- [x] **M1 — `actions/thymio_actions.py:407` — Thymio sensor/sound choice options are stored as full label strings ('2 - Front Center'); the simulator parses them to 0, so every non-default sensor choice silently uses sensor 0 (and Aseba export emits invalid code)** *(fixed `77974b2`, tests/test_thymio_choice_labels.py)*
  - *Found by:* events-actions-plugins; *category:* correctness
  - thymio_read_proximity / thymio_if_proximity define sensor_index options like "0 - Front Left Far" ... "6 - Back Right" (lines 407-415, 506-514), thymio_read_ground / thymio_if_ground_dark / thymio_if_ground_light use "0 - Left" / "1 - Right" (441-444, 548-551, 576-580), and thymio_play_system_sound uses "0 - Startup" ... "7 - Target Detected" (366-375). The GM80 action dialog saves choice params as the raw currentText (gm80_action_dialog.py:336-337: `values[param_name] = widget.currentText()`), so the saved value is the full label. At runtime, thymio_action_handlers._parse_value (lines 414-427) tries int("2 - Front Center") → ValueError, then hasattr(instance, ...) → False, and returns 0. So sensor_index and sound_id collapse to 0 for every selection. Proof that full labels are the persisted format: the Aseba exporter explicitly splits sound_id on ' - ' (aseba_exporter.py:410-411) — but does NOT do the same for sensor_index, so _translate_if_proximity emits `if prox.horizontal[2 - Front Center] > 2000 then`, which is not valid Aseba.
  - **Failure:** A student builds a line-follower: 'If Ground is Dark' on sensor "1 - Right" turns one way, sensor "0 - Left" the other. In the simulator both conditions read int(_parse_value("1 - Right"))==0 — the LEFT sensor — so the robot can never distinguish sides and the lesson's logic is silently wrong. Exporting the same project to a real Thymio produces Aseba source with `prox.horizontal[1 - Right]` that fails to compile. Choosing any system sound always plays the Startup tone.
  - **Suggested fix:** Either store only the numeric value (use the label as display text and the index as the item's userData in gm80_action_dialog, mirroring conditional_editor's _add_canonical_items), or normalise at the consumers: in thymio_action_handlers add a helper that splits "N - Label" before _parse_value, and apply the existing sound_id split logic to sensor_index in the Aseba exporter.
  - **Verifier:** Traced end-to-end: GM80ActionDialog saves choice params as the full label via widget.currentText() (gm80_action_dialog.py:336-337), thymio_events_panel stores them unmodified, and _parse_value (thymio_action_handlers.py:417-427) returns 0 for any label like '1 - Right' (int() raises, hasattr fails), so all sensor_index/sound_id selections collapse to 0 in the simulator. The Aseba exporter splits only sound_id on ' - ' (aseba_exporter.py:408-411) and passes sensor_index raw into prox.horizontal[...] (:417-434), emitting invalid Aseba.

- [x] **M2 — `core/asset_manager.py:206` — replace_sprite_image deletes the old image and thumbnail BEFORE copying the new one — a failed copy destroys the original art** *(fixed `11cddb2`, tests/test_replace_sprite_image.py)*
  - *Found by:* core-managers-utils; *category:* data-integrity
  - The method unlinks the sprite's existing image file (line 206) and thumbnail (line 214) first, and only then calls shutil.copy2 on the new file (line 220). If the copy raises — source on an unplugged USB stick / network share, permission error, disk full — the except block returns None with the old file already gone: the sprite's `file_path` still points at the deleted image, the asset renders broken everywhere, and the original art is unrecoverable. This is the delete-then-write anti-pattern the rest of the save stack (atomic tmp+replace, snapshot/rollback) was specifically hardened against. Called from the asset tree's 'replace image' flows (widgets/asset_tree/asset_dialogs.py:190, asset_operations.py:777).
  - **Failure:** A teacher right-clicks a sprite and replaces its image with a file on a USB key that has been removed (or a file they lack read permission on). copy2 raises, the error dialog appears — and the sprite's existing image file and thumbnail have already been deleted from the project. The game now shows a missing sprite and the only copy of the old art is gone.
  - **Suggested fix:** Reorder: copy the new image to a temp name (or directly to the new dest, which usually differs only by suffix) first; only after the copy and Image.open validation succeed, delete the old file/thumbnail and update file_path. On any failure, leave the original files untouched.
  - **Verifier:** asset_manager.py:200-220 unlinks the old sprite image and thumbnail before shutil.copy2 of the new file; the except block (:245-251) only logs and returns None with no restore, and neither caller (asset_dialogs.py:190, asset_operations.py:777) snapshots or pre-validates readability. A copy2 failure (permissions, removed media, disk full) leaves file_path dangling and the project's only copy of the art deleted.

- [x] **M3 — `core/asset_manager.py:457` — Renaming a room silently moves it to the end of the room order, changing the game's start room (auto-saved immediately)** *(fixed `3a5c89b`, tests/test_rename_asset_references.py)*
  - *Found by:* widgets-assets; *category:* correctness
  - AssetOperations.rename_asset -> ProjectManager.rename_asset -> AssetManager.rename_asset updates the assets_cache OrderedDict by deleting the old key and inserting the new key, which appends the renamed asset at the END of the dict. For rooms, dict order IS the room order: no code in the repo writes 'room_order' (verified by grep), so the runtime falls back to `list(self.rooms.keys())[0]` (runtime/game_runner.py:2144) for the start room and key order for next-room navigation. ProjectManager.rename_asset then calls save_project() immediately (project_manager.py:1208), and save_assets_to_project_data() writes the cache order into project.json, persisting the corrupted order. The tree item is updated in place (refresh_asset_item_after_rename), so the tree still shows the old position — the user sees nothing wrong until they run the game or reload. The dropEvent docstring in asset_tree_widget.py confirms users actively depend on room dict order ('a user reordered the rooms in maze_3 expecting room3 to start first').
  - **Failure:** A teacher renames the first room of their project from 'room0' to 'menu' via the asset tree's Rename context action. The rename auto-saves. They press Test Game: the game now starts in what used to be the second room; 'go to next room' sequencing is also shifted. Nothing in the UI indicated the room was moved to the last position.
  - **Suggested fix:** In AssetManager.rename_asset, replace the key in place preserving position, e.g. rebuild the OrderedDict as `OrderedDict((new_name if k == old_name else k, v) for k, v in cache.items())`, instead of del-old + append-new.
  - **Verifier:** Confirmed: rename_asset does del-then-reassign on the cache (asset_manager.py:457-462), appending the renamed room last; ProjectManager.rename_asset auto-saves (:1208) and save_assets_to_project_data persists cache order. No production code writes 'room_order' (only tests and the GMK binary parser), so the runtime falls back to dict order (game_runner.py:2144, get_room_list), and the asset-tree dropEvent docstring confirms dict order is the room order users rely on. Re-graded medium: silently wrong start-room/next-room behavior, but no crash or data loss and recoverable via the existing drag-reorder feature.

- [x] **M4 — `core/asset_manager.py:570` — Renaming a background updates only the legacy single background_image field, not the 8-layer 'backgrounds' list rooms actually use** *(fixed `3a5c89b`, tests/test_rename_asset_references.py)*
  - *Found by:* widgets-assets; *category:* correctness
  - AssetOperations.rename_asset delegates to AssetManager.rename_asset -> _update_asset_references. The 'backgrounds' branch only rewrites `room_data.get('background_image')` (the legacy single-background field). But rooms configured via the properties panel's Background Layers dialog (enhanced_properties_panel.py BackgroundLayersDialog / _migrate_backgrounds) store layers in `room_data['backgrounds']` — a list of dicts each with its own 'background_image' key — and the runtime renders from exactly that list (`self.bg_layers = room_data.get('backgrounds', [])`, runtime/game_runner.py:1158). Those per-layer references are never updated on rename, leaving them dangling.
  - **Failure:** A teacher sets room1's background layer 0 to background 'sky' via Properties -> Backgrounds -> Configure, then renames the asset 'sky' to 'ciel' in the asset tree (which also renames the PNG). The rename completes 'successfully', but room1's layer still references 'sky': the room's background disappears in both the editor preview and the running game.
  - **Suggested fix:** In _update_asset_references's 'backgrounds' branch, also iterate `room_data.get('backgrounds', [])` and rewrite each layer dict whose 'background_image' == old_name.
  - **Verifier:** The backgrounds rename branch (asset_manager.py:570-577) rewrites only the legacy room_data['background_image'], but rooms configured through BackgroundLayersDialog store layers in room_data['backgrounds'] (enhanced_properties_panel.py:298, per-layer 'background_image' key at :1261/:1289) and the runtime renders exactly that list (game_runner.py:1158, name lookup at :1594 silently skips the now-dangling name). Rename also renames the PNG, so the layer reference breaks in editor and runtime.

- [x] **M5 — `core/asset_manager.py:598` — Renaming an asset does not update references nested in then_actions/else_actions, leaving stale names inside conditional blocks** *(fixed `3a5c89b`, tests/test_rename_asset_references.py)*
  - *Found by:* core-managers-utils; *category:* correctness
  - `_update_param_in_all_actions` (sprites/sounds/rooms) and `_update_actions_object_refs` (objects) only walk `event_data['actions']` and one level of sub-event dicts; neither recurses into an action's `parameters['then_actions']` / `parameters['else_actions']`. Nested action lists are a core, fully-supported structure (events/conditional_editor.py builds them; runtime/action_executor.py:427+ executes 'arbitrarily deep then_actions/else_actions'). So renaming a sprite, sound, room, or object updates top-level references but silently leaves stale names in any action placed inside an if/conditional block, while the rename is immediately auto-saved (ProjectManager.rename_asset calls save_project()), persisting the broken references with no undo.
  - **Failure:** A student builds 'if lives <= 0 then [goto_room(room="game_over")]', later renames the room 'game_over' to 'ecran_fin'. The nested goto_room still targets 'game_over'; at runtime the room change silently fails (and validate_project flags a phantom error for a room the student is sure they renamed everywhere). Same for create_instance(object=...) or set_sprite(sprite=...) inside any conditional's then/else branch.
  - **Suggested fix:** In both helpers, after processing an action's direct parameters, recurse into `params.get('then_actions', [])` and `params.get('else_actions', [])` (and any other action_list parameters) the same way `_check_actions_for_room_issues` in project_manager.py already does.
  - **Verifier:** _update_param_in_all_actions (asset_manager.py:582-614) and _update_actions_object_refs (:616-648) walk only event_data['actions'] and one level of sub-event dicts; neither recurses into parameters['then_actions']/['else_actions'], which are a fully supported nested structure built by events/conditional_editor.py and executed arbitrarily deep by action_executor.py:427-435 — and which project_manager.py's validation (:1576-1585) does recurse into. Renames therefore leave stale sprite/sound/room/object names inside conditional branches, immediately auto-saved.

- [x] **M6 — `core/ide_window.py:593` — Auto-Save Settings dialog persists to dead config keys — settings are lost on restart and the menu checkbox desynchronizes from the actual auto-save timer** *(fixed `bbb4929`, tests/test_auto_save_settings_persistence.py)*
  - *Found by:* dialogs; *category:* correctness
  - The AutoSaveSettingsDialog accept path (show_auto_save_settings) applies the choice for the session via set_auto_save(), then persists it as top-level Config keys: Config.set('auto_save_enabled', enabled) and Config.set('auto_save_interval', interval_seconds). But IDE startup wires the auto-save timer exclusively from the editor config written by the Preferences dialog: ide_window.py:79-82 reads Config.get_editor_config() ('editor.auto_save_enabled' and 'editor.auto_save_interval' in MINUTES). The top-level 'auto_save_interval' (seconds) is never read anywhere, and the top-level 'auto_save_enabled' is read only at ide_window.py:123 to set the menu checkbox display — not the timer. So the dialog's settings evaporate on restart, and the enabled flag lands in a key that controls only the checkbox, producing a UI/behavior mismatch.
  - **Failure:** A teacher opens Tools > Auto-Save Settings and disables auto-save (e.g., to experiment with a room without overwriting the saved version). It works for the session. After restarting the IDE, the timer is re-enabled from editor config (default: every 5 minutes) while the auto-save menu checkbox still shows unchecked (it reads the top-level key the dialog wrote). The project gets silently auto-saved — overwriting on-disk state the user believed was protected — while the UI claims auto-save is off. Conversely, an interval chosen in the dialog (e.g., 15s) never survives a restart.
  - **Suggested fix:** Persist the dialog's result to the same store the startup path reads: Config.set_editor_config(auto_save_enabled=enabled, auto_save_interval=max(1, round(interval_seconds/60))) — or change startup to read the top-level seconds keys and reconcile both UIs to one canonical key/unit. Also make the menu-checkbox initialization (line 123) read the same key as the timer wiring (line 80).
  - **Verifier:** show_auto_save_settings writes top-level Config keys (ide_window.py:592-593) that startup never reads — the timer is wired from Config.get_editor_config() (lines 79-82, the fix for audit item #29) and the top-level 'auto_save_enabled' is read only at line 123 to set the menu checkbox; 'auto_save_interval' (seconds) is read nowhere. Config persists via main.py's aboutToQuit save_on_exit, so after restart the timer re-enables from editor config while the checkbox shows the dialog's stale choice, exactly as described.

- [x] **M7 — `core/ide_window.py:1266` — Zip projects recorded in Recent Projects can never be reopened from the Recent menu (load_project has no zip branch)** *(fixed `2de3cd2`, tests/test_recent_zip_reopen.py)*
  - *Found by:* core-ide-window; *category:* correctness
  - open_project's zip branch loads via project_manager.load_project_from_zip and then records the .zip file path with add_to_recent_projects(str(file_path)) (line 1266). But the reopen path — Recent Projects menu -> open_recent_project (line 1276) -> load_project (line 1353) — passes the path straight to project_manager.load_project, which only handles folders: it computes `project_path / 'project.json'` against the .zip *file*, finds nothing, and returns False. The IDE then shows the generic 'Failed to load project' warning. So every zip-based project becomes a permanently dead Recent entry (the entry survives because Path(zip).exists() is True in update_recent_projects_menu line 1191).
  - **Failure:** A teacher receives a class project as project.zip, opens it via File > Open Project (works fine). Next session they click the project in Recent Projects — 'Failed to load project'. The entry never works and never disappears; they must re-navigate to the zip through File > Open Project every time, or conclude the file is corrupt.
  - **Suggested fix:** In open_recent_project / load_project, detect `.zip` paths and route them through the same ProjectCompressor.is_project_zip + project_manager.load_project_from_zip branch used by open_project (or record the extracted working folder in recents instead of the zip path).
  - **Verifier:** open_project's zip branch records the .zip path (ide_window.py:1266), but open_recent_project (1276) routes through load_project (1369) to project_manager.load_project (235-244), which computes zip_path / 'project.json', finds it missing, and returns False; load_project_from_zip (project_manager.py:1430) is reachable only from the File>Open dialog branch. The dead entry persists because Path(zip).exists() is True in update_recent_projects_menu (1191).

- [x] **M8 — `core/ide_window.py:1826` — test_game pre-run editor sync skips detached/floating editors — F5 in Floating window mode runs the game with stale data** *(fixed `ebe4f90`, tests/test_test_game_editor_sync.py)*
  - *Found by:* core-ide-window; *category:* correctness
  - Before launching the game, test_game syncs every open editor's live data into the project ('Sync all open editors' loop), but it iterates only self.editor_tabs. Editors floated via the toolbar 'Floating' mode or the per-editor float button are removed from editor_tabs (float_editor line 3688 removeTab) and live only in self.open_editors / self.detached_editor_windows, so they are never synced. The file already has the correct iterator for exactly this purpose (_iter_open_editors, line 1572, '...so that detached windows stay in sync with tabbed ones') and uses it in five other refresh paths — test_game is the one place that didn't get it. The editor-level 3-second auto-save narrows but doesn't close the gap: the canonical workflow is tweak-a-value-then-immediately-F5, and editors also support a Manual Save mode where auto-save is off entirely.
  - **Failure:** A student has window mode set to Floating (one toolbar click; persisted in config across sessions). They change an object's speed in the floated object editor and press F5 within the 3-second auto-save delay. The game launches with the old value; the student concludes their change 'didn't work' and changes it again, compounding confusion. With editor auto-save toggled off, no floated editor's changes are ever tested until manually saved, while tabbed editors in the identical situation are synced automatically.
  - **Suggested fix:** Replace the tab-only loop with `for widget in self._iter_open_editors():` (it already yields both tabbed and detached editors and skips the welcome tab).
  - **Verifier:** test_game's sync loop (ide_window.py:1826-1833) iterates only self.editor_tabs, while float_editor (3688) removes floated editors from the tab strip into detached_editor_windows/open_editors; _iter_open_editors (1572) exists precisely to cover both populations and is not used here. The editor-level fallback is a 3-second single-shot auto-save that is per-editor toggleable off (base_editor.py:73-79, 164-167), so the stale-data window is real, matching the finding's own caveats.

- [x] **M9 — `core/ide_window.py:2564` — Pressing Esc on the export progress dialog freezes the entire IDE for the remainder of the export (export_thread.wait() on the GUI thread)** *(fixed `c85068a`, tests/test_export_progress_dialog.py)*
  - *Found by:* core-ide-window; *category:* concurrency
  - _run_export_with_progress runs `progress_dialog.exec()` followed by `export_thread.wait()`. The modal QDialog can be dismissed at any moment with Esc (QDialog's default reject) — and for the exe/linux/macos exports there is no cancel button and no key/close-event suppression, so Esc is the natural thing a user tries. exec() then returns while the export thread is still running, and wait() blocks the Qt main thread until the PyInstaller/Buildozer build finishes — minutes of a completely frozen, 'not responding' IDE with no progress UI. For Android/iOS, Esc additionally bypasses cancel_export(), so exporter.cancel_requested is never set and the build cannot be aborted.
  - **Failure:** A teacher starts 'Export Game > Windows Executable', decides to abort, and presses Esc (or clicks the dialog's window-close button). The progress dialog disappears but the whole IDE immediately stops painting and responding for the duration of the multi-minute PyInstaller build; the OS offers to force-kill the app. If they wait it out, the success dialog eventually appears as if nothing happened.
  - **Suggested fix:** Either make the dialog truly unescapable while the thread runs (override reject()/closeEvent to no-op or to route through cancel_export), or replace the blocking wait() with a finished-signal handler: connect export_thread.finished to dialog accept and only deleteLater the thread there.
  - **Verifier:** Traced at ide_window.py:2563-2565: progress_dialog is a plain modal QDialog (2489) with no reject/keyPressEvent override, so Esc or the close button returns from exec() while ExportThread is still running, and export_thread.wait() then blocks the GUI thread with no event loop until the PyInstaller/buildozer build finishes. The cancel flag is only set via the Cancel button (2553-2554), which is absent for exe/linux/macos (show_cancel=False at 2251/2277/2303).

- [x] **M10 — `core/ide_window.py:2565` — Pressing Esc (or the title-bar X) on the export progress dialog freezes the entire IDE until the export thread finishes** *(fixed `c85068a` — duplicate of M9, same code path, tests/test_export_progress_dialog.py)*
  - *Found by:* cross-lifecycle; *category:* concurrency
  - _run_export_with_progress() builds a plain modal QDialog (no keyPressEvent/reject override, line 2489) and then runs `export_thread.start(); progress_dialog.exec(); export_thread.wait()`. QDialog's default Esc handling (and the window-manager close button) calls reject(), which returns from exec() immediately WITHOUT setting the cancel flag (the cancel flag is only set by the Cancel button's clicked handler, and several export targets don't even show a Cancel button — show_cancel defaults to False). Control then reaches `export_thread.wait()`, which blocks the GUI thread with no event loop running until ExportThread.run() completes. For the Android target that is a buildozer build ('this may take a long time on first run' — tens of minutes); for exe/linux/macos it's a PyInstaller run (minutes). During that time the IDE repaints nothing and the OS flags it 'not responding'.
  - **Failure:** A teacher starts an Android export, gets impatient at 'Building APK (this may take a long time on first run)...', and presses Esc on the progress dialog. The dialog vanishes and the whole IDE hard-freezes (grey window / 'not responding') for the remaining 10-30+ minutes of the buildozer run. Most users will force-kill the IDE, losing any unsaved editor state. Even for cancel-enabled targets, Esc bypasses cancel_export() entirely, so the flag-based cooperative cancel never engages.
  - **Suggested fix:** Make the dialog non-rejectable while the thread runs (override reject() to route through cancel_export, or setWindowFlags without close button and filter Esc), and replace the blocking wait() with a finished-signal driven teardown: connect export_thread.finished to the dialog's accept and only exec() once; if rejected-with-cancel, keep the UI alive (re-enter a local loop or poll with QTimer) instead of QThread.wait() on the GUI thread.
  - **Verifier:** Duplicate of finding 13, same code path confirmed: exec() returns on Esc/window-close without setting exporter.cancel_requested (cancel only wired to the button, and bypassed even for the cancel-enabled android/ios targets), then export_thread.wait() at 2565 hard-freezes the GUI thread for the remaining minutes of the build.

- [x] **M11 — `core/ide_window.py:3251` — Re-opening an already-open *modified* asset creates a duplicate editor because the focus-existing check doesn't strip the '*' dirty marker from tab text** *(fixed `5c24731`, tests/test_reopen_modified_editor.py)*
  - *Found by:* core-ide-window; *category:* data-integrity
  - All five open_*_editor methods (open_room_editor line 3251, open_playground_editor 3300, open_object_editor 3348, open_sprite_editor 3413, open_script_editor 3461) detect an already-open asset by comparing `self.editor_tabs.tabText(i) == name`. But on_editor_data_modified (line 3634-3637) renames the tab to `name + '*'` as soon as the editor has unsaved changes, so the comparison fails exactly when the editor is dirty. The code falls through and constructs a second editor for the same asset, overwriting `self.open_editors[name]` with the new instance. close_editor_by_name (line 3653) does `.replace('*','')` before comparing — proving the intended behavior — but the open paths don't.
  - **Failure:** A student edits a room (tab shows 'level1*'), then double-clicks 'level1' in the asset tree expecting to focus it. Instead a second 'level1' tab opens, loaded from last-saved data. The two editors now diverge: the new one is the only one registered in open_editors, so cross-editor refreshes, rename handling, and close_editor_by_name all target it; saving from the fresh editor silently clobbers nothing visible, but the original tab's unsaved changes are stranded — closing it later via the orphaned tab's prompt or just confusion leads to lost work and two tabs fighting over the same asset file.
  - **Suggested fix:** Strip the marker in the focus loops: `if self.editor_tabs.tabText(i).replace('*','') == room_name:` (mirror close_editor_by_name), or better, compare widgets: `if self.editor_tabs.widget(i) is self.open_editors[room_name]:`.
  - **Verifier:** Traced through the code: on_editor_data_modified (3634-3637) sets the tab text to name+'*', all five open_*_editor focus checks compare tabText(i) == name exactly (e.g. 3251-3254), so with a dirty editor neither return fires and control falls through to construct a second editor and overwrite open_editors[name] (3278); close_editor_by_name's .replace('*','') (3653) shows the intended normalization the open paths lack. Re-opening a modified asset from the tree is routine classroom behavior, and the stranded original tab's unsaved changes plus the re-keyed registry make this a real data-integrity bug.

- [x] **M12 — `core/ide_window.py:3838` — Switching projects (and closing the IDE) silently discards modified editors — is_modified is never consulted outside close_editor_tab** *(fixed `c4e83ae`, tests/test_modified_editor_preservation.py)*
  - *Found by:* core-ide-window; *category:* data-integrity
  - on_project_loaded tears down every editor tab with deleteLater and destroys every detached editor window without ever checking the editors' is_modified flag, and neither core/ide_window.py's load_project nor project_manager.load_project prompts about the dirty project being replaced. closeEvent (line 4133) checks only project_manager.is_dirty(), which editor modifications do NOT set (on_editor_data_modified only renames the tab; BaseEditor.on_data_modified only sets editor-local state). By contrast, closing a single tab via close_editor_tab DOES prompt 'has unsaved changes. Save before closing?'. The pending editor auto-save timer (3s, and a user-toggleable 'Manual Save' mode exists with auto-save fully off) is killed by deleteLater before it can fire, so the in-editor changes are simply gone.
  - **Failure:** A student working in an editor with auto-save toggled off (the '[Manual Save]' mode BaseEditor explicitly supports, marking the title with '*') opens another project from the Welcome tab or File > Open Project to peek at it. All editors are destroyed instantly with no prompt and the unsaved work is lost. Same on IDE close: with only editor-level (not project-level) changes pending, closeEvent's project_manager.is_dirty() is False, so the window closes without the 'Unsaved Changes' prompt that closing the individual tab would have shown.
  - **Suggested fix:** Before tearing down editors in on_project_loaded (and in closeEvent), iterate _iter_open_editors() and for any editor with is_modified either auto-invoke save() or show the same Save/Discard/Cancel prompt close_editor_tab uses; alternatively have on_editor_data_modified call project_manager.mark_dirty() so the existing closeEvent prompt at least covers app close.
  - **Verifier:** on_project_loaded (core/ide_window.py:3838-3846) deleteLater()s every tab and floated editor with no is_modified check, and grep confirms is_modified is consulted only in close_editor_tab (line 976). load_project/open_project never prompt, and closeEvent (4133) checks only project_manager.is_dirty(), which editor-local edits don't set (on_editor_data_modified at 3631 only renames the tab; editor saves go through on_editor_save_requested only when auto-save fires or the user saves). With the per-editor auto-save toggle OFF (base_editor.py:164 toggle_auto_save exists), unsaved editor work is silently destroyed on project switch and IDE close; even with the default 3s auto-save ON there is a small silent-loss window. Medium: silent loss of in-editor work in plausible use, mitigated by default auto-save.

- [x] **M13 — `dialogs/project_dialogs.py:427` — Export dialog routes on translated combo text compared to English literals — 'Desktop Executable' and 'Source Code (.zip)' exports silently no-op in French (and other localized) IDEs** *(fixed `2667b1a`, tests/test_export_dialog_routing.py)*
  - *Found by:* dialogs; *category:* correctness
  - The export-platform combo items are added via self.tr() (lines 322-328), so in a localized IDE currentText() returns the translated label. accept_export() then compares that translated text against hard-coded English literals. The compiled French translation (translations/pygm2_fr.qm, verified to contain the strings) renders 'Desktop Executable (.exe/.app)' as 'Exécutable bureau (.exe/.app)' and 'Source Code (.zip)' as 'Code source (.zip)'. Neither matches any branch, so execution falls into the else branch which just calls self.accept() without exporting anything. The caller core/ide_exporters.py:97-103 then shows 'Export completed to: <path>' in the status bar — a false success. ('Web (HTML5)' and 'Mobile (Kivy)' happen to translate identically in French, so only those two targets work.) The same pattern exists at core/ide_exporters.py:85 where export_kivy() pre-selects via dialog.export_platform.findText("Mobile (Kivy)") against translated items.
  - **Failure:** A French teacher (this project's primary audience; the IDE ships and loads pygm2_fr.qm) opens File > Export, selects 'Exécutable bureau (.exe/.app)' or 'Code source (.zip)', clicks Export. The dialog closes immediately, the status bar reports the export completed to the chosen folder, but no file is ever written. Two of the five export targets are completely dead in any non-English locale that translates the labels, with a false success message.
  - **Suggested fix:** Route on locale-independent data instead of display text: add each platform with an id via export_platform.addItem(self.tr(label), "html5"/"kivy"/"apk"/"zip"/"exe") and branch on export_platform.currentData(). Fix core/ide_exporters.py:85 to findData("kivy") likewise. Also remove the silent else: self.accept() fallback.
  - **Verifier:** Combo items are added via self.tr() (project_dialogs.py:322-328) but accept_export() compares currentText() to English literals with else->self.accept() (427-437). Loaded the compiled pygm2_fr.qm in PySide6: context ExportProjectDialog translates 'Desktop Executable (.exe/.app)'->'Exécutable bureau (.exe/.app)' and 'Source Code (.zip)'->'Code source (.zip)', so both branches never match in French and ide_exporters.py:100-102 shows 'Export completed to: ...' anyway. Re-graded medium: silent feature failure with false success, but no crash/data loss.

- [x] **M14 — `dialogs/thymio_config_dialog.py:129` — ThymioConfigDialog never updates config.preset_name when customizing — saved Thymio block restrictions are silently reverted on next IDE start (or immediately, when a project preset is set)** *(fixed `83a79e7`, tests/test_thymio_config_preset_name.py)*
  - *Found by:* dialogs; *category:* data-integrity
  - Unlike its twin BlocklyConfigDialog (which overrides _on_revert_to_custom to set self.config.preset_name = "custom", blockly_config_dialog.py:95-96), ThymioConfigDialog neither overrides the base hook (default: pass, _block_config_dialog_base.py:113-115) nor sets preset_name in _apply_thymio_full/_apply_thymio_basic/_apply_thymio_sensors/select_none. BlocklyConfig.enable_block/enable_category/disable_* also never touch preset_name (config/blockly_config.py:254-274). So after customizing Thymio blocks and clicking Save, save_config() writes the modified block set with the STALE preset_name (default global config is preset_name="full"). Two clobber paths follow: (1) load_config() has a migration that, whenever preset_name == "full", force re-adds every block missing from enabled_blocks and re-saves (config/blockly_config.py:1128-1140) — any block the user disabled in the Thymio dialog is re-enabled on next config load. (2) With a project open, core/ide_window.py:2762-2766 writes settings['blockly_preset'] = new_config.preset_name (the stale name, which IS in PRESETS) into the project and then refresh_event_panels_config() applies the pristine PRESETS[stale_name] config to all open editors — discarding the just-saved customization immediately.
  - **Failure:** A teacher opens Configure Thymio Events & Actions to restrict the class to 'Thymio Basic (Buttons + Motors)' (or unchecks individual blocks) and clicks Save. The confirmation says the configuration was saved. If a project is open, open editors immediately revert to the old preset's block set; in all cases, on the next IDE launch load_config()'s "full" migration re-enables every disabled Thymio block and rewrites the config file. The restriction — the dialog's entire purpose — silently disappears.
  - **Suggested fix:** Mirror the Blockly twin: add `def _on_revert_to_custom(self): self.config.preset_name = "custom"` to ThymioConfigDialog, and set self.config.preset_name = "custom" (or a dedicated thymio preset name) inside _apply_thymio_full/_apply_thymio_basic/_apply_thymio_sensors and select_none.
  - **Verifier:** ThymioConfigDialog never writes preset_name and doesn't override the no-op _on_revert_to_custom (_block_config_dialog_base.py:113-115), unlike BlocklyConfigDialog (blockly_config_dialog.py:95-96); enable/disable_category (blockly_config.py:254-274) don't touch it either, so save_config persists the customization under the stale default 'full'. load_config's migration (blockly_config.py:1130-1153) then re-adds every disabled block whenever preset_name=='full' and re-saves, and ide_window.configure_thymio writes the stale name to project settings then refresh_event_panels_config applies pristine PRESETS[name] to open editors — the saved restriction is reverted both immediately and on next launch.

- [x] **M15 — `editors/base_editor.py:176` — Ctrl+S and F5 are dead (Ambiguous shortcut overload) whenever an Object/Sprite/Script editor tab is current** *(fixed `a792f84`, tests/test_editor_shortcuts.py)*
  - *Found by:* other-editors; *category:* correctness
  - BaseEditor registers Ctrl+S twice itself — save_action.setShortcut(QKeySequence.Save) at line 122 AND QShortcut(QKeySequence.Save, self, self.save) at line 176 — both window-context in the same window. The IDE menubar adds a third (ide_window.py:168 'Save Project' Ctrl+S). Qt resolves multiple same-key window shortcuts as ambiguous: QAction logs 'QAction::event: Ambiguous shortcut overload' and does NOT trigger, and QShortcut emits only activatedAmbiguously (unconnected). The same collision kills F5: BaseEditor's QShortcut('F5', self, self.refresh) at line 182 vs the IDE's 'Test Game' F5 action (ide_window.py:275). Verified empirically with the real ScriptEditor offscreen: Ctrl+S fires NO handler (neither editor save nor menu Save Project) both docked-in-a-main-window and standalone/floated (the editor's own two registrations conflict even alone), and F5 fires neither Test Game nor refresh. Note the code comment at base_editor.py:136 shows the team already removed editor-level Ctrl+Z/Ctrl+Y for exactly this reason, but Ctrl+S/F5 were left behind. The editor status widget literally instructs 'Unsaved - press Ctrl+S' when auto-save is off, and that keypress does nothing.
  - **Failure:** A student has an object editor tab open (the normal state), turns off auto-save, edits, and presses Ctrl+S as the status bar tells them to — nothing saves, no feedback. They press F5 to test their game — nothing runs. Both core IDE shortcuts silently fail whenever any BaseEditor-derived tab (Object, Sprite, Script editor) is the current tab or is floated and focused.
  - **Suggested fix:** Remove the duplicate registrations from BaseEditor: drop the QShortcut(QKeySequence.Save) and QShortcut('F5') in setup_shortcuts (and the save_action.setShortcut), and follow the documented Ctrl+Z pattern — let the IDE menu own Ctrl+S/F5 and delegate to the active editor; or give the editor-level shortcuts Qt.WidgetWithChildrenShortcut context so they only fire on focus and never collide with the menubar.
  - **Verifier:** Reproduced empirically offscreen with the real BaseEditor inside a QMainWindow carrying IDE-style Ctrl+S/F5 actions: Qt logged 'QAction::event: Ambiguous shortcut overload' and zero handlers fired for both Ctrl+S and F5; standalone, Ctrl+S also fires nothing once save_action is enabled (i.e., exactly when the editor is modified). BaseEditor registers Ctrl+S twice itself (base_editor.py:122 and :176) plus the IDE's Ctrl+S (ide_window.py:168) and F5 (ide_window.py:275), all default WindowShortcut context in the same window since editors are main-window tabs.

- [x] **M16 — `editors/object_editor/blockly_widget.py:357` — get_workspace_xml returns before the async runJavaScript callback fires — it always returns "", so the Blockly workspace is never saved** *(fixed `ed95629`, tests/test_blockly_workspace_xml.py)*
  - *Found by:* object-editor; *category:* correctness
  - QWebEnginePage.runJavaScript with a result callback is asynchronous: the callback is invoked on a later event-loop turn. get_workspace_xml registers the callback and immediately returns result["xml"], which is still the initial "". Consequently in ObjectEditor.get_data() (object_editor_main.py:985-991) `blockly_xml = self.blockly_tab.get_workspace_xml(); if blockly_xml: object_data['blockly_workspace'] = blockly_xml` never stores anything, and the load-side branch in load_data (object_editor_main.py:899-905) that calls load_workspace_xml is dead. The user's block layout (positions, arrangement, any blocks without action equivalents such as workspace comments) is silently discarded on every save and re-derived from events JSON on every open.
  - **Failure:** A student arranges blocks carefully in the Blockly tab, saves, closes, reopens the object: the workspace is rebuilt by auto-layout from the events data — the arrangement is gone every single session, even though the code explicitly tries to persist 'blockly_workspace'.
  - **Suggested fix:** Make the save flow asynchronous: cache the XML on every blocks_changed notification (the JS side already pushes events JSON; have it push XML too, or call getXml in on_blocks_changed and store it in self._last_xml), and return the cached value from get_workspace_xml.
  - **Verifier:** blockly_widget.py:349-362 returns result['xml'] immediately after registering the QWebEnginePage.runJavaScript callback, which fires on a later event-loop turn (the same file's comment at lines 373-376 documents runJavaScript as asynchronous), so it always returns ''. Consequently object_editor_main.py:987-989 never stores 'blockly_workspace' and the load_workspace_xml branch at 899-902 is dead; the workspace is always rebuilt from events data, losing layout/arrangement every session.

- [x] **M17 — `editors/object_editor/object_events_panel.py:437` — Mouse events are dead end-to-end: every producer writes flat 'mouse_*' event keys but the runtime only dispatches the nested events['mouse'][...] shape that nothing produces** *(fixed by H11 `92d4216` — `_mouse_sub_event`/`_FLAT_MOUSE_KEY_ALIASES` in runtime/game_runner.py accept the flat producer keys; tests/test_mouse_events.py. Remaining hover/wheel/double-click selector entries with no runtime dispatch are a separate feature gap, not this bug.)*
  - *Found by:* events-actions-plugins; *category:* correctness
  - The runtime fires mouse events exclusively from a nested dict: GameRunner.handle_mouse_press/release/motion (runtime/game_runner.py:2628, 2695, 2718) look up events['mouse'] and then sub-keys 'left_button', 'right_button', 'middle_button', 'left_button_released', ..., 'mouse_move'. No producer in the codebase creates that shape: (1) the IDE events panel stores mouse events as flat top-level keys built as f"mouse_{button.lower()}_{event_type}" (e.g. 'mouse_left_press', 'mouse_left_release', 'mouse_move'); (2) the Blockly generator emits flat names ('mouse_' + BUTTON, e.g. 'mouse_left_press' — blockly_generators.js:58); (3) the GMK importer emits flat names ('mouse_left_button', 'mouse_left_press' — importers/gmk_mappings.py:92-115). Even ignoring nesting, the sub-event names don't match ('left_press' vs 'left_button'). Keyboard events, by contrast, ARE stored nested (current_events_data['keyboard'][key] at object_events_panel.py:392), which is why keyboard works and mouse doesn't. The only code exercising the nested mouse shape is tests/test_game_runner.py, which constructs it by hand. Additionally, the mouse selector offers hover/enter/leave/wheel/drag/double-click/global events (events/mouse_events_complete.py) that have no runtime dispatch at all.
  - **Failure:** A student adds a 'Mouse <Left Button>' event to an object via Add Event → Mouse, attaches actions (e.g. destroy_instance), and runs the game. Clicking the object does nothing — handle_mouse_press finds no events['mouse'] dict because the event was saved as the top-level key 'mouse_left_press'. The same is true for every mouse event added from the IDE, from Blockly, or imported from a .gmk file: the entire mouse event category silently never fires.
  - **Suggested fix:** Pick one canonical schema. Either make the producers write the nested shape with the runtime's sub-keys (store under events['mouse']['left_button' / 'left_button_released' / 'mouse_move']), or extend handle_mouse_press/release/motion to also match the flat 'mouse_<button>_<type>' keys that the IDE, Blockly, and the GMK importer actually emit (and map GMK's 'mouse_left_button' held-style names). Add an end-to-end regression test that authors a mouse event through the panel's storage format and asserts the runtime executes its actions.
  - **Verifier:** Runtime dispatch (game_runner.py:2628, 2695, 2718) only reads the nested events['mouse'][left_button|left_button_released|mouse_move] shape, while every producer writes flat top-level keys: events panel f"mouse_{button}_{event_type}" (object_events_panel.py:437), Blockly 'mouse_'+BUTTON with values like left_press (blockly_generators.js:58), and GMK importer flat names (gmk_mappings.py:93-115); sub-names don't match even ignoring nesting. The flat-handling InputMixin in runtime/input_handler.py is dead code — GameRunner (game_runner.py:1735) inherits nothing — and only tests/test_game_runner.py hand-builds the nested shape. Re-graded medium per rubric (silent wrong behavior, no crash/data loss), though it is a total failure of the mouse-event category.

- [x] **M18 — `editors/object_editor/python_code_parser.py:705` — Plain variable assignments in custom code are misclassified as thymio_set_variable, whose runtime handler int()-coerces values — floats are truncated and strings become 0** *(fixed `e36dc82`, tests/test_thymio_set_variable.py)*
  - *Found by:* object-editor; *category:* correctness
  - _try_parse_thymio_assignment treats ANY `name = constant` statement as a Thymio robot action: `if isinstance(target, ast.Name) and isinstance(value, (ast.Constant, ast.Num)): ... return {"action": "thymio_set_variable", ...}` — with no check that the project uses Thymio at all (it runs before the generic fallback in _try_parse_statement). Verified: parsing 'points = 0' in on_create yields thymio_set_variable. The runtime handler (runtime/thymio_action_handlers.py:314-319, registered unconditionally via game_runner.py:1772) does `setattr(instance, variable, int(value))` after _parse_value, which returns 0 for non-numeric strings. So `speed_mult = 1.5` stores 1 and `name = "Bob"` stores 0 — silently. Had the statement stayed in the execute_code fallback, exec_locals copy-back would have set the exact value. The events panel also displays a robot action in a plain desktop game.
  - **Failure:** A student in a non-robot platformer writes `bounce_factor = 1.5` in the code editor. Auto-apply stores it as a Thymio action; at runtime the instance gets bounce_factor == 1 and the jump physics that reads it behaves wrong, with no error anywhere. String assignments (`status = "alive"`) silently become 0.
  - **Suggested fix:** Only emit thymio_* variable actions when the surrounding context is a Thymio event or the code references thymio elsewhere; otherwise fall through to set_variable or the execute_code fallback. Independently, fix the runtime handler to setattr the value without int() coercion for float/string values.
  - **Verifier:** Reproduced: 'points = 0', 'speed_mult = 1.5', and 'name = "Bob"' all parse to thymio_set_variable (python_code_parser.py:704-715, tried before the execute_code fallback at 484-488); the handler is registered unconditionally (game_runner.py:1772) and does setattr(instance, variable, int(value)) with _parse_value returning 0 for non-numeric strings (thymio_action_handlers.py:314-319, 404-429), so 1.5 silently becomes 1 and "Bob" becomes 0 in a non-robot project. Medium: silent wrong values in plausible classroom code-editor use, but only floats/strings are corrupted (int assignments happen to behave identically to the execute_code path).

- [x] **M19 — `editors/object_editor/python_code_parser.py:1043` — Conditional actions without nested action lists generate a bodiless 'if ...:' / 'for ...:' — the generated class is invalid Python, breaking View Generated Code and permanently blocking Edit Custom Code** *(fixed `d673a39`, tests/test_bodiless_conditional_codegen.py)*
  - *Found by:* object-editor; *category:* correctness
  - _generate_action_code only routes conditionals through _generate_conditional (which appends 'pass' for empty bodies) when sub_actions/then_actions/else_actions/actions_list is non-empty: `if is_conditional and (sub_actions or then_actions or else_actions or actions_list):`. Otherwise it falls through to the bare template return, emitting just the header line ('if self.x > 5:' or 'for _i in range(3):') with no indented body. This is exactly the shape produced by GMK imports, where a test_expression/test_variable conditional gates the NEXT sibling action via start_block/end_block markers (which render as '' here), and also by an if_condition created in the events panel before any then-actions are added. Verified empirically: events {step: [test_expression, start_block, set_hspeed, end_block]} generate 'if self.x > 5:\n        self.hspeed = 4' at the SAME indent — ast.parse raises 'expected an indented block after if statement'.
  - **Failure:** A user imports a .gmk game (or adds an If-condition action and a following action in the events panel), then opens the Code Editor tab. View mode shows syntactically broken Python. Switching to 'Edit Custom Code' seeds the editor with that broken code; every keystroke triggers auto-apply which fails with '● Parse error' forever — the code-editing feature is unusable for that object until the user manually restructures code they didn't write.
  - **Suggested fix:** Route all is_conditional actions through _generate_conditional (it already emits 'pass' when the body is empty), i.e. drop the '(sub_actions or ...)' requirement; additionally, for GM block-marker sequences, fold the actions between start_block/end_block into the preceding conditional's body during generation.
  - **Verifier:** Reproduced: the gate at python_code_parser.py:1043 only routes conditionals through _generate_conditional (which adds 'pass') when nested actions exist; a flat [test_expression, start_block, set_hspeed, end_block] list — exactly the shape gmk_converter.py emits (lines 543-563) and the runtime consumes flatly (action_executor.py:224ff) — generates 'if self.x > 5:' with the body at the same indent, and ast.parse fails with 'expected an indented block'. _auto_apply_code returns on parse errors so Edit Custom Code is permanently blocked for that object. Medium: code-tab feature broken for GMK-imported objects, but no data loss (the failed apply never writes) and the game/events panel still work.

- [x] **M20 — `editors/object_editor/python_code_parser.py:1101` — if_condition 'key_pressed' generates a call to keyboard.check(), an API that exists nowhere in the runtime — after one code-editor round trip the condition becomes an execute_code that NameErrors forever** *(fixed `5c9418b`, tests/test_execute_code_keyboard.py)*
  - *Found by:* object-editor; *category:* correctness
  - For condition_type 'key_pressed', _generate_action_code and _generate_conditional emit `if keyboard.check("<key>"):`. There is no `keyboard` object anywhere in the runtime (grep over runtime/ and export/ finds no keyboard.check and no such binding in the execute_code exec namespace at runtime/action_executor.py:2868). The parser cannot re-parse generic if statements into if_condition actions (only Thymio conditionals are recognized), so after the user makes any edit in Edit Custom Code mode, the auto-apply converts the structured if_condition into execute_code containing keyboard.check(...) — verified empirically: round trip yields {"action":"execute_code","parameters":{"code":"if keyboard.check('space'):\n    self.hspeed = 4"}}. At runtime that exec raises NameError and the whole gated behavior is dead. More broadly, every structured conditional (if_condition, test_variable, test_expression...) silently degrades into an opaque execute_code blob after one code round trip — it disappears from the events panel and Blockly as an editable condition.
  - **Failure:** A student builds 'If key space pressed -> set hspeed' in the events panel, opens Edit Custom Code to tweak something unrelated, and types one character. Auto-apply replaces the structured condition with execute_code('if keyboard.check(...)'). The game stops responding to the space key entirely (silent NameError per frame in the log), and the events panel now shows an inscrutable code block instead of the friendly condition.
  - **Suggested fix:** Generate code the runtime actually supports (e.g. emit a comment-marked pattern the parser recognizes, or generate `if game.key_pressed("space"):` and implement that GameRunner helper). Better: teach PythonToActionsParser to recognize the exact code shapes its own generator emits for if_condition (game.instance_count / keyboard.check / random.randint / self.<var> compare) and reconstruct the structured action, so the round trip is lossless.
  - **Verifier:** Reproduced: condition_type 'key_pressed' emits keyboard.check(...) (python_code_parser.py:1101-1103, 1172-1174); grep confirms no keyboard binding anywhere in runtime/, and the execute_code exec namespace (action_executor.py:~2868) contains only sel/game/instance/math/random — not keyboard (nor even self). The round trip verifiably degrades the structured if_condition into an execute_code blob whose exec raises NameError each frame (caught and logged), silently killing the keyboard-gated behavior and removing it from the events panel as an editable condition. Medium: silent behavior breakage in a plausible student workflow, no data loss beyond structure degradation.

- [x] **M21 — `editors/playground_editor/playground_canvas.py:473` — Every robot placed with the Robot tool gets the same port 33333 (_next_port never increments)** — FIXED (add_robot now advances _next_port in the data path too; tests/test_playground_editor_undo.py)
  - *Found by:* other-editors; *category:* correctness
  - add_robot() only increments self._next_port in the `else` branch (robot_data is None). The only live caller — mousePressEvent's ROBOT mode — builds a PlaygroundRobot with port=self._next_port and then calls self.add_robot(robot.to_dict()), which takes the robot_data branch where the increment never runs. So within an editing session, every robot placed via the tool gets port 33333 (verified empirically: placing 3 robots yields ports [33333, 33333, 33333]). The auto-increment only self-corrects after a save+reload via load_data's max(port)+1, which most users won't hit mid-session.
  - **Failure:** A teacher places two Thymio robots in a playground and exports the .playground (or runs the simulation). Both robots carry port 33333 — the second Aseba node can't bind/connect because the TCP port collides with the first, so the multi-robot playground silently misbehaves or fails. The Port spinbox in the properties panel shows the duplicate, but nothing warned the user.
  - **Suggested fix:** Increment in the data path too, e.g. at the end of add_robot: `self._next_port = max(self._next_port, robot.port + 1)`; or in mousePressEvent ROBOT mode, bump self._next_port after the add_robot call.
  - **Verifier:** The only live placement path (mousePressEvent ROBOT branch, playground_canvas.py:471-475) calls add_robot(robot.to_dict()), which takes the robot_data branch where _next_port += 1 never executes — every session-placed robot gets 33333 until a save+reload re-derives max(port)+1 in load_data. The exporter (export/Aseba/playground_exporter.py:92) writes the duplicate ports into the .playground XML, where Aseba nodes bind TCP ports.

- [x] **M22 — `editors/playground_editor/playground_undo_commands.py:43` — Redo after Undo of Add Wall/Add Robot never restores the element (already_added is never reset)** — FIXED (AddElementCommand.redo consumes the already_added flag on first call; tests/test_playground_editor_undo.py)
  - *Found by:* other-editors; *category:* correctness
  - PlaygroundCanvas.add_wall/add_robot append the element themselves, then push an AddElementCommand with already_added=True so the initial redo-on-push doesn't double-add (playground_canvas.py:156-160 and 174-178). But already_added is never reset to False, so every subsequent redo() is also a no-op: after the user undoes the add, redo can never bring the element back. The undo stack index still advances, leaving an inert command in the history and the element permanently lost (its only reference lives inside the dead command). Verified empirically with offscreen Qt: add_wall() -> undo() -> redo() leaves the wall absent from canvas.walls while undo_stack.canUndo() is True.
  - **Failure:** A student places a wall in the playground editor, presses Ctrl+Z (one too many), then presses Redo to get it back. Nothing appears. The wall is gone for good; the Redo button consumed the step silently, and further Undo/Redo presses around that entry do nothing for it.
  - **Suggested fix:** Consume the flag on first redo: in AddElementCommand.redo(), `if self.already_added: self.already_added = False; return` then unconditionally append on later calls (and emit element_added). Alternatively, append the element only inside redo() and push the command without pre-adding, like RemoveElementCommand does symmetrically.
  - **Verifier:** add_wall/add_robot append the element, set cmd.already_added = True, then push; AddElementCommand.redo() guards on 'if not self.already_added' and nothing ever resets the flag, so after undo() removes the element every redo() is a permanent no-op while the stack index still advances. The element is only recoverable by re-creating it manually.

- [x] **M23 — `editors/room_editor/__init__.py:246` — Clear All / Shift All bypass the undo system and leave a stale undo stack; undo after Clear All resurrects arbitrary instances** — FIXED (clear_instances/shift_all_instances route through BatchRemove/BatchMove commands; tests/test_room_editor_undo.py)
  - *Found by:* room-sprite-editors; *category:* correctness
  - clear_all_instances calls room_canvas.clear_instances() (room_canvas.py:363-367), which empties self.instances directly; shift_all_instances (room_canvas.py:419-426) likewise mutates every instance.x/y directly. Neither pushes an undo command nor clears the undo stack. So (1) Clear All is the only destructive room operation that Ctrl+Z cannot revert (every single/batch delete IS undoable), and (2) the stack retains commands holding references to the now-removed instance objects: after Clear All, pressing Ctrl+Z walks back through stale history — a prior RemoveInstanceCommand.undo() re-appends its old instance into the supposedly empty room, while prior Add/Move undos silently no-op or mutate ghost objects. Shift All has the analogous problem: undoing a pre-shift MoveInstanceCommand restores absolute pre-shift coordinates, tearing that instance out of alignment with the shifted layout.
  - **Failure:** A teacher deletes one instance, then clicks 'Clear All' (confirms) to restart the layout, then presses Ctrl+Z expecting the clear to revert. Instead the single previously-deleted instance pops back alone into the empty room, and no number of further Ctrl+Z presses restores the cleared layout — the room content is unrecoverable except by closing without saving.
  - **Suggested fix:** Implement Clear All as a BatchRemoveInstancesCommand over all instances (it already exists and handles selection), and Shift All as a BatchMoveInstancesCommand; alternatively call undo_stack.clear() after any non-undoable bulk mutation so stale history can't replay.
  - **Verifier:** clear_all_instances (room_editor/__init__.py:245-249) calls clear_instances() which does self.instances.clear() directly — no undo command and no undo_stack.clear(); shift_all_instances (room_canvas.py:419-426) mutates x/y the same way. RemoveInstanceCommand.undo() unconditionally re-appends its held instance, so Ctrl+Z after Clear All resurrects a previously-deleted instance into the cleared room and the cleared layout itself is unrecoverable via undo. Not in the 2026-06-03 latent-bug registry.

- [x] **M24 — `editors/room_undo_commands.py:40` — already_added flag permanently disables Redo for paste/duplicate/paint commands (instances and tiles)** — FIXED (all four Add*/BatchAdd* redo() methods consume already_added on first call; tests/test_room_editor_undo.py)
  - *Found by:* room-sprite-editors; *category:* correctness
  - AddInstanceCommand, BatchAddInstancesCommand (line 166), AddTileCommand (line 287), and BatchAddTilesCommand (lines 316-319) take an already_added=True flag because the canvas appends the objects to the list before pushing the command (QUndoStack.push() immediately calls redo(), so the first redo must be a no-op). But the flag is never cleared after that first call, so EVERY subsequent redo() is also a no-op. Sequence: paste/duplicate/paint -> push (redo no-op, correct) -> Ctrl+Z (undo removes the instances, correct) -> Ctrl+Y (redo runs but the guard skips re-adding). The undo stack index advances as if the redo succeeded, so the stack and the canvas state desynchronize: the redone instances/tiles never reappear, and pressing Undo again is a silent no-op too. Affected operations: paste (room_canvas.py:474/476), duplicate (539/541), paint-mode placement (1136/1139), tile painting (1072/1075).
  - **Failure:** A student paints 30 wall instances in the room editor (one BatchAddInstancesCommand with already_added=True), presses Ctrl+Z to undo (walls disappear), then presses Ctrl+Y to bring them back. Nothing reappears; the Redo button goes grey as if it worked. The same happens for paste, duplicate, single placed instances, and painted tiles. The student must redo the work by hand, or worse saves the room believing redo restored it.
  - **Suggested fix:** Consume the flag on first use: in each affected redo(), `if self.already_added: self.already_added = False; return` (plus the QTimer update), so later redos actually re-add the objects.
  - **Verifier:** room_undo_commands.py sets self.already_added only in __init__ and never clears it; redo() is gated on `if not self.already_added` (lines 40, 166, 287, 316), so after the initial push-consumed redo every subsequent Ctrl+Y is a permanent no-op. All cited room_canvas.py sites (474/476, 539/541, 1072/1075, 1136/1139) pass already_added=True, and playground_canvas.py has the identical pattern. Redo silently failing after undo of paste/duplicate/paint is wrong behavior in normal classroom use.

- [x] **M25 — `editors/sprite_editor/sprite_editor_main.py:598` — Eyedropper draws on the canvas if the user drags: tool is switched to Pencil mid-gesture** — FIXED (SpriteCanvas.set_tool clears _painting, ending any active gesture on a mid-stroke tool swap; tests/test_sprite_canvas_eyedropper.py)
  - *Found by:* room-sprite-editors; *category:* correctness
  - SpriteCanvas.mousePressEvent emits color_picked synchronously during on_press while self._painting is True. The slot _on_color_picked immediately calls self._select_tool("pencil"), which calls canvas.set_tool(pencil) — replacing canvas._current_tool in the middle of the active mouse gesture. canvas.mouseMoveEvent then sees `self._painting and self._current_tool` and dispatches on_move to the freshly-reset PencilTool: the first move primes _last_x/_last_y, every subsequent move draws Bresenham lines in the just-picked color. So a click-and-slight-drag with the color picker (extremely common with a mouse) paints over the very pixels the user was sampling. Additionally, on release canvas_modified fires and _on_canvas_modified pushes a SpriteEditCommand and emits data_modified, so even a clean pick marks the sprite dirty and pollutes the undo stack.
  - **Failure:** A student selects the eyedropper (I), clicks a pixel to sample its color, and moves the mouse a few pixels before releasing the button. The editor silently scribbles pencil strokes in the picked color along the drag path, and the sprite is marked modified. The student often doesn't notice until later frames/saves.
  - **Suggested fix:** Defer the tool switch until the gesture ends: in _on_color_picked, don't call _select_tool while canvas._painting is True (e.g. set a pending-tool flag consumed in mouseReleaseEvent), or have SpriteCanvas suppress on_move dispatch after the active tool is swapped mid-stroke (clear _painting in set_tool).
  - **Verifier:** color_picked is emitted synchronously inside mousePressEvent while _painting is already True; _on_color_picked (sprite_editor_main.py:594-598) calls _select_tool('pencil'), swapping canvas._current_tool mid-gesture, and mouseMoveEvent only checks `self._painting and self._current_tool` — the first move primes PencilTool._last_x and subsequent moves draw Bresenham lines in the picked color along the drag path. Silent canvas scribbling from a common click-and-slight-drag gesture is medium (though the pre-press snapshot means one undo can revert it).

- [x] **M26 — `editors/sprite_editor/sprite_frames.py:250` — Frame add/duplicate/delete bypass the undo stack: deletion is irreversible and stale frame indexes make undo stamp pixels onto the wrong frame** — FIXED (timeline ops call an _undo_hook the editor wires to FrameEditCommand; tests/test_sprite_frame_undo.py)
  - *Found by:* room-sprite-editors; *category:* data-integrity
  - FrameTimeline.add_frame (line 228), duplicate_frame (239) and delete_frame (250) mutate self._frames directly and only emit frames_changed; no FrameEditCommand is pushed (FrameEditCommand exists but is used only by _resize_canvas in sprite_editor_main.py:763). Two consequences: (1) deleting a frame is permanent — Ctrl+Z cannot restore it; (2) SpriteEditCommand captures `self._frame_index = editor.frame_timeline.get_current_index()` at stroke time (sprite_editor_main.py:47) and undo/redo route through _set_frame_image(frame_index, ...), so any later insert/delete shifts indexes and undo targets the wrong frame. Example: frames [A,B]; draw on B (cmd index 1); select A and press '+' (blank inserted at index 1 -> [A, blank, B]); Ctrl+Z now writes B's pre-stroke image over the new blank frame at index 1, while B keeps the stroke — visible artwork corruption via undo.
  - **Failure:** A student building a 4-frame walk cycle mis-clicks the '-' button: the frame's artwork is destroyed with no undo and no confirmation (only recoverable by closing the editor without saving, losing all other unsaved work). Separately, after inserting a frame, pressing Ctrl+Z copies another frame's old pixels onto the wrong frame.
  - **Suggested fix:** Route add/duplicate/delete through FrameEditCommand on the editor's undo_stack (the command class already exists and snapshots whole frame lists, which also sidesteps the index-shift problem since SpriteEditCommand entries older than a structural change would at least restore consistent lists). At minimum, push a FrameEditCommand for delete_frame.
  - **Verifier:** sprite_frames.py add_frame/duplicate_frame/delete_frame (228/239/250) mutate _frames directly and only emit frames_changed; no FrameEditCommand is pushed (it is used only by _resize_canvas), so frame deletion is unrecoverable via undo. SpriteEditCommand captures _frame_index at stroke time and _set_frame_image writes by raw index, so the [A,B] -> draw on B -> insert at 1 -> undo walkthrough genuinely stamps B's pre-stroke pixels onto the new blank frame. Bounded data loss (recoverable only by discarding all unsaved work) — medium.

- [x] **M27 — `events/action_editor.py:293` — Sprite combo is non-editable and lacks the '<self>' sentinel: editing any set_sprite action silently re-points it to the project's first sprite** — FIXED (sprite combo prepends sentinel/empty/saved-but-absent values so they round-trip; tests/test_action_editor_sprite_sentinel.py)
  - *Found by:* events-actions-plugins; *category:* data-integrity
  - ACTION_TYPES['set_sprite'] (events/action_types.py:1058) defaults sprite to the sentinel "<self>" (keep current sprite, change only frame/speed — the form GMK imports rely on to restart an animation, per the registry comment and execute_set_sprite_action's docstring). But ActionConfigDialog's sprite branch builds a QComboBox with setEditable(False) containing only the project's sprite names; setCurrentText("<self>") on a non-editable combo with no matching item is a no-op, so the selection stays on item 0 (the first sprite). On OK, get_parameter_values saves currentText — the first sprite's name. Consequences: (a) a new set_sprite action can never be configured with '<self>'; (b) merely opening an existing set_sprite action whose sprite is '<self>' (e.g. GMK-imported) and clicking OK silently rewrites it to a concrete sprite, changing behavior. draw_lives has the same defect for its optional sprite param (default "", action_types.py:1537): editing forces the first sprite, and an empty selection is unreachable.
  - **Failure:** A teacher imports a GMK game where an object uses set_sprite(sprite='<self>', subimage=0) to restart its current animation. They open that action in the editor to tweak the speed and click OK. The action now reads sprite=<first sprite alphabetically in the project>, so at runtime the object's sprite is replaced by an unrelated image. No warning is shown; the corruption is only visible when the game looks wrong.
  - **Suggested fix:** In the sprite branch, prepend the sentinel entries before the project sprites — e.g. widget.addItem("<self>") for set_sprite (drive it off param.default_value or param.choices like the object branch does) and an empty/'(none)' entry for optional sprite params — so saved sentinel values round-trip and remain selectable.
  - **Verifier:** ActionConfigDialog's sprite branch (action_editor.py:293-307) builds a non-editable combo from project sprites only; setCurrentText('<self>') with no matching item is a no-op so it stays on item 0, and get_parameter_values saves currentText — opening any set_sprite (default '<self>', action_types.py:1058) and clicking OK rewrites it to the first sprite, which the runtime then actually applies (action_executor.py:4547). Tellingly, the Thymio-only GM80ActionDialog (gm80_action_dialog.py:237-240) prepends '<self>' for exactly this case, but the standard events panel routes through the broken ActionConfigDialog. Same defect makes draw_lives' empty-sprite default (action_types.py:1537) unreachable.

- [x] **M28 — `events/action_editor.py:627` — Room dropdowns offer '→ Next Room' / '← Previous Room' / '↺ Restart Current Room' which save sentinels ('__next__', '__prev__', '__current__') that no runtime code consumes** — FIXED (goto_room maps them to next/previous/restart flags; check_room resolves them against the room list; tests/test_room_navigation_sentinels.py)
  - *Found by:* events-actions-plugins; *category:* correctness
  - Every room-typed parameter dialog (matched by `"room" in param_name.lower()` — i.e. goto_room's 'room' and check_room's 'room') prepends three navigation options at the top of the dropdown (get_available_rooms, lines 511-516) and maps them to '__next__'/'__prev__'/'__current__' on save (lines 625-631). A repo-wide grep shows these sentinels appear ONLY in action_editor.py — execute_goto_room_action (runtime/action_executor.py:5405-5420) looks the value up in get_room_list(), logs "Room '__next__' not found" at debug level, and returns without doing anything; execute_check_room_action compares it against the actual room name and is always false. The exporters don't handle them either.
  - **Failure:** A teacher adds a 'Go to Room' action and picks '→ Next Room' — the most prominent entry, listed first in the dropdown. At runtime the action is a silent no-op (debug-level log only): the level-exit object does nothing when touched. Same for check_room with any navigation option: the condition is always false.
  - **Suggested fix:** In execute_goto_room_action, translate the sentinels before lookup: '__next__' → next_room behavior, '__prev__' → previous_room, '__current__' → restart_room (the handlers/flags already exist). For check_room, either resolve the sentinels against the room list or stop offering navigation options for that parameter (only goto-like actions should get them).
  - **Verifier:** The editor saves '__next__'/'__prev__'/'__current__' (action_editor.py:626-631) but no Python runtime code consumes them: execute_goto_room_action rejects them as unknown room names with only a debug log (action_executor.py:5417-5420) and execute_check_room_action compares them literally so it is always false. Only the HTML5 export engine handles '__next__' — and it expects '__previous__'/'__restart__' for the other two, which the editor never writes — so in the primary pygame runtime all three prominently-listed options are silent no-ops.

- [x] **M29 — `events/action_types.py:1288` — test_health offers operation values 'less_or_equal'/'greater_or_equal' that no runtime branch matches — the condition is silently always false** — FIXED (choices changed to less_equal/greater_equal; runtime heals the legacy spellings; tests/test_test_health_operations.py)
  - *Found by:* events-actions-plugins; *category:* correctness
  - ACTION_TYPES['test_health'] declares choices=["equal", "less", "greater", "less_or_equal", "greater_or_equal", "not_equal"], but execute_test_health_action (runtime/action_executor.py:2452-2463) only matches 'equal', 'less', 'greater', 'less_equal', 'greater_equal', 'not_equal'. A repo-wide grep shows 'less_or_equal'/'greater_or_equal' appear nowhere else: every sibling conditional (test_score line 1921, test_lives line 1934, test_instance_count line 1972, test_variable line 365) uses 'less_equal'/'greater_equal', and the runtime _compare helper likewise. When the user selects either *_or_equal choice, no branch matches, `result` stays False, and the conditional always takes the false path.
  - **Failure:** A student builds 'Test Health: less_or_equal 0' followed by a death sequence (the natural reading of 'health <= 0'). At runtime execute_test_health_action falls through every elif and returns False forever, so the death sequence never triggers even at health 0 — with no error or log hinting why. The two equivalent-looking choices in the same dropdown ('less' / 'less_or_equal') behave completely differently.
  - **Suggested fix:** Change the choices list to ["equal", "less", "greater", "less_equal", "greater_equal", "not_equal"] to match every other conditional, and have execute_test_health_action additionally accept the legacy 'less_or_equal'/'greater_or_equal' spellings so already-saved projects heal.
  - **Verifier:** action_types.py:1288 offers 'less_or_equal'/'greater_or_equal' but execute_test_health_action only matches 'less_equal'/'greater_equal' (action_executor.py:2458-2461), and a repo-wide grep shows the *_or_equal strings exist nowhere else (no normalization layer; the choice combo saves raw currentText per action_editor.py:240-247). Selecting either choice leaves result=False forever with no warning — silent wrong behavior in a natural classroom pattern (health <= 0 death check).

- [x] **M30 — `events/conditional_editor.py:367` — if_condition key_pressed: the four arrow-key choices store 'Left Arrow' etc., which can never match the runtime's 'left'/'right'/'up'/'down' key names (and the Pressed/Held/Released state is ignored)** — FIXED (canonical values are now lowercase runtime key names; runtime heals legacy 'X Arrow' spellings; misleading Held/Released states removed; tests/test_if_condition_input.py)
  - *Found by:* events-actions-plugins; *category:* correctness
  - The key_pressed condition combo stores canonical values 'Left Arrow', 'Right Arrow', 'Up Arrow', 'Down Arrow' (and 'Enter'). The runtime evaluates `key.lower() in getattr(instance, 'keys_pressed', set())` (runtime/action_executor.py:4421), and keys_pressed is populated by pygame_key_name (runtime/_keymap.py:28-33) with the names 'left', 'right', 'up', 'down' (arrows) and 'enter' (Return). 'left arrow' != 'left', so an arrow-key condition is permanently false ('Enter'.lower()=='enter' happens to match; 'Space', letters, 'Shift'/'Control'/'Alt' also match). Additionally, the 'state' sub-field offers Pressed/Held/Released (lines 376-378) but _evaluate_if_condition reads only `key` and never `state` — 'Released' behaves identically to a held-key check, i.e. true while the key IS down, the exact opposite of its label.
  - **Failure:** A student creates an If Condition with key_pressed = 'Right Arrow' to gate movement. At runtime the condition compares 'right arrow' against the keys_pressed set containing 'right' and always evaluates false; the then-branch never runs no matter what is pressed. Likewise, anyone choosing state 'Released' gets held-key semantics.
  - **Suggested fix:** Store the runtime key names as the canonical userData ('left', 'right', 'up', 'down', 'enter', ...) — _add_canonical_items already separates display text from stored value, so only the value strings need fixing — plus a load-time alias for already-saved 'Left Arrow'-style values. Either implement the state field (per-frame pressed/released sets) or remove the Pressed/Held/Released combo until it does something.
  - **Verifier:** Editor stores canonical 'Left Arrow' etc. (conditional_editor.py:367, saved verbatim at :732); runtime compares key.lower() against instance.keys_pressed populated with 'left'/'right'/'up'/'down' by _keymap.py, so arrow conditions are permanently false, and _evaluate_if_condition (action_executor.py:4413-4421) never reads the saved 'state' parameter. Distinct from the closed audit item #8 (which only fixed the pressed_keys attribute name); no normalization exists anywhere in runtime.

- [x] **M31 — `events/conditional_editor.py:394` — if_condition mouse_check: the editor offers five mouse conditions but the runtime unconditionally returns False** — FIXED (runtime now evaluates the three buttons via pygame.mouse.get_pressed and 'Over object' via the mouse pos vs instance bbox; tests/test_if_condition_input.py)
  - *Found by:* events-actions-plugins; *category:* correctness
  - create_mouse_check_widget presents 'Left button pressed', 'Right button pressed', 'Middle button pressed', 'Over object', and 'In region' as selectable conditions, and get_parameter_values saves the chosen value under 'check' (line 737). The runtime branch is a hard stub: _evaluate_if_condition's mouse_check arm (runtime/action_executor.py:4454-4457) is `# Simplified mouse check / return False` — it never reads 'check' or any mouse state. Unlike goto_room's transition parameter (which is honestly labelled "currently accepted but not rendered" with a single 'none' choice), nothing in this UI signals the condition is non-functional, and it is not listed in TODO.md.
  - **Failure:** A student sets up 'If Condition → mouse_check → Left button pressed' with then-actions. At runtime the condition always evaluates false: the then-branch never runs and the else-branch always runs, regardless of mouse input. The student gets no error and no hint that the dropdown they used is a stub.
  - **Suggested fix:** Implement at least the three button checks via pygame.mouse.get_pressed() (and 'Over object' via mouse position vs instance bbox, which the runtime already tracks as mouse_x/mouse_y) — or remove mouse_check from the condition_type combo until it works, matching the project's rc.11 'no dead UI' policy.
  - **Verifier:** action_executor.py:4454-4457 is literally 'elif condition_type == "mouse_check": # Simplified mouse check / return False'; the 'check' parameter the editor saves (conditional_editor.py:737) is never read. Not tracked in TODO.md or docs/LATENT_BUG_AUDIT_2026-06-03.md, and the UI gives no stub indication.

- [x] **M32 — `export/Aseba/aseba_exporter.py:80` — Exported .aesl files are raw Aseba source text, but the .aesl format Aseba Studio opens is an XML document — File→Open fails for every export** — FIXED (_wrap_aesl_xml wraps the source in the aesl-source XML envelope with a CDATA node; tests/test_aseba_export.py)
  - *Found by:* export-desktop-scripts; *category:* correctness
  - The exporter writes the generated Aseba source code directly into <object>.aesl ('with open(output_file, ...) as f: f.write(aesl_code)'). Real .aesl files saved/opened by Aseba Studio are XML documents: '<!DOCTYPE aesl-source>\n<network>...<node nodeId="1" name="thymio-II">…code…</node></network>'. Aseba Studio's File→Open parses the file as XML and rejects plain text. The generated README.md (lines 519-526) explicitly instructs the user to 'In Aseba Studio, click File → Open, select the .aesl file' — a workflow that cannot succeed with the produced artifact. The export reports success ('Export complete!') unconditionally.
  - **Failure:** A teacher uses File → Export Aseba (Thymio) code… in the IDE, gets 'Export complete', follows the generated README on a machine with Aseba Studio: File → Open on the .aesl file fails with an invalid-file/parse error. The only workaround (manually pasting the text into the Studio editor) is undocumented; the advertised round-trip is broken for 100% of exports.
  - **Suggested fix:** Wrap the generated code in the aesl-source XML envelope: emit '<!DOCTYPE aesl-source>' + '<network>' + a '<node nodeId="1" name="thymio-II">' element whose text is the generated code (XML-escaped), '</network>'. Alternatively rename the output to .txt/.aseba and fix the README instructions — but the XML wrapper is what makes File→Open work.
  - **Verifier:** aseba_exporter.py:79-81 writes raw Aseba source text into .aesl, but Aseba Studio's .aesl format is an XML document (<!DOCTYPE aesl-source><network><node>...), so File->Open per the generated README (and the IDE menu wiring in core/ide_window.py:2409) fails; the sibling playground_exporter.py correctly emits XML, confirming the project knows Aseba file formats are XML. Downgraded to medium: completely broken feature but no crash/data-loss, and the code itself is pasteable as a manual workaround (modulo finding 57).

- [x] **M33 — `export/Aseba/aseba_exporter.py:264` — Every generated onevent handler is terminated with a stray 'end', which is invalid AESL and fails compilation** — FIXED (removed the unconditional 'end' after onevent blocks; tests/test_aseba_export.py)
  - *Found by:* export-desktop-scripts; *category:* correctness
  - _generate_event_handler emits the onevent line (e.g. 'onevent button.forward', per THYMIO_EVENT_TO_ASEBA in events/thymio_events.py:184-210) followed by the translated actions, then unconditionally appends 'end'. In the AESL language, 'end' only closes if/when/while/for blocks; 'onevent' handlers have no terminator — they end implicitly at the next onevent/sub or EOF. The stray 'end' is a syntax error ('end' without a matching block), so the Aseba compiler rejects the generated program for every object that has at least one event handler (i.e. virtually all exports).
  - **Failure:** A student exports a Thymio object with a 'button forward → move forward' event. The generated file contains 'onevent button.forward\n\tmotor.left.target = 200\n\tmotor.right.target = 200\nend'. Loading/compiling it in Aseba Studio fails with a syntax error on the 'end' line; the code never reaches the robot.
  - **Suggested fix:** Remove the unconditional lines.append("end") in _generate_event_handler (line 264). 'end' must only be emitted by _translate_end_block for explicitly opened if-blocks.
  - **Verifier:** _generate_event_handler (aseba_exporter.py:252-266) unconditionally appends 'end' after every onevent block; in the AESL grammar 'end' only closes if/when/while/for blocks and onevent handlers terminate implicitly, so every export with at least one event handler fails Aseba compilation with a syntax error. No tests cover the Aseba exporter.

- [x] **M34 — `export/Kivy/code_generator.py:131` — Kivy/Android export generates calls to self.check_collision_at(), which is never defined anywhere in the exported game** — FIXED (generated GameObject now defines check_collision_at; generator uses offset-from-self semantics + honors not_flag; tests/test_kivy_collision_export.py)
  - *Found by:* cross-data-integrity; *category:* correctness
  - ActionCodeGenerator emits `if self.check_collision_at({x}, {y}, '{obj_name}'):` for the 'if_collision', 'if_collision_at', and 'check_collision' actions (lines 131, 141, 151) and `if not self.check_collision_at(...)` for 'check_empty' (lines 163-165). A repo-wide grep shows no `def check_collision_at` in export/Kivy/kivy_exporter.py's generated GameObject code (which only defines `check_collision(self, other)` at kivy_exporter.py:1844) nor in any template (game_object.py.template, scene.py.template). So every exported event containing one of these actions raises AttributeError at runtime in the exported app. Two further contract mismatches compound it even if the method were added: (1) the pygame runtime treats x/y as OFFSETS from the instance (action_executor.py:1381: check_x = instance.x + x_offset) while the generated code passes them as absolute coordinates with defaults 'self.x'/'self.y' (line 129-130); (2) the 'not_flag' parameter (used by the shipped plateforme_2 sample: {"action": "if_collision", "parameters": {"x": "0", "y": "1", "object": "solid", "not_flag": true}}) is read by the pygame runtime (action_executor.py:1351) but completely ignored by the Kivy generator, inverting the condition.
  - **Failure:** A teacher exports the working plateforme_2 sample (or any project whose jump/ground check uses if_collision/check_empty) to Android via the Kivy exporter. In the exported APK, the player's keyboard-up event raises AttributeError: 'obj_personnage' object has no attribute 'check_collision_at' — depending on the surrounding try/except the event either crashes or silently does nothing, so the character can never jump even though the same project works in Test Game.
  - **Suggested fix:** Define check_collision_at(x, y, object_name) in the generated GameObject base class (offset-from-self semantics matching action_executor.execute_if_collision_action), honor not_flag by emitting 'if not ...' when set, and use offsets (self.x + x, self.y + y) for if_collision to match the pygame runtime.
  - **Verifier:** code_generator.py:131/141/151/163/165 emit self.check_collision_at(), but no 'def check_collision_at' exists anywhere in kivy_exporter.py's generated GameObject code (only check_collision(self, other) at :1844) or the templates; the generated scene update loop calls instance.on_update(dt) with no try/except, so plateforme_2's step-event if_collision raises AttributeError in the exported game. The not_flag (action_executor.py:1351/1397) and offset semantics (:1381) mismatches also verified.

- [x] **M35 — `export/Kivy/kivy_exporter.py:2059` — keyboard_release events are silently dropped from the Kivy export** — FIXED (dict-expansion adds a keyboard_release branch; _generate_keyboard_release_handler emits the dispatched on_keyboard_up; tests/test_kivy_keyboard_export.py)
  - *Found by:* export-mobile; *category:* correctness
  - When converting the events dict, only 'keyboard' (line 2059) and 'keyboard_press' (line 2072) get the per-key sub-event expansion. A 'keyboard_release' event (nested {KEY: {actions: [...]}} like its siblings) falls into the 'regular event' branch, gets event_type='keyboard_release' with no top-level 'actions' key, and is then skipped by `if not actions: continue` in _generate_event_methods (line 2225). Even if actions did survive, _get_event_method_name maps it to 'on_keyboard_release' (line 2537), a method nothing ever dispatches — the generated scene's on_keyboard_up calls instance.on_keyboard_up only. Three bundled platformer samples (plateforme_2/3/4) use keyboard_release LEFT/RIGHT -> set_variable + stop_movement.
  - **Failure:** A student exports plateforme_2 to desktop EXE or Android. In the IDE the character stops when arrow keys are released; in the exported game the release actions never run, so the character keeps sliding forever after a single key press.
  - **Suggested fix:** Add a 'keyboard_release' branch to the dict-expansion (mirroring keyboard_press) and generate a consolidated on_keyboard_up(self, key, scancode) handler keyed by key codes, which the scene already dispatches.
  - **Verifier:** kivy_exporter.py:2059-2092 expand only 'keyboard' and 'keyboard_press'; 'keyboard_release' (nested {KEY: {actions}}) falls to the regular-event branch with no top-level 'actions' key and is dropped by 'if not actions: continue' (:2225); even if it survived, the mapped 'on_keyboard_release' (:2537) is never dispatched — generated scenes call only on_keyboard_up (:1497). plateforme_2/3/4 all author keyboard_release events, so their release actions (image_speed reset, stop_movement) are silently lost on export.

- [x] **M36 — `export/Kivy/kivy_exporter.py:2271` — Grid-movement heuristic hijacks ALL keyboard events and discards every non-speed action (jump, sprite flip, collision checks)** — FIXED (uses_grid_movement now keys off if_on_grid only; plain set_hspeed/set_vspeed events use the normal handler that keeps all actions; tests/test_kivy_keyboard_export.py)
  - *Found by:* export-mobile; *category:* correctness
  - _generate_keyboard_handler sets uses_grid_movement=True if ANY keyboard event contains if_on_grid, set_hspeed or set_vspeed (lines 2264-2279) — which is true of virtually every game that moves with the keyboard, including platformers. It then replaces the user's keyboard logic wholesale with a synthesized grid-stepping on_update (_generate_grid_keyboard_handler), which only extracts speed magnitudes (lines 2327-2354) and throws away every other authored action. In plateforme_2's player, keyboard right/left = [set_sprite, set_hspeed] and keyboard up = [if_collision, set_vspeed] (the jump): the export discards set_sprite (no facing flip), discards the on-ground check and the jump entirely, and converts free horizontal movement into one-cell grid snapping — completely wrong for a platformer.
  - **Failure:** A teacher exports the bundled plateforme_2 platformer to Android. The exported character can no longer jump at all, never changes sprite direction, and moves in rigid 32px grid steps instead of smooth platformer motion.
  - **Suggested fix:** Restrict the heuristic to events that actually contain if_on_grid (the grid marker), and generate the normal keyboard handler for plain set_hspeed/set_vspeed events; alternatively, generate the grid mover only for keys whose action list is purely movement and keep the remaining actions in a real on_keyboard handler.
  - **Verifier:** kivy_exporter.py:2264-2279 flag uses_grid_movement on ANY set_hspeed/set_vspeed in a keyboard event — true for plateforme_2's left/right keys — and _generate_grid_keyboard_handler (:2319-2463) extracts only speed magnitudes, discarding set_sprite and the entire if_collision-gated jump, replacing free motion with grid-cell snapping/targets. Verified against plateforme_2's actual event JSON; the platformer heuristic misfire and action loss are real.

- [x] **M37 — `export/Kivy/kivy_exporter.py:2357` — Generated classes can contain duplicate method definitions — a step event silently shadows the synthesized keyboard on_update** — FIXED (_merge_duplicate_methods merges same-named method bodies into one def; tests/test_kivy_keyboard_export.py)
  - *Found by:* export-mobile; *category:* correctness
  - _generate_grid_keyboard_handler emits `def on_update(self, dt):` (line 2357), and the event_map also maps the 'step' event to 'on_update' (line 2531). _generate_event_methods appends the keyboard-derived method first and the step-derived method later into the same class body, so for an object that has both keyboard-movement events and a step event, Python keeps only the LAST def: the entire synthesized keyboard movement handler is dead code. The bundled plateforme_2 player has exactly this combination (keyboard right/left/up with set_hspeed + a step event), so the exported character ignores keyboard input entirely. Same collision class exists between _generate_keyboard_handler and _generate_keyboard_press_handler, which both emit `def on_keyboard(...)` when an object has both keyboard and keyboard_press events.
  - **Failure:** Export plateforme_2: obj_personnage.py contains two `def on_update` methods; the step version (gravity logic) overrides the keyboard-movement version, so arrow keys do nothing and the player just falls in place.
  - **Suggested fix:** Merge generated bodies that target the same method name (emit one on_update containing both the keyboard-state block and the step actions; one on_keyboard containing both press-style dispatches), or at least fail the export loudly when a duplicate method name is about to be emitted.
  - **Verifier:** Empirically reproduced on the bundled samples/plateforme_2/objects/obj_personnage.json: the generated class contains two `def on_update(self, dt)` (grid-keyboard handler from _generate_grid_keyboard_handler:2357, then step via event_map 'step'->'on_update'); _generate_event_methods has no dedup, so the later step def shadows the keyboard movement handler and arrow keys are dead in the exported game. The keyboard/keyboard_press collision on `def on_keyboard` exists too.

- [x] **M38 — `export/exe/exe_exporter.py:312` — Project names containing an apostrophe (or quote) break the generated PyInstaller spec / macOS launcher — desktop export fails for legitimate French names** — FIXED (game_name sanitized to a word-char token in exe/linux/macos specs + crash-log; macOS display name embedded via repr; tests/test_exe_export_robustness.py)
  - *Found by:* export-desktop-scripts; *category:* correctness
  - The project name is interpolated unescaped into single-quoted Python string literals in generated build files: exe spec "name='{game_name}.exe'," (line 312), linux spec "name='{game_name}'," (linux_exporter.py:273), macOS spec name/display_name/bundle lines (macos_exporter.py:441, 467, 471-472), and the macOS launcher script's crash-log paths "os.path.join(app_parent, '{game_name}_crash.log')" (macos_exporter.py:245, 252). .replace(' ', '_') is applied but apostrophes/quotes pass through, producing e.g. name='L'aventure.exe' — a Python SyntaxError when PyInstaller execs the spec (or when modulegraph parses the macOS launcher). The export then aborts with a cryptic PyInstaller traceback unrelated to the actual cause.
  - **Failure:** A French teacher names a project "L'aventure" (apostrophes are ubiquitous in French titles; this is an educational French-context product). Windows/Linux/macOS desktop export runs the full Kivy generation, then PyInstaller fails with 'SyntaxError: invalid syntax' inside game.spec; the user sees 'PyInstaller build failed' with no hint that the project name is the problem.
  - **Suggested fix:** Sanitize game_name for use in generated code (e.g. re.sub(r'[^A-Za-z0-9_]', '_', name)) for the spec/exe/bundle name, and embed free-text strings (display name, crash-log name) via repr(...) instead of manual single quotes.
  - **Verifier:** game_name = project_data.get('name','Game').replace(' ','_') (exe_exporter.py:204) is interpolated unescaped into single-quoted Python literals in the generated PyInstaller spec (line 312 name='{game_name}.exe'), and the same pattern in linux_exporter.py:273 and macos_exporter.py. Only spaces are replaced; an apostrophe passes through, so a project named L'aventure produces name='L'aventure.exe' = a SyntaxError when PyInstaller execs the spec. NewProjectDialog.accept_project does no name sanitization and apostrophes are valid folder chars, so the failure is reachable for ordinary French names.

- [x] **M39 — `export/exe/exe_exporter.py:415` — EXE export reports success and deletes the built .exe when copying to the output folder fails** — FIXED (_copy_to_output returns False on failure; export() skips cleanup and emits failure pointing at the surviving build; tests/test_exe_export_robustness.py)
  - *Found by:* export-desktop-scripts; *category:* correctness
  - _copy_to_output retries PermissionError 5 times, then swallows the failure with only logger.warning/logger.info ('The EXE is available at: <dist path>'). export_project has no way to know the copy failed: it proceeds to the cleanup step, which (unless include_debug is set) runs shutil.rmtree(build_dir) — deleting the dist/ folder that holds the only copy of the built .exe — and then emits export_complete(True, 'Game exported successfully to: <output>'). Contrast with the macOS exporter, which catches copy failures and emits a failure message pointing at the surviving dist dir; the Linux exporter lets the exception propagate to the failure path. Only the EXE exporter swallows it.
  - **Failure:** A teacher on Windows exports to a folder where the freshly built .exe is locked by antivirus/Dropbox (the exact scenario the retry loop was written for). All 5 copy attempts fail. The IDE shows 'Game exported successfully to: <folder>', but the folder is empty — and the fallback location the log mentioned (build_temp_exe/dist) was just deleted by _cleanup. The user distributes/looks for a game that does not exist.
  - **Suggested fix:** Make _copy_to_output return False (or raise) when any item ultimately fails to copy; in export_project, on copy failure skip _cleanup and emit export_complete(False, ...) telling the user where the build survived (mirroring the macOS exporter's copy-failure branch).
  - **Verifier:** _copy_to_output (exe_exporter.py:392-416) catches PermissionError, retries 5x, then only logs warning/info and returns normally — it does not raise or signal failure. export_project (line 84-92) unconditionally proceeds to _cleanup, which shutil.rmtree(build_dir) deletes build_dir/dist (the fallback copy the log mentions), then emits export_complete(True,...). So a locked output (antivirus/Dropbox, the exact retry scenario) reports success with an empty output folder and no surviving copy.

- [x] **M40 — `importers/gmk_converter.py:857` — Unbounded image dimensions cause multi-GB allocation / OOM in _bgra_to_image** — FIXED (_check_image_dimensions caps dims at 8192px before allocation in _bgra_to_image and the strip surface; per-sprite/bg try/except skips+warns; tests/test_gmk_image_bounds.py)
  - *Found by:* importers; *category:* resource-leak
  - _bgra_to_image computes expected = width * height * 4 and, when the supplied pixel buffer is shorter than expected, pads it with b"\x00" * (expected - len(bgra_data)) and then copies it into a bytearray. width and height come straight from the parser, which reads them as int32 from the .gmk with no upper bound (for v800 sprites the actual stored data_length can be tiny while width/height are huge — gmk_parser.py:743-747 only checks width>0 and height>0). A sprite declaring width=height=40000 with 4 bytes of pixel data forces allocation of ~6.4 GB (twice), from a .gmk only a few dozen bytes larger.
  - **Failure:** A teacher imports a malicious .gmk whose sprite section declares enormous frame dimensions but contains almost no pixel data. During _convert_sprites the importer tries to allocate gigabytes for the padded RGBA buffer and the IDE crashes / is OOM-killed.
  - **Suggested fix:** Clamp/validate width and height against a sane maximum (e.g. a few thousand px) in the parser or before allocation in _bgra_to_image, and reject (warn + skip) frames whose declared size grossly exceeds the available pixel data.
  - **Verifier:** gmk_parser.py:743-747 reads sprite width/height as int32 guarding only width>0 and height>0, while data_length is a separate field; _bgra_to_image (gmk_converter.py:853-859) computes expected=width*height*4 and pads a short buffer to that size then copies it into a bytearray and Image.frombytes. A malicious .gmk declaring width=height=40000 with a few bytes of pixel data forces multi-GB allocations (and a ~1.6e9-iteration Python channel-swap loop) from a tiny file — OOM/DoS via the untrusted .gmk foreign format. No upper-bound clamp exists.

- [x] **M41 — `importers/gmk_reader.py:187` — Unbounded zlib decompression (decompression bomb) when importing a .gmk** — FIXED (read_zlib_data uses a bounded decompressobj capped at 512MB and rejects oversized blocks; tests/test_gmk_zlib_bomb.py)
  - *Found by:* importers; *category:* security
  - read_zlib_data reads a compressed block and calls zlib.decompress(compressed) with no output-size cap. The compressed length is validated (>= 0) but the decompressed size is not bounded. .gmk files are explicitly untrusted (a teacher imports a downloaded/student file), and zlib readily achieves >1000:1 ratios, so a small compressed block can inflate to many gigabytes. This path is used for the settings block, triggers, includes, pre-v800 sprite/background BMPs, and v440 sounds, so it is reachable on essentially every import.
  - **Failure:** A teacher imports a malicious .gmk containing a ~5 MB zlib block that decompresses to ~5 GB. zlib.decompress allocates the full output, exhausting memory and crashing (or freezing/OOM-killing) the IDE during import.
  - **Suggested fix:** Use a bounded decompressor: zlib.decompressobj().decompress(compressed, max_length=CAP) and raise GmkReadError if unconsumed_tail remains, with a generous but finite CAP (e.g. a few hundred MB).
  - **Verifier:** gmk_reader.py:171-189 validates only the compressed length and calls zlib.decompress(compressed) with no output cap (no decompressobj/max_length); the multi-GB allocation happens inside decompress before any caller's except can fire, and read_zlib_data/read_zlib_stream are reached on every import (settings block at gmk_parser.py:514). .gmk is an explicitly untrusted format (the import code itself talks about grading student submissions, and the repo already hardened the analogous Roberta-XML entity-expansion DoS with defusedxml), so a ~5 MB bomb inflating >1000:1 can OOM-kill the IDE mid-import.

- [x] **M42 — `importers/roberta_importer.py:388` — Roberta import writes LED color parameters as r/g/b, but the runtime and action editor read red/green/blue — imported LED colors are silently lost** — FIXED (LED actions now emit canonical red/green/blue keys; tests/test_roberta_led_keys.py)
  - *Found by:* export-desktop-scripts; *category:* correctness
  - _convert_block for mbedActions_leds_on emits pygm2 actions with parameter keys 'r', 'g', 'b' (e.g. _action("thymio_set_led_top", r=str(r), g=str(g), b=str(b))). The canonical parameter names everywhere else are 'red'/'green'/'blue': the action definition (actions/thymio_actions.py:133-150, ActionParameter name="red"...), the runtime handler (runtime/thymio_action_handlers.py:90, parameters.get('red', 0)), and the Aseba exporter (params.get('red', '0')). Only the Roberta *exporter* has a compensating fallback (params.get("red", params.get("r", "0"))). So after a Roberta import, LED actions execute with red=green=blue=0 and the action-config dialog shows 0/0/0.
  - **Failure:** A student imports a Roberta program containing 'turn LED on (red)'. In pygm2 the imported thymio_set_led_top action runs with parameters.get('red',0)==0 — the LED stays off in the simulator and on the robot — and opening the action in the editor shows Red/Green/Blue all 0, silently discarding the imported color.
  - **Suggested fix:** In importers/roberta_importer.py change the three LED returns to use the canonical keys: _action("thymio_set_led_top", red=str(r), green=str(g), blue=str(b)) (and likewise bottom_left/bottom_right).
  - **Verifier:** roberta_importer.py:382-389 emits LED actions with parameter keys r/g/b while every consumer reads red/green/blue: runtime/thymio_action_handlers.py:90-116 (parameters.get('red', 0) etc.), actions/thymio_actions.py action definitions, python_code_parser.py:205-207, and the Aseba exporter; only export/Roberta/roberta_exporter.py:338 has the compensating params.get("red", params.get("r", "0")) fallback. So every Roberta-imported LED action silently executes as 0/0/0 — wrong behavior in the importer's core classroom use case.

- [x] **M43 — `runtime/action_executor.py:204` — exit_event is swallowed at every nesting level — inside if_condition then/else branches or repeat blocks it does not abort the event** — FIXED (nested action lists run through the non-catching _execute_action_list_inner so _ExitEvent reaches the single top-level catch; also removed if_condition's internal then/else run that double-executed every branch; tests/test_exit_event_nesting.py)
  - *Found by:* runtime-action-executor; *category:* correctness
  - The _ExitEvent sentinel's docstring (lines 37-44) and execute_exit_event_action's docstring (lines 274-287) state the exception is 'Caught ... at the top-level dispatch boundary — so a nested then_actions / else_actions / repeat block can still trip it and unwind through every level at once.' But nested action lists are executed via the same `execute_action_list`, which unconditionally catches _ExitEvent (lines 202-205). Concretely: (a) the generic nested-conditional branch in execute_action runs `self.execute_action_list(instance, actions_to_run)` (line 436); (b) execute_if_condition_action runs `self.execute_action_list(instance, then_actions)` (lines 4350-4354); (c) _handle_repeat_action runs each iteration through `self.execute_action_list(instance, block_actions)` (line 331). In all three, an exit_event raised inside the sublist is caught at that innermost boundary, the nested call returns normally, and the OUTER event continues — for repeat, the remaining iterations even keep running. The bug is proven by internal inconsistency: execute_if_on_grid_action (lines 1253-1259) and _dispatch_room_test (lines 1790-1793) execute their nested then/else actions one-by-one via execute_action, where the `except _ExitEvent: raise` (lines 440-445) propagates correctly and the whole event aborts. So exit_event semantics differ depending on which conditional action it sits inside.
  - **Failure:** A student building in the Blockly/nested style (the form that produces then_actions/else_actions — e.g. an if_condition block) puts 'Exit Event' inside the if-branch, exactly like the documented monster pattern: 'tries each candidate direction, and the moment one fits it commits + exits so the later fallbacks don't undo the choice.' The exit only terminates the then-branch; the fallback direction actions after the if still run and overwrite the chosen direction, so the monster takes the last fallback every time. Inside `repeat 10 { ... exit_event }` the loop still performs all 10 iterations.
  - **Suggested fix:** Catch _ExitEvent only at the true top level: e.g. keep _execute_action_list_inner as the recursive workhorse, route all nested callers (execute_action line 436, execute_if_condition_action, _handle_repeat_action) through a non-catching variant (or simply call _execute_action_list_inner / re-raise when self._event_depth indicates a nested context), and let execute_event / execute_collision_event own the single catch.
  - **Verifier:** Reproduced empirically against the live executor: exit_event inside then_actions of the generic conditional path (action_executor.py:436) and of if_condition (line 4351) is caught by the inner execute_action_list's `except _ExitEvent: return` (lines 202-205), so the outer event continues, and a repeat block (line 331) runs all remaining iterations plus the actions after the block — directly contradicting both docstrings (lines 37-44, 280-286) and the sibling handlers (if_on_grid 1255-1259, _dispatch_room_test 1791-1792) that execute nested actions per-action so _ExitEvent propagates. Commit 49039a3's verification only exercised the propagating per-action variant, which is why the swallowing variants went unnoticed.

- [x] **M44 — `runtime/action_executor.py:432` — Thymio conditional blocks from Blockly are inert: runtime never executes their `sub_actions`, and a false condition skips the unrelated next action** — FIXED (generic conditional dispatch honors sub_actions on the action dict or in parameters, returning None so the sibling isn't skipped; tests/test_blockly_sub_actions.py)
  - *Found by:* runtime-support; *category:* correctness
  - Blockly serializes every Thymio condition block (thymio_if_proximity, thymio_if_ground_dark/light, thymio_if_button_pressed/released, thymio_if_variable) with its nested DO-slot children under a top-level `sub_actions` key (editors/object_editor/blockly/blockly_generators.js:431 `}, sub_actions: proxSubActions};`), and the Blockly->Python->events round trip preserves that shape (editors/object_editor/python_code_parser.py:797-803 emits `"action": "thymio_if_proximity", "parameters": {...}, "sub_actions": sub_actions`). The runtime, however, only knows two conditional shapes: nested `parameters.then_actions`/`else_actions` (action_executor.py:431-436) or flat GM80 skip-next flow. `grep -rn sub_actions runtime/ core/` returns nothing — no code path ever reads `sub_actions`. So the handler's boolean return falls through to the flat-list flow: when the condition is TRUE the nested children are never executed; when it is FALSE, `skip_next` is set and the *sibling* action placed after the if-block in the same event is silently skipped. Empirically reproduced: executing `[{action: thymio_if_button_pressed (button held), sub_actions:[thymio_set_led_top red]}]` leaves the LED at (0,0,0); executing the false-condition variant followed by an unrelated `thymio_set_led_bottom_left` leaves that LED at (0,0,0) too (sibling skipped). Affects both the playground runner and Thymio instances in game rooms (register_thymio_actions is used by both).
  - **Failure:** A student uses the Blockly Thymio preset to build the canonical lesson: "on proximity update: if front sensor > 2000 -> stop motors / set LED red", then a `thymio_move_forward` block after the if. At runtime the robot never stops or lights up (the if's children never run), and whenever the sensor reads below threshold the following move_forward block is skipped too — the program visibly does nothing it was built to do.
  - **Suggested fix:** In ActionExecutor.execute_action, when a handler returns a bool, also honor the Blockly shape: `then_actions = parameters.get("then_actions") or action_data.get("sub_actions") or parameters.get("sub_actions", [])` (the load path blockly_workspace.html:2098 also copies sub_actions into parameters, so check both), and run it as the then-branch. Mirror the same lookup in the collision-list conditional path. Alternatively normalize sub_actions -> parameters.then_actions at project load.
  - **Verifier:** Verified by repo-wide grep (sub_actions exists only in blockly_generators.js and python_code_parser.py — no runtime consumer) and reproduced empirically: with a true condition the nested LED action never runs (LED stays (0,0,0)), and with a false condition the boolean return falls through to the GM80 skip_next flow and silently skips the unrelated sibling action; the Blockly thymio_if blocks are exposed in config/blockly_config.py:173-178 and both the playground runner and game rooms dispatch through the same ActionExecutor. Worse, the Blockly path emits the key `type` (not `action`) for these blocks, so execute_action (line 398) treats them as missing-action no-ops. Graded medium per the rubric (wrong behavior, no crash/data loss), though it renders the Blockly Thymio conditional feature entirely inert.

- [x] **M45 — `runtime/action_executor.py:2868` — execute_code's exec namespace defines 'sel' but not 'self' (contradicting its own docstring) — every unrecognized custom statement the code parser preserves as execute_code silently NameErrors at runtime** — FIXED ('self' was already bound in 82f9b04; this also binds 'other' in execute_code/execute_script for collision-context code, matching the eval path; tests/test_execute_code_self.py)
  - *Found by:* object-editor; *category:* correctness
  - The object editor's code parser deliberately wraps every statement it can't map to an action into an execute_code action via ast.unparse (python_code_parser.py:_extract_actions_from_body), and that code is written in terms of `self.` because that is what the editor generates and tells users to write (the generated class methods are `def on_create(self):`). But execute_execute_code_action's namespace is `{'__builtins__': __builtins__, 'sel': instance, 'game': ..., 'instance': instance, 'math': ..., 'random': ...}` — no 'self' (and no 'other'), even though the docstring at line 2849 promises "self: the current instance". exec("self.points += 1", ...) raises NameError ('self' is not defined — verified), which is caught at line 2889 and only logged, so the statement is a silent no-op. This breaks the parser's core 'preserve unknown code' contract: e.g. `self.points += 1`, `self.image_xscale = -1`, or any if-statement referencing self that fell through to execute_code does nothing at runtime.
  - **Failure:** A teacher writes `self.coins = self.coins + 1` in the collision event via Edit Custom Code. The editor accepts it (status 'Saved'), the events panel shows an Execute Code action, and at runtime nothing happens — the coin counter never increments, with only a console log the user never sees.
  - **Suggested fix:** Add 'self': instance (and, when invoked from a collision context, 'other') to exec_globals in both execute_execute_code_action and execute_execute_script_action, matching the eval namespace at runtime/action_executor.py:4198 which already includes 'self'.
  - **Verifier:** exec_globals at runtime/action_executor.py:2868 binds 'sel' and 'instance' but not 'self', while the docstring promises 'self' and the editor's parser (python_code_parser.py:418-448) emits execute_code actions containing 'self.'-form code for any unrecognized statement (custom vars like self.coins aren't in ATTRIBUTE_TO_ACTION). Reproduced the NameError empirically with the exact namespace; the except at :2889 only logs, so the statement is a silent no-op. execute_script (line 4198) does bind 'self', confirming the asymmetry is a bug.

- [x] **M46 — `runtime/action_executor.py:4816` — Instances created via create_random_instance / create_moving_instance are not rendered (depth-sort cache never invalidated)** — FIXED (both creators set current_room._depth_dirty = True after _add_to_grid; tests/test_create_moving_random.py)
  - *Found by:* runtime-action-executor; *category:* correctness
  - GameRoom.render() draws from a cached list: `if self._depth_dirty or self._sorted_instances is None: self._sorted_instances = sorted(self.instances, ...)` (game_runner.py:1493-1496) and iterates only `self._sorted_instances`. The flag is set only at room init, rebuild_spatial_grid, the destroy sweep (only when something was destroyed, game_runner.py:2358), and in execute_create_instance_action (action_executor.py:3096, which explicitly sets `_depth_dirty = True  # Mark for re-sort`). The two sibling creation actions, execute_create_random_instance_action (lines 4814-4817) and execute_create_moving_instance_action (lines 4917-4921), append the new instance to `room.instances` and call `_add_to_grid` + `invalidate_collision_listened_types()` but never set `_depth_dirty` (and `_add_to_grid` does not set it either, verified at game_runner.py:1303-1315). So the new instance exists, moves, and collides, but is missing from `_sorted_instances` and is therefore invisible until some unrelated event (any instance destruction, room change) dirties the cache.
  - **Failure:** A student uses the 'Create Moving Instance' action (e.g. a spawner firing a bullet, or an enemy emitter) in a room where nothing has been destroyed yet. The bullet flies, collides, and can kill the player, but is never drawn on screen — an invisible projectile. In a game with no destructions at all, every instance spawned by these two actions stays permanently invisible while still being fully active in the simulation.
  - **Suggested fix:** Add `self.game_runner.current_room._depth_dirty = True` after the `_add_to_grid` call in both execute_create_random_instance_action and execute_create_moving_instance_action, mirroring execute_create_instance_action line 3096 (or better: move the add-to-room block into a shared helper used by all three creators).
  - **Verifier:** render() draws only the cached _sorted_instances, rebuilt solely when _depth_dirty (game_runner.py:1493-1496); the only writers are room init (:1174), rebuild_spatial_grid (:1287, called only on room build/change/remember-destroyed prune), the destroy sweep (:2358, only when something was destroyed), and execute_create_instance_action (action_executor.py:3096). execute_create_random_instance_action (:4814-4817) and execute_create_moving_instance_action (:4918-4921) append + _add_to_grid (which does not set the flag), so the new instance is simulated and collides but is not rendered until an unrelated destruction or room change dirties the cache.

- [x] **M47 — `runtime/action_executor.py:4900` — create_moving_instance ignores parent inheritance — spawned instance loses all inherited events and properties** — FIXED (resolve_parent_inheritance applied before set_object_data, and create events read from merged_data; tests/test_create_moving_random.py)
  - *Found by:* runtime-action-executor; *category:* correctness
  - execute_create_instance_action (line 3084) and execute_create_random_instance_action (line 4805) both run `merged_data = resolve_parent_inheritance(object_data, self.game_runner._objects_data)` before `new_instance.set_object_data(merged_data)`, matching how room-built instances are constructed (game_runner.py:1422). execute_create_moving_instance_action instead calls `new_instance.set_object_data(object_data)` (line 4900) on the raw, unmerged object data, and later reads `events = object_data.get('events', {})` (line 4924). Since set_object_data derives `_collision_targets` and the runtime dispatches step/keyboard/collision events from `instance.object_data['events']`, an instance of a child object spawned this way gets none of the events or properties its parent chain defines — diverging from every other way the same object can enter a room.
  - **Failure:** A teacher builds an `obj_projectile` parent holding the shared `collision_with_obj_wall` (destroy self) and `outside_room` events, with children `obj_arrow`, `obj_fireball` that only set a sprite. Instances placed in the room editor or created with 'Create Instance' behave correctly; the same `obj_fireball` fired with 'Create Moving Instance' (the natural choice for a projectile, since it takes speed+direction) never reacts to walls and flies through everything forever, because its inherited collision events were silently dropped.
  - **Suggested fix:** Mirror the other two creators: `merged_data = resolve_parent_inheritance(object_data, self.game_runner._objects_data); new_instance.set_object_data(merged_data)` and read `events = merged_data.get('events', {})` for the create-event dispatch.
  - **Verifier:** execute_create_moving_instance_action calls new_instance.set_object_data(object_data) on the raw dict (~:4900) and reads events from object_data (~:4924), while both sibling creators and room-built instances use resolve_parent_inheritance first (action_executor.py:3084, :4805; game_runner.py:1422). set_object_data (game_runner.py:720-745) does no inheritance resolution and derives _collision_targets from the dict it receives, so a child object spawned this way loses all parent-defined events (collision, step, outside_room) and inherited properties.

- [x] **M48 — `runtime/game_runner.py:1794` — settings['room_speed'] written by the GMK importer (and project templates) is never read; runtime hardcodes 60 FPS so GMK games run at double speed** — FIXED (_load_project_settings reads room_speed into self.fps, clamped 1..240; tests/test_room_speed_fps.py)
  - *Found by:* cross-data-integrity; *category:* correctness
  - The GMK converter persists the source game's speed into project settings (importers/gmk_converter.py:162 `room_speed = gs.game_speed if gs else 30`, :176 `"room_speed": room_speed`) — GM8 games default to 30. The pygame runtime never reads it: GameRunner._load_project_settings (game_runner.py:1965-1977) only loads lives/score/health/caption flags, and the frame rate is hardcoded `self.fps = 60` (game_runner.py:1794), only changeable at runtime via the set_room_speed ACTION (action_executor.py:3858 sets game_runner.fps). Since the engine is per-frame (x += hspeed each tick, alarms count frames), running a 30-step/s game at 60 FPS doubles every speed, gravity and alarm rate. A repo-wide grep confirms no other reader of settings['room_speed'] (the Kivy exporter also hardcodes 1.0/60.0).
  - **Failure:** A teacher imports their old GameMaker 8 game (game speed 30, the GM default). In Test Game everything — player movement, gravity, monster patrols, alarm timers — runs exactly twice as fast as the original, making imported games unplayably fast with no indication why; the converter dutifully stored room_speed=30 but nothing consumes it.
  - **Suggested fix:** In GameRunner._load_project_settings, read `self.fps = int(settings.get('room_speed', 60))` (clamped like execute_set_room_speed_action). Optionally do the same in the Kivy exporter's Clock.schedule_interval rate.
  - **Verifier:** gmk_converter.py:162-176 persists settings['room_speed'] (GM8 default 30), but a repo-wide grep shows no reader: GameRunner._load_project_settings (game_runner.py:1965-1979) loads only lives/score/health/caption flags, self.fps is hardcoded to 60 (:1794, used at clock.tick :2371), and the only fps writer is the authored set_room_speed action (action_executor.py:3835). The engine is per-frame (hspeed/alarms per tick), so an imported 30-step/s GM8 game runs at exactly double speed.

- [x] **M49 — `runtime/game_runner.py:2245` — Main event loop iterates the live instance list; instances created during step/keyboard events are processed in the same frame, so any spawn cycle hard-hangs the game in one frame** — FIXED (begin_step/alarm/step, end_step/destroy, and all keyboard/mouse handler loops iterate a list() snapshot; tests/test_spawn_iteration_snapshot.py)
  - *Found by:* runtime-game-runner; *category:* correctness
  - The merged begin_step/alarm/step loop in run_game_loop is `for instance in self.current_room.instances:` over the LIVE list. ActionExecutor.execute_create_instance_action (action_executor.py:3094) appends the new instance to `current_room.instances` synchronously, so Python's list iteration visits the newly created instance later in the SAME frame and runs its begin_step/alarms/step too. If an object's step event creates an instance of an object whose step event also creates one (including the classic beginner mistake of an unconditioned self-spawner: obj_A step -> create_instance obj_A), every visited instance appends another, the for loop never terminates, and the frame never completes. Because the hang is inside this loop, pygame.event.get() is never reached again: ESC, the window close button, and the QUIT event are all dead, and memory grows until OOM. The same live-list iteration exists in handle_keyboard_press (line 2427), which both adds the pressed key to newly appended instances' keys_pressed and fires their keyboard_press event, so a key-press spawner cycle hangs identically. GameMaker semantics (instances created in a step are processed starting next step) would instead degrade over multiple frames, leaving the game closable. Even without a cycle, same-frame processing means a freshly created instance has its alarms decremented and step executed before its first render, an ordering divergence from the documented GM-7.0 event order this loop claims to implement.
  - **Failure:** A student making a rain/bullet spawner writes obj_spawner with a step event containing create_instance of obj_spawner (or A creates B and B creates A), forgetting the limiting condition. They press Test Game: the window freezes instantly on the first frame, ESC and the close button do nothing, Python consumes memory without bound, and the student has to kill the process from the IDE/OS. In GameMaker the same mistake produces visible exponential growth over several frames and remains closable.
  - **Suggested fix:** Iterate over a snapshot in the per-frame event loops: `for instance in list(self.current_room.instances):` in the begin_step/alarm/step loop, the end_step/destroy loop, and the input handlers (handle_keyboard_press/release, mouse handlers). Instances created mid-frame then start participating on the next frame, matching GameMaker and bounding per-frame work.
  - **Verifier:** Traced: the merged loop is `for instance in self.current_room.instances:` over the live list, execute_create_instance_action appends synchronously (only the create EVENT is deferred), and instance.step() runs the step event, so a spawn cycle never exits the for-loop; pygame.event.get() is downstream, so ESC/close are dead, and no instance cap exists. Downgraded to medium: it needs a student-authored unconditioned spawner, the IDE Stop button still terminate/kills the subprocess, and GM-correct semantics would also OOM within ~1s (exponential), so the practical delta is mainly the same-frame ordering divergence plus the unclosable window.

- [ ] **M50 — `runtime/game_runner.py:3413` — push_back_instance computes overlap from raw instance coordinates but bbox dimensions — separation lands at the wrong position whenever sprite origin or auto-bbox offsets are nonzero**
  - *Found by:* runtime-game-runner; *category:* correctness
  - separate_overlapping_instances detects overlap using bbox-in-world coordinates (`ix = instance.x - s1.origin_x + s1.bbox_left`, line 3385) and passes the bbox widths/heights (w1=bw1 etc., lines 3390-3393) to push_back_instance. But push_back_instance then computes overlaps and the resolved position from the RAW origin coordinates: `overlap_left = (moving_inst.x + w1) - static_inst.x` and `moving_inst.x = static_inst.x - w1`. The origin and bbox_left/top offsets of both sprites are dropped. Since _compute_collision_bbox now auto-derives the bbox from frame-0 opaque pixels by default, bbox_left/top are routinely nonzero (any sprite with a transparent margin), and GMK-imported sprites often have nonzero origins — so the resolved position is shifted by (origin1 - bbox_left1) - (origin2 - bbox_left2) per axis. The result either leaves the bboxes still overlapping (separation re-triggers every frame, visible jitter) or overshoots, knocking the instance off the movement grid.
  - **Failure:** In a Sokoban-style game, the player (sprite art with a 4px transparent margin, so auto bbox_left=4) pushes a solid box into a wall; separate_overlapping_instances pushes the player back to `box.x - bbox_width`, which is 4-8px away from the true flush position. The player is now off the 32px grid, so its if_on_grid-gated step movement never fires again and the character is stuck (or visibly jitters against the box every frame while separation keeps re-firing).
  - **Suggested fix:** Pass the bbox world top-left coordinates (ix, iy, ox, oy) into push_back_instance and compute overlaps from those; convert the resolved bbox position back to instance coordinates with `moving_inst.x = resolved_bbox_x + s1.origin_x - s1.bbox_left` (same for y).
  - **Verifier:** Traced: separate_overlapping_instances tests overlap in bbox-world coords (ix = x - origin_x + bbox_left, :3385) but push_back_instance (:3411-3428) resolves from raw instance.x with bbox widths, dropping origin/bbox offsets — the landing position is wrong by (o1-b1)-(o2-b2) per axis whenever the two sprites' offsets differ. _compute_collision_bbox (:214) defaults to the opaque-pixel union, so nonzero bbox_left/top is routine; the result is persistent bbox overlap or off-grid positions that break if_on_grid movement. Medium confirmed.

- [ ] **M51 — `runtime/game_runner.py:4006` — restart_current_room silently destroys persistent instances — a persistent player dies with the room on restart_room**
  - *Found by:* runtime-game-runner; *category:* correctness
  - change_room (line 4241-4247) collects instances whose object has `persistent: true` and carries them into the next room. restart_current_room has no such pass: it builds a brand-new GameRoom from the authored layout and assigns it (`self.rooms[room_name] = new_room; self.current_room = new_room`), discarding every instance in the old room object — including persistent ones that were carried in from other rooms. In GameMaker, persistent instances survive a room restart. If the current room's authored layout does not contain an instance of the persistent object (the normal case: the persistent player is placed only in room 1 and carried forward), the player ceases to exist after restart_room.
  - **Failure:** A teacher marks the player object persistent, places it only in room 1, and uses the standard 'on collision with monster: restart_room' death pattern. The player walks to room 2 (carried over by change_room) and touches a monster: restart_room rebuilds room 2 from its layout, which contains no player. The game keeps running with no controllable instance — a silent soft-lock the student cannot diagnose.
  - **Suggested fix:** Mirror change_room: before replacing the room in restart_current_room (and in restart_game for the first room, where a full reset of persistent instances may instead be intended), collect instances with object_data.get('persistent') from the old room, then after _apply_destroyed_memory re-add them to new_room (replacing any authored non-persistent instance of the same object, as change_room does) and skip their create events.
  - **Verifier:** Traced: restart_current_room (:3990-4050) rebuilds the room purely from authored room_data (only _apply_destroyed_memory is applied) and swaps it into self.rooms with no persistent-instance carry-over, whereas change_room (:4237-4282) explicitly collects instances with object_data['persistent'] and re-adds them. A persistent player carried into room 2 and not in that room's layout ceases to exist after restart_room — silent soft-lock, medium.

- [ ] **M52 — `runtime/game_runner.py:4118` — restart_game rebuilds only the first room; all other rooms keep the previous playthrough's mutated state**
  - *Found by:* runtime-game-runner; *category:* correctness
  - restart_game resets score/lives/health, clears _destroyed_memory (comment: 'forget every "stay destroyed" instance so bonuses and the like reappear on a fresh playthrough'), and recreates ONLY the first room (`self.rooms[first_room_name] = new_room`). Every other GameRoom object in self.rooms is left untouched. Because change_room reuses the stored room objects (line 4253 `self.current_room = self.rooms[room_name]`), rooms 2..N still carry the previous run's state: instances destroyed during the prior run were physically removed from room.instances (the to_destroy sweep at line 2348-2357), moved instances keep their positions, and change_instance'd instances keep their new types. So the fresh playthrough promised by the function's own comments only exists in room 1; clearing _destroyed_memory cannot resurrect instances in rooms whose instance lists were already mutated.
  - **Failure:** A student plays a 3-room maze, collects the bonuses and kills the monsters in room 2, dies in room 3, and the game-over handler runs the standard restart_game action. They replay room 1 normally, then enter room 2: it is empty — bonuses already collected, monsters already dead, pushed boxes still displaced — making the second playthrough trivially broken (or unwinnable if a required key/door instance was consumed).
  - **Suggested fix:** On restart_game, rebuild every room, not just the first: re-run the GameRoom construction for all entries in rooms_data (i.e., effectively redo load_rooms_without_sprites + assign_sprites_to_rooms + set_backgrounds_ref), or lazily mark all rooms stale and rebuild each on next entry.
  - **Verifier:** Traced: restart_game (:4052-4140) resets score/lives, clears _destroyed_memory, and rebuilds only room_list[0]; change_room reuses the stored GameRoom objects (self.current_room = self.rooms[room_name], :4253) whose instance lists were permanently mutated by the destroy sweep (room.instances = kept, ~:2357), and no rebuild happens on room entry. So after restart_game, rooms 2..N retain the previous playthrough's destroyed/moved/changed instances, contradicting the function's own 'fresh playthrough' intent. Medium.

- [ ] **M53 — `runtime/game_runner.py:4301` — Re-entering a previously visited room re-fires create events on the same already-initialized instances**
  - *Found by:* runtime-game-runner; *category:* correctness
  - change_room reuses the stored GameRoom object (`self.current_room = self.rooms[room_name]`, line 4253) — instances are NOT rebuilt and keep their runtime state (positions, variables, destroyed instances stay gone). Yet at lines 4301-4304 it unconditionally executes the create event for every non-persistent instance, every time the room becomes current. There is no first-entry guard (no visited flag exists anywhere in the file). On the second and later entries this re-runs create on live, already-initialized instances whose state was deliberately preserved, which is neither GameMaker's non-persistent semantics (fresh instances + create) nor its persistent semantics (kept instances, no create). Create-event side effects therefore accumulate per visit: create_instance actions spawn a duplicate companion each entry, relative variable changes apply repeatedly, and alarms/randomized state get re-armed on instances that may be mid-behavior at a moved position.
  - **Failure:** A student builds a hub world: room 1 has a door to room 2 and back. obj_boss in room 2 has a create event that spawns its health-bar companion via create_instance. The player enters room 2, walks back to room 1, and returns: the boss (still at its mid-fight position with reduced health variable) re-runs create — a second health bar appears, its hp variable is reset, and its movement alarm restarts. Each round trip adds another duplicate companion.
  - **Suggested fix:** Track per-instance create delivery (e.g., set `instance._create_fired = True` in every path that executes the create event — startup, change_room, create_instance, room rebuild — and in change_room only fire create for instances where it is false). Room rebuilds (restart) naturally produce fresh instances with the flag unset.
  - **Verifier:** change_room (game_runner.py:4253) reuses the stored GameRoom — confirmed by the in-code comment at lines 1761-62 ('change_room reuses the same room object') — yet lines 4301-4304 unconditionally re-execute the create event for every non-persistent instance with no first-entry/visited guard anywhere (no create-fired flag exists; execute_event in action_executor.py:148 has no once-only logic). Only restart_current_room/restart_game rebuild rooms; plain re-entry re-fires create on live, state-preserved instances, so create side effects (create_instance spawns, relative variable changes, alarms) accumulate per visit.

- [ ] **M54 — `runtime/game_runner.py:4550` — Modal dialogs swallow KEYUP events, leaving instance.keys_pressed stale — the player keeps moving after the dialog closes**
  - *Found by:* runtime-game-runner; *category:* correctness
  - show_message_dialog runs its own `pygame.event.get()` loop (lines 4550-4562) that handles only QUIT, KEYDOWN(Return/Space/Escape), and MOUSEBUTTONDOWN; all other events, including KEYUP, are consumed and dropped (it also calls `pygame.event.clear()` at line 4481 before the loop). Per-instance held-key state lives in instance.keys_pressed, which is only updated by handle_keyboard_release on a KEYUP event. If the user releases a movement key while the dialog is open, that KEYUP is eaten, the key remains in keys_pressed forever, and _process_held_keys (line 2489) keeps firing the keyboard-held event every frame after the dialog closes. show_highscore_dialog (line 4760) and _show_name_entry_dialog (line 4971) have the same blind spot.
  - **Failure:** Player holds Right-arrow and walks into a sign that triggers show_message. The dialog pauses the game; the player naturally releases Right while reading, then presses Enter to dismiss. The character now runs right on its own indefinitely (held-key event re-fires every frame from the stale keys_pressed entry) until the player happens to tap and release Right again. Students report it as 'my character is haunted after messages'.
  - **Suggested fix:** In each modal dialog's event loop, forward KEYUP to handle_keyboard_release(event.key) (or at minimum discard the key from every instance's keys_pressed); alternatively, after the dialog closes, resynchronize keys_pressed against pygame.key.get_pressed() for all instances.
  - **Verifier:** show_message_dialog's loop (game_runner.py:4550-4562) drains pygame.event.get() handling only QUIT/KEYDOWN/MOUSEBUTTONDOWN, so a KEYUP released while the dialog is open is silently consumed; instance.keys_pressed is only ever cleared by handle_keyboard_release (line 2557) on a KEYUP event, and _process_held_keys (line 2489) re-fires the held-key event every frame from the stale entry after the dialog closes. No resync against pygame.key.get_pressed() exists anywhere.

- [ ] **M55 — `runtime/thymio_simulator.py:43` — Ground sensors sample beside the robot (90 degrees off) instead of in front; readings disagree with the rendered indicators** *(corroborated by 1 other finder)*
  - *Found by:* runtime-support; *category:* correctness
  - GROUND_SENSOR_POSITIONS encodes the offsets transposed for the rotation formula used in update_ground_sensors (`sensor_x = self.x + (offset_x * cos_a - offset_y * sin_a)`, line 312). In that formula local +x is the robot's forward axis, so front-mounted sensors need offsets (forward=+40, lateral=±20); the constants are (±20, 40), which puts both sample points 40px to the robot's lateral side, 20px apart along the heading. Empirically verified: robot at (100,100) facing +x samples (80,140) and (120,140) — beside/below the robot — instead of ~(140, 100±20) in front. The renderer meanwhile draws the ground-sensor indicators correctly at the front (thymio_renderer.py:396: `sensor_positions = [(48 * s, -12 * s), (48 * s, 12 * s)]  # Left, Right at front`), so the green/red indicators reflect positions the simulator never samples. Real Thymio ground sensors are at the front edge. Affects playground and in-room Thymio instances alike (game_runner.py:5121 and playground_runner.py:364 both call sim.update with the rendered surface).
  - **Failure:** A teacher builds the standard line-following exercise: black line on the floor, 'if ground dark -> steer'. The robot reacts to floor pixels 40px off to its side rather than under its front, so it never detects the line it is visibly driving over; the on-screen indicators (drawn at the front) turn red over the line while thymio_if_ground_dark stays false — readings and visualization contradict each other.
  - **Suggested fix:** Change GROUND_SENSOR_POSITIONS to [(GROUND_SENSOR_OFFSET, -20), (GROUND_SENSOR_OFFSET, 20)] so the first component is the forward offset and the second the lateral one (left sensor at lateral -20, matching the front proximity sensors' negative-is-left convention).
  - **Verifier:** Empirically reproduced: with GROUND_SENSOR_POSITIONS = [(-20, 40), (20, 40)] (thymio_simulator.py:43-46) and the rotation at lines 312-313, a robot at (100,100) facing +x samples (80,140) and (120,140) — 40px to its lateral side — while update_physics moves along (cos, sin) of angle (forward = local +x) and the renderer draws the indicators at forward offset 48 (thymio_renderer.py:396, '(forward_offset, side_offset)'). The constants are transposed for the formula used; readings and the rendered indicators disagree, breaking line-following exercises.

- [ ] **M56 — `runtime/thymio_simulator.py:248` — Differential-drive rotation sign is inverted: 'Turn Left' turns the robot right on screen (and vice versa)**
  - *Found by:* runtime-support; *category:* correctness
  - The simulator works in screen coordinates (y down; docstring line 125: "0 = right, 90 = down") and the renderer draws the left wheel on the -y side when facing +x (thymio_renderer.py:109 "Top wheel (left side of robot when facing right)"). In that frame, driving the right wheel (left stopped) must pivot the nose toward -y (the robot's left). The code computes `angular_velocity_rad = (right_velocity - left_velocity) / THYMIO_WHEEL_BASE`, which is the y-up math-convention formula; with y down it has the wrong sign. Empirically verified: `set_motor_speed(0, 300)` — exactly what `execute_thymio_turn_left_action` issues (thymio_action_handlers.py:64) — makes the angle increase (+217 deg over 30 frames, wrapping to -142.9) and the robot curves downward/south (y 100 -> 183.5), i.e. to its RIGHT on the top-down view. So the 'Turn Left' Blockly block (exposed in config/blockly_config.py:150) turns right, 'Turn Right' turns left, and any asymmetric set_motor_speed turns opposite to a real Thymio.
  - **Failure:** A student programs 'when left button pressed -> turn left' in the playground; clicking the robot's left button (or pressing the Left arrow) makes the on-screen robot rotate clockwise toward its right. Any obstacle-avoidance or line-following logic transferred from real-Thymio teaching material steers the wrong way and spirals into walls.
  - **Suggested fix:** Flip the sign for the y-down screen frame: `angular_velocity_rad = (left_velocity - right_velocity) / THYMIO_WHEEL_BASE`. (The handlers' motor assignments — turn_left = right motor on — already match the real Thymio and become correct once the physics sign is fixed.)
  - **Verifier:** Empirically reproduced: execute_thymio_turn_left_action issues set_motor_speed(0, 300) (thymio_action_handlers.py:64); after 30 frames angle increased +217 deg (clockwise in this y-down frame — the renderer rotates the body by -angle at thymio_renderer.py:253 and places the left wheel on the -y side) and the robot curved to y=183.5, i.e. toward its right. The formula (right - left)/WHEEL_BASE at thymio_simulator.py:248 is the y-up convention; in screen coordinates the sign is inverted, so Turn Left turns right and vice versa. No test pins the current behavior.

- [ ] **M57 — `scripts/compile_translations.py:111` — compile_translations.py compiles the incomplete French split .ts files, creating split .qm files that hijack the loader away from the complete monolithic French translation**
  - *Found by:* export-desktop-scripts; *category:* data-integrity
  - The script compiles every translations/pygm2_*.ts with no guard. core/language_manager.py's get_translation_files prefers split files (pygm2_<lang>_<group>.qm) over the monolithic pygm2_<lang>.qm whenever any split .qm exists. French currently ships split .ts files but deliberately NO split .qm — only the hand-translated monolithic pygm2_fr.qm (1322 finished translations) is loaded. The fr split .ts set is drastically less complete (pygm2_fr_actions.ts: 17/155 translated; fr_core: 282/456; fr_misc: 24/115). The repo's own newer scripts (add_tutorial_dock_translations.py / add_python_operator_translations.py) added a should_compile() guard precisely because 'Compiling a split _misc.ts for a language that has no split set (e.g. fr) would create a brand-new split .qm that hijacks the loader and drops the full monolithic translation' — but this general-purpose script, the one a maintainer reaches for after editing translations, still has no such guard.
  - **Failure:** A maintainer runs 'python scripts/compile_translations.py' (its documented usage) after touching any .ts file. It silently emits pygm2_fr_core.qm, pygm2_fr_actions.qm, etc. On the next IDE start in French, the loader switches to the split set and most of the French UI (actions tab, editors, misc strings) reverts to English — a regression that looks unrelated to the command that caused it.
  - **Suggested fix:** Port the should_compile() guard from scripts/add_python_operator_translations.py: skip pygm2_<lang>_<group>.ts unless pygm2_<lang>_core.qm already exists (i.e. the language already uses a split set).
  - **Verifier:** Verified the directory state (fr ships split .ts files but no split .qm; only the monolithic pygm2_fr.qm with 1322 finished entries) and the loader preference (core/language_manager.py:149-157 returns the split set if ANY pygm2_fr_<group>.qm exists). compile_translations.py compiles every pygm2_*.ts with no guard (lines 111-129), and the fr split .ts files carry only 17/282/24/... active finished translations (the rest are 'vanished', which lrelease drops) — exactly the hazard the repo's own should_compile docstring in scripts/add_tutorial_dock_translations.py:140-159 warns 'hijacks the loader and drops the full monolithic translation'.

- [ ] **M58 — `utils/resource_packager.py:88` — Resource packager hardcodes .png for sprite/background files — assets in any other supported format are silently dropped from packages**
  - *Found by:* core-managers-utils; *category:* correctness
  - export_object/export_room locate dependency image files as `project_path / 'sprites' / f"{sprite_name}.png"` (lines 88, 168, 175) and import_object/import_room look up `f'sprites/{sprite_name}.png'` in the archive (lines 247, 338, 363). But AssetManager imports sprites/backgrounds with their original extension (SUPPORTED_FORMATS includes .jpg, .jpeg, .bmp, .gif, .tga, .webp; import_asset stores `f"{asset_type}/{asset_name}{file_path.suffix}"`). For any non-.png asset the export silently skips the image file (the `if sprite_path.exists()` check just fails), and on import the importer prints 'Sprite file not found' and does not add the sprite at all — the imported object references a sprite that does not exist in the recipient's project.
  - **Failure:** A teacher imports player.jpg as a sprite, builds an object around it, and exports it via 'Export Object Package' to share with the class. The .gmobj contains no image. Students importing it get an object whose sprite is entirely missing (the dependency was skipped, not even its metadata is added) — the object is invisible in the room editor and at runtime, with only a console print as a clue.
  - **Suggested fix:** Resolve the actual file from the asset's `file_path` (e.g. `project_path / sprite_data['file_path']`) on export, store it in the archive under that relative path, and on import extract whatever image member the dependency's `file_path` names (still routed through _safe_join).
  - **Verifier:** resource_packager.py hardcodes f"{name}.png" at all export sites (lines 88, 168, 175) and import lookups (247, 338, 363), while AssetManager.SUPPORTED_FORMATS allows .jpg/.jpeg/.bmp/.gif/.tga/.webp and import_asset stores the original suffix (asset_manager.py:155). For non-PNG sprites the export's exists() check silently fails and on import the sprites[name]=sprite_data add is inside the namelist() branch, so the sprite is dropped entirely with only a console print.

- [ ] **M59 — `widgets/asset_tree/asset_operations.py:384` — Deleting (or renaming) a room never deletes rooms/<name>.json; the orphan file resurrects the deleted room's instances when the name is reused**
  - *Found by:* widgets-assets; *category:* data-integrity
  - remove_asset_from_project only deletes the physical file referenced by `asset_data.get('file_path') or asset_data.get('project_path')`. Rooms (and objects) have neither key — their payload lives in external files rooms/<name>.json / objects/<name>.json referenced via '_external_file', which this code never handles. So deleting a room removes its project.json entry but leaves rooms/<name>.json on disk. This orphan is then actively harmful because of two project_manager behaviors: (1) `_save_rooms_to_files` (project_manager.py:631) preserves instances from an existing room file when the in-memory room has 0 instances and was not in `_rooms_loaded_this_session` — a newly created same-named room is exactly that case, so the FIRST SAVE injects the deleted room's instances into the new room's file; (2) `_load_rooms_from_files`/merge_room_file gives the file precedence for instances on every load. AssetManager.rename_asset has the same gap (only renames `file_path`-based files), so renaming a room also orphans rooms/<old>.json. No other code path cleans these up (verified: ide_window.on_asset_deleted, asset_manager.delete_asset).
  - **Failure:** A student deletes room 'level2' (confirming the dialog that says it 'will permanently remove the asset and its file' — it doesn't). Later they create a fresh, empty room named 'level2' and save. The preserve-instances safeguard reads the orphaned rooms/level2.json and writes the deleted room's full instance layout into the new room's file; on the next project load, the 'new' level2 is filled with the old deleted level's walls and monsters.
  - **Suggested fix:** In remove_asset_from_project, for rooms/objects/playgrounds also unlink the external payload file (e.g. project_path/'rooms'/f'{asset_name}.json', via the existing _safe_asset_path helper). In AssetManager.rename_asset, rename the external file alongside the cache key.
  - **Verifier:** Rooms have no file_path/project_path (only _external_file), and no code path deletes rooms/<name>.json on delete or rename (checked remove_asset_from_project:384-393, AssetManager.delete_asset:365-369, AssetManager.rename_asset:431-437, ide_window.on_asset_deleted). A new same-named room has instances=[] and is never added to _rooms_loaded_this_session (populated only in _load_rooms_from_files), so the preserve branch (project_manager.py:631-649) injects the orphan file's instances into the new room on first save, and merge_room_file gives the file precedence on the next load.

- [ ] **M60 — `widgets/asset_tree/asset_tree_widget.py:1083` — force_project_refresh reloads the project from disk with no dirty check, silently discarding unsaved in-memory changes (e.g. drag-reorder of rooms)** *(corroborated by 1 other finder)*
  - *Found by:* widgets-assets; *category:* data-integrity
  - force_project_refresh() calls project_manager.load_project(current_path) unconditionally. load_project re-reads project.json from disk, replaces current_project_data and assets_cache, and sets is_dirty_flag = False (project_manager.py:278) — without checking is_dirty or saving first. It is invoked after every asset delete (asset_operations.py:156) and after import_sprite_image (asset_tree_widget.py:576). Any change that only marks the project dirty is silently reverted and the dirty flag cleared, so even the close-with-unsaved-changes prompt won't fire. The clearest case is dropEvent's drag-reorder of rooms (lines 170-176), which deliberately only calls mark_dirty() ('The standard Ctrl+S path picks up the new order') — a subsequent delete of ANY asset reverts the reorder with no warning.
  - **Failure:** A student drags room3 to the top of the Rooms category (dropEvent reorders the cache and marks the project dirty but does not save). They then delete an unused sprite. delete_asset's force_project_refresh reloads project.json from disk: the room order snaps back to the old order, the dirty flag is cleared, and a later Ctrl+S saves the OLD order. The reorder is silently lost.
  - **Suggested fix:** In force_project_refresh, if project_manager.is_dirty, call save_project() before load_project() (or skip the disk reload and refresh the tree from the live in-memory data instead).
  - **Verifier:** Traced fully: dropEvent reorder is memory-only by explicit design comment (asset_tree_widget.py:171-176, mark_dirty only); delete_asset's remove_asset_from_project re-reads project.json FROM DISK (old order), saves it, then force_project_refresh (asset_operations.py:156) calls load_project unconditionally, replacing current_project_data/assets_cache with disk state and clearing is_dirty_flag (project_manager.py:278) — so the unsaved reorder is reverted and the close-prompt guard defeated. No dirty check or pre-save exists anywhere in force_project_refresh.

- [ ] **M61 — `widgets/enhanced_properties_panel.py:926` — Object property edits in the properties panel are silently discarded when no object editor is open — while the preview updates as if they worked**
  - *Found by:* widgets-assets; *category:* correctness
  - Clicking an object in the asset tree (single click, no editor open) routes through ide_window -> set_asset -> show_asset_properties -> show_object_properties, which builds fully editable controls (sprite combo, Visible/Solid/Persistent checkboxes) wired to on_object_property_changed. But on_object_property_changed only forwards the change to `self.current_object_editor`, which is None in this path, so the edit goes nowhere — it is never written to project data, the cache, or disk. Worse, for sprite changes the handler FIRST updates the preview image (lines 916-923, using a local copy of the data), so the panel visibly confirms a change that was dropped. There is no fallback write path and no read-only indication; the docstring even says 'Show object properties with live editing'.
  - **Failure:** A student selects obj_coin in the asset tree, picks sprite 'spr_coin' in the panel's Sprite combo, and sees the preview switch to the coin image. They run the game: the object still has no sprite. The edit (same for Visible/Solid/Persistent toggles) was silently thrown away because no object editor tab was open.
  - **Suggested fix:** When current_object_editor is None, write the change through to the project data/asset_manager cache (and mark dirty), or render the rows with the same read-only _readonly_value style used for rooms/sprites so the panel doesn't present dead controls.
  - **Verifier:** Traced fully: tree single-click → on_item_clicked emits asset dict → set_asset → show_asset_properties → show_object_properties builds live-editable sprite combo + Visible/Solid/Persistent checkboxes wired to on_object_property_changed (enhanced_properties_panel.py:728-738, 766-772). That handler (lines 908-932) updates the preview from a local copy for sprite changes, then forwards ONLY to self.current_object_editor — which is None unless set_object_editor_context ran (i.e., an object editor tab is open). No fallback write to cache/project data exists, so the edit is silently dropped while the preview confirms it.


## Low (35)

- [ ] **L1 — `config/editions.py:12` — Committee edition shows a completely empty tutorial list (no placeholder) in every IDE language except English and French**
  - *Found by:* widgets-misc-config-main; *category:* correctness
  - The 'committee' edition gates tutorials to the single folder '09_catch_the_coins'. Tutorial widgets resolve a localized tutorials path first (widgets/_tutorial_paths.py), and the localized Tutorials/<lang>/index.json files for de, es, it, ru, sl and uk list only folders 01-08 — '09_catch_the_coins' is absent (only the root/English and fr indexes include it; verified by inspecting all index.json files). filter_tutorials_for_edition therefore returns an empty list. Compounding it, both consumers (TutorialPanel.load_tutorial_list at widgets/tutorial_panel.py:170-174 and TutorialDialog.load_tutorial_list at widgets/tutorial_dialog.py:135-140) `return` immediately after iterating the (empty) filtered index list, so the 'No tutorials available' placeholder — which only exists after the fallback folder-scan path — is never added. The user gets a wordless empty list widget with a permanently disabled Open button.
  - **Failure:** A teacher sets the IDE language to German (or es/it/ru/sl/uk) and switches to the 'Committee Demo' edition to prepare a demo. Opening Tutorials (footer link or panel) shows an entirely blank list: no tutorial, no explanatory message, Open button disabled — the curated 'Catch the Coins' tutorial the edition exists to showcase is unreachable until the language is switched back to English or French.
  - **Suggested fix:** Add the 09_catch_the_coins entry (and folder) to the localized indexes, or make localized_tutorials_path / filter_tutorials_for_edition fall back to the base English index when the edition filter yields zero tutorials from a localized index; additionally, move the count==0 'No tutorials available' placeholder in both load_tutorial_list implementations so it also covers the index.json branch.
  - **Verifier:** Verified all localized index.json files: de/es/it/ru/sl/uk list only folders 01-08, so filter_tutorials_for_edition with committee's ['09_catch_the_coins'] returns []. Both tutorial_panel.py (return at :174) and tutorial_dialog.py return immediately after the index loop, so the 'No tutorials available' placeholder (only on the folder-scan fallback path) is never shown. Low: affects only the committee demo edition in non-en/fr languages.

- [ ] **L2 — `core/ide_window.py:622` — Package import reloads the project from disk without saving or prompting, silently discarding unsaved in-memory changes**
  - *Found by:* core-managers-utils; *category:* data-integrity
  - ResourcePackager.import_object/import_room mutate the on-disk project.json (read-modify-write of the stale disk copy, also via plain json.dump with no atomic write), and the IDE then calls `self.load_project(self.current_project_path)` (ide_window.py:622 and :663; asset_tree_widget.force_project_refresh does the same) to pick up the new asset. Any dirty in-memory changes made since the last save/auto-save (auto-save runs every 30s, and can be disabled in settings) are not flushed first and are silently replaced by the disk state — no prompt, no warning. With auto-save disabled the window is the whole editing session.
  - **Failure:** A teacher with auto-save turned off edits several object events, then uses Assets > Import Object Package to add a shared object. The import succeeds, the project reloads from disk, and all event edits made since the last manual save vanish without any prompt.
  - **Suggested fix:** Before running ResourcePackager.import_* on the open project, call project_manager.save_project() if dirty (or prompt to save), so the disk copy the importer modifies and the post-import reload both reflect the user's latest state. Also route the importer's project.json write through an atomic tmp+os.replace.
  - **Verifier:** ResourcePackager.import_object/import_room read-modify-write the on-disk project.json (resource_packager.py:225/270, 317/381), then ide_window.py:622/:663 call load_project, which has no dirty check or prompt, and on_project_loaded (lines 3838-3846) deleteLater()s every open editor and clears open_editors, discarding any in-memory edits since the last save. No guard exists anywhere on this path.

- [ ] **L3 — `core/ide_window.py:1910` — test_game leaks the stderr capture temp file and open fd when Popen raises**
  - *Found by:* core-ide-window; *category:* resource-leak
  - test_game creates the stderr temp file and an open file handle (tempfile.mkstemp + os.fdopen) before calling subprocess.Popen. If Popen raises (FileNotFoundError when the interpreter or runtime/run_game.py is missing/moved — exactly the packaged-edge cases the surrounding detection code worries about), control jumps to the except block at line 1933, which shows the error dialog but never closes _game_stderr_handle nor unlinks _game_stderr_path. Each failed F5 leaves another open fd and an orphan /tmp/pygm2_game_*.log; the attributes are overwritten on the next attempt so _drain_game_stderr can never clean the old ones.
  - **Failure:** On a misconfigured install where run_game.py launch fails, a student repeatedly presses F5; every attempt leaks one fd and one temp file for the IDE process lifetime. Long sessions accumulate stale pygm2_game_*.log files that are never deleted.
  - **Suggested fix:** Wrap the Popen call so the capture is cleaned on failure, e.g. `try: process = subprocess.Popen(...) except Exception: self._drain_game_stderr(None); raise` (the existing _drain_game_stderr already closes the handle and removes the file and is safe to call here).
  - **Verifier:** tempfile.mkstemp + os.fdopen (ide_window.py:1910-1912) precede subprocess.Popen (1914), and the except block at 1933 shows a dialog without closing the handle or unlinking the file; _drain_game_stderr is only called from _check_game_process/stop_game, and the next test_game overwrites _game_stderr_path/_handle, orphaning the previous log file permanently (CPython GC closes the old fd on overwrite, slightly softening the fd-accumulation claim, but the temp-file leak is real). Reachable in unpackaged installs (e.g., deleted project cwd or PermissionError) since the is_packaged check only excludes the missing-interpreter case.

- [ ] **L4 — `core/ide_window.py:2827` — Thymio Playground and Playground Runner windows leak on every open: no WA_DeleteOnClose, parented to a long-lived widget, reference overwritten per call**
  - *Found by:* cross-lifecycle; *category:* resource-leak
  - show_thymio_playground() executes `self.thymio_playground = ThymioPlaygroundWindow(self)` on every menu invocation. ThymioPlaygroundWindow (widgets/thymio_playground.py) never sets Qt.WA_DeleteOnClose, so closing the window only hides it; being parented to the IDE keeps the C++ object alive, and the next open overwrites the only Python reference with a brand-new instance. Each instance holds pygame Surfaces (full playground world surface), a ThymioSimulator, renderer, and an undo stack — all retained until the IDE itself closes. The same pattern exists in editors/playground_editor/__init__.py:592 (`self._runner_window = PlaygroundRunnerWindow(data, project_data, self)` per Run click, runtime/playground_runner.py has no WA_DeleteOnClose either). Additionally, opening the playground twice yields two independent simulator windows each ticking a 60 FPS QTimer simultaneously, which is clearly not intended for a singleton-named attribute.
  - **Failure:** A student exploring the IDE repeatedly opens and closes the Thymio Playground (or clicks Run in the playground editor many times during an iterate-test loop). Each cycle permanently retains a full simulator window (pygame surfaces + widgets) in memory; over a long lab session memory usage climbs steadily and multiple stale hidden windows accumulate under the IDE. Opening the menu item twice without closing also produces two live simulations running at 60 FPS each.
  - **Suggested fix:** In both window classes call `self.setAttribute(Qt.WA_DeleteOnClose)` (their closeEvent already stops the timers), and in show_thymio_playground() reuse/raise an existing live window instead of constructing a new one (mirror the _focus_detached_editor pattern used for editors).
  - **Verifier:** Confirmed: show_thymio_playground (ide_window.py:2827) creates a new ThymioPlaygroundWindow(self) per invocation; the window is a QMainWindow parented to the IDE (thymio_playground.py:171-172) with no WA_DeleteOnClose anywhere in the file, so closing only hides it and each reopen overwrites the sole reference, accumulating retained windows/surfaces until IDE exit; same pattern in PlaygroundRunnerWindow (playground_runner.py:139-140, editors/playground_editor/__init__.py:592). Mitigating: both closeEvents stop the 60 FPS timer (thymio_playground.py:1304-1309, playground_runner.py:478-480), so closed instances consume memory but no CPU — low severity stands.

- [ ] **L5 — `core/ide_window.py:3345` — open_editors is keyed by bare asset name across asset types — a room and an object with the same name collide**
  - *Found by:* cross-state-sync; *category:* correctness
  - Every open_*_editor method guards with `if <name> in self.open_editors:` and stores `self.open_editors[<name>] = editor` (rooms 3248/3278, objects 3345/3375, sprites 3410/3431, scripts 3458/3479, playgrounds 3297/3326), with no asset-type component in the key. Asset-name uniqueness is only enforced per category (get_unique_asset_name / asset_name_exists check one category), so a room and an object may legally share a name. With both names equal: opening the second asset finds the first's entry, focuses the wrong editor (or its tab via tabText match), and returns — the second asset can never be opened while the first is open. The same name-only keying corrupts the other flows that index open_editors: close_editor_by_name, on_asset_deleted (deleting object 'X' closes the room editor 'X'), AssetOperations.delete_asset's close-first step, on_asset_renamed's editor re-keying, and detached_editor_windows.
  - **Failure:** A student names both their object and their room 'niveau1' (no obj_/rm_ prefix discipline). With the room 'niveau1' open, double-clicking the object 'niveau1' in the asset tree just re-focuses the room tab — the object editor never opens, with no error. Conversely, deleting the object 'niveau1' from the tree closes the open ROOM editor of the same name.
  - **Suggested fix:** Key open_editors and detached_editor_windows by a composite key such as (asset_category, asset_name) (or f"{category}/{name}"), and thread the category through close_editor_by_name / on_asset_deleted / on_asset_renamed / _focus_detached_editor.
  - **Verifier:** Confirmed: open_editors is keyed by bare asset name across all five editor types (e.g. 3278, 3375, 3431), name uniqueness is enforced only within one category (asset_manager.py asset_exists/get_unique_asset_name 703-716, rename check 408-419), and on_asset_deleted's objects branch (1643-1645) closes by name regardless of type — so a same-named room and object collide exactly as described. Downgraded to low because it requires the student to give a room and an object the identical name, an edge case rather than typical classroom use.

- [ ] **L6 — `core/project_manager.py:409` — Sprite-file merge whitelist omits 'precise', 'frame_width', 'frame_height', 'animation_type', 'speed' — edits to sprites/<name>.json are silently ignored and reverted on save**
  - *Found by:* cross-data-integrity; *category:* data-integrity
  - Both sprite-file loaders (core/project_manager.py:409-413 and the runtime copy at runtime/game_runner.py:1956-1960) merge sprites/<name>.json into the project.json entry with a fixed whitelist ('frames', 'width', 'height', 'origin_x', 'origin_y', 'collision_mask', 'bbox_*', 'imported', 'created', 'modified', 'file_path') that omits five keys every sprite file actually contains: 'precise', 'frame_width', 'frame_height', 'animation_type', 'speed' (see samples/maze_1/sprites/spr_person.json). The merge kernel's own contract is 'file takes precedence' and its docstring warns that a key present in the file but missing from the list means 'projects would silently lose data on load'. In the normal IDE flow the embedded project.json copy matches the file so nothing is visibly lost — but whenever the sprite FILE is the newer copy (hand-edit, external tool, partially-synced share), those five fields are ignored at load, and the next project save rewrites sprites/<name>.json from the stale in-memory data (_save_sprites_to_files, project_manager.py:706), reverting the user's edit on disk.
  - **Failure:** A teacher slows a too-fast animation by editing "speed": 10.0 → 4.0 in sprites/spr_monster.json (the file that visibly owns that value), or toggles "precise": true there to enable pixel-perfect collision. Reopening the project and running the game shows no change (the stale project.json copy wins), and the next Ctrl+S silently rewrites the sprite file back to the old values, erasing the edit.
  - **Suggested fix:** Add 'precise', 'frame_width', 'frame_height', 'animation_type', 'speed' to both sprite-merge whitelists (better: single-source the sprite key list in utils/project_file_merge.py next to _ROOM_FILE_KEYS/_OBJECT_FILE_KEYS, and include 'remember_destroyed' in _OBJECT_FILE_KEYS for the same reason).
  - **Verifier:** Both merge whitelists (core/project_manager.py:409-413 and runtime/game_runner.py:1956-1960) omit 'precise', 'frame_width', 'frame_height', 'animation_type', 'speed', all of which sprite files demonstrably contain (samples/maze_1/sprites/spr_person.json), despite the 'file takes precedence' comment. _save_sprites_to_files (project_manager.py:706) then rewrites the sprite file wholesale from the stale in-memory copy, so a hand edit to sprites/<name>.json for those keys is ignored at load and reverted on the next save. Low: only manifests when the sprite file diverges from project.json (hand edit / external tool); normal IDE flows keep them in sync.

- [ ] **L7 — `core/project_manager.py:1451` — Zip temp extraction directories are never cleaned up (close_project has no callers; closeEvent doesn't clean either) — full project copies accumulate in TEMP**
  - *Found by:* cross-lifecycle; *category:* resource-leak
  - load_project_from_zip() extracts the whole project (all sprites, sounds, rooms) to a mkdtemp directory (`ProjectCompressor.create_temporary_extraction`, utils/project_compression.py:135) and stores it in self._temp_extraction_dir. The only cleanup paths are close_project() — which is never called from anywhere (only its definition exists) — and the load-failure branch. Consequences: (1) opening a second zip overwrites _temp_extraction_dir (line 1451), permanently orphaning the previous extraction; (2) quitting the IDE (PyGameMakerIDE.closeEvent, core/ide_window.py:4129) never removes the extraction of the currently open zip project either. On Windows, %TEMP% is not auto-purged, so every zip-open session leaks a complete copy of the project (images/sounds can be many MB) forever.
  - **Failure:** A teacher who routinely opens student-submitted .zip projects (the documented workflow) accumulates one full extracted project copy under %TEMP%\pygamemaker_* per zip opened, across every session, with no mechanism ever deleting them. Over a school year this silently consumes hundreds of MB to GBs of disk, and the stale copies are also what finding #1 recompresses over the original zips.
  - **Suggested fix:** Clean the previous _temp_extraction_dir (shutil.rmtree(..., ignore_errors=True)) at the start of load_project_from_zip() and when any other project is loaded/created; call project_manager.close_project() from PyGameMakerIDE.closeEvent. Optionally sweep leftover 'pygamemaker_*' dirs in tempfile.gettempdir() at startup.
  - **Verifier:** create_temporary_extraction uses bare tempfile.mkdtemp (utils/project_compression.py:135) with no atexit/TemporaryDirectory; cleanup exists only in close_project (project_manager.py:829-831, zero callers) and the load-failure branch (:1459). load_project_from_zip (:1451) overwrites _temp_extraction_dir without removing the previous extraction, and neither PyGameMakerIDE.closeEvent (ide_window.py:4129-4164) nor main.py's aboutToQuit hook (only saves config) ever cleans it — my repro itself left a /tmp/pygamemaker_* dir behind. Confirmed leak, but downgraded to low: the consequence is gradual disk consumption only (Linux /tmp purges on reboot; Windows %TEMP% accumulates), no crash or data loss — the stale-recompression harm is finding 20's.

- [ ] **L8 — `dialogs/project_dialogs.py:129` — New Project dialog's Description field is silently discarded — never written to the created project**
  - *Found by:* dialogs; *category:* correctness
  - NewProjectDialog.accept_project() puts the description into self.project_data["description"], but the only consumer (core/ide_window.py:1227-1231) passes just name, path and template to project_manager.create_project(); create_new_project() builds the project.json from _create_default_project_data(project_name) which contains no description, and the dialog's value is dropped. The ProjectSettingsDialog later loads/edits 'description' from project data, so the field is clearly meant to be persisted.
  - **Failure:** A student creates a new project and fills in the 'Optional project description...' box. The project is created, but the description is nowhere in project.json; opening Project > Settings shows an empty Description field. The text is silently lost.
  - **Suggested fix:** Have new_project() in core/ide_window.py write the description into the created project's data (e.g., project_manager.current_project_data['description'] = project_info['description'] followed by mark_dirty()/save), or extend create_project()/create_new_project() to accept and store it.
  - **Verifier:** NewProjectDialog.accept_project stores 'description' (dialogs/project_dialogs.py:129) but the sole consumer, new_project (core/ide_window.py:1227-1231), passes only name/path/template to create_project, whose signature (project_manager.py:146) has no description parameter; create_new_project builds project.json from _create_default_project_data(project_name), which contains no description key (grep confirms 'description' never appears in project_manager writes). ProjectSettingsDialog reads/writes project_data['description'] (:233/:265), so the field is meant to persist but the dialog value is silently dropped. Distinct from the audit doc's #30 (OpenProjectDialog dead code). Low: cosmetic loss of an optional metadata field.

- [ ] **L9 — `dialogs/project_dialogs.py:595` — Export Options checkboxes (Include Assets / Optimize / Include Debug Info) are collected but ignored by every export path; the EXE path actively overrides them with hardcoded values**
  - *Found by:* dialogs; *category:* correctness
  - accept_export() stores the three option checkboxes into self.export_settings (lines 416-422), but no export routine uses them: _export_kivy() calls export_with_adapter(project_manager, output_path) with no settings; _export_html5() and _export_zip() likewise ignore them; and _export_executable() builds a brand-new hardcoded dict {'include_assets': True, 'include_debug': False, 'optimize': True} and passes that to ExeExporter — which genuinely consumes those flags (export/exe/exe_exporter.py:87 filters debug log output, :313/:319 set PyInstaller debug=/console=, :316 sets upx=). So checking 'Include Debug Info' or unchecking 'Optimize for Release' has zero effect.
  - **Failure:** A user's exported Windows game crashes on startup. They re-export with 'Include Debug Info' checked, expecting a console window with the traceback (exactly what ExeExporter's console= flag provides). The exe is built with console=False anyway and still dies silently; the user has no way to get the debug build the UI offers.
  - **Suggested fix:** In _export_executable(), pass the user's choices: export_settings = {'include_assets': self.include_assets_check.isChecked(), 'include_debug': self.include_debug_check.isChecked(), 'optimize': self.optimize_check.isChecked()} (or reuse self.export_settings populated in accept_export). Either honor or remove the options for the HTML5/Kivy/zip paths.
  - **Verifier:** accept_export stores the checkbox choices in self.export_settings (project_dialogs.py:416-422), but _export_kivy/_export_html5/_export_zip never read them and _export_executable builds a hardcoded {'include_assets': True, 'include_debug': False, 'optimize': True} dict (595-599) passed to ExeExporter, which genuinely consumes include_debug/optimize (exe_exporter.py:87, 313, 316, 319). The UI options have zero effect.

- [ ] **L10 — `editors/object_editor/python_code_parser.py:165` — String parameters are interpolated into double-quoted code templates without escaping — a quote or newline in a message/sound/sprite/room name produces syntactically invalid generated code**
  - *Found by:* object-editor; *category:* correctness
  - Templates like 'display_message': 'game.show_message("{message}")', 'play_sound': 'game.play_sound("{sound}")', 'set_sprite': 'self.sprite_index = "{sprite}"', 'goto_room': 'game.goto_room("{room}")' and 'create_instance' use plain str.format with no escaping. A message containing a double quote, backslash, or newline yields invalid Python. Verified empirically: display_message with message 'He said "hi"' generates game.show_message("He said "hi"") and ast.parse raises SyntaxError. The whole generated class becomes unparseable, so View Generated Code shows broken code and Edit Custom Code mode can never auto-apply for that object (perpetual 'Parse error' pill) until the user hand-fixes a line the tool wrote.
  - **Failure:** A teacher creates a Show Message action with the text: Press "SPACE" to jump. The events panel and the game work, but the Code Editor tab shows invalid Python; switching to Edit Custom Code to add a tweak elsewhere blocks all auto-applies with a parse error caused by the tool's own output. Multi-line messages (typed in the message dialog) hit the same wall.
  - **Suggested fix:** Render string parameters with repr()/json.dumps in _generate_action_code (e.g. f'game.show_message({json.dumps(message)})') instead of embedding raw text inside literal double quotes in the templates.
  - **Verifier:** Reproduced: display_message with 'He said "hi"' generates game.show_message("He said "hi"") and ast.parse raises SyntaxError; no escaping exists anywhere in the template path (python_code_parser.py:165-166, 162, 153, 129). Impact is confined to the code tab (view broken, edit-mode auto-apply blocked); the events panel and runtime still work, so low is right.

- [ ] **L11 — `editors/playground_editor/__init__.py:489` — Playground properties panel shows stale values after Undo/Redo of move or property changes**
  - *Found by:* other-editors; *category:* correctness
  - PlaygroundEditor.undo()/redo() run the canvas undo stack and mark_modified(), but never refresh the PlaygroundElementProperties panel; MoveElementCommand and ModifyElementCommand likewise only schedule canvas.update() and emit nothing. So after undoing a move or a spinbox property change on the selected element, the canvas shows the reverted value while the panel still displays the pre-undo value. The next spinbox interaction then emits a property_changed based on the stale displayed value (e.g. arrow-up from the stale 200.0 while the element is back at 100.0 jumps the element to 200.1).
  - **Failure:** A student selects a wall, changes X from 100 to 200 in the properties spinbox, then presses Undo. The wall jumps back to x=100 on canvas, but the panel still reads 200. Nudging the X spinbox up by one tick teleports the wall to 200.1 instead of 101.
  - **Suggested fix:** After undo()/redo() in PlaygroundEditor, re-sync the panel: `if self.canvas.selected_elements: self.element_properties.set_element(self.canvas.selected_elements[0])` (or have Move/ModifyElementCommand emit canvas.element_selected/element_moved for the affected element when it is selected).
  - **Verifier:** PlaygroundEditor.undo()/redo() (__init__.py:489-497) only run the stack and mark_modified(); ModifyElementCommand/MoveElementCommand only QTimer canvas.update() and emit no signal, so element_properties is never refreshed. The spinbox then steps from the stale displayed value and _on_property_changed pushes a command with old=getattr(element,prop) (reverted) and new=stale+step, producing the described jump.

- [ ] **L12 — `editors/room_editor/room_canvas.py:398` — Instance hit-testing and selection highlight ignore scale (and rotation): scaled instances are unclickable over most of their visible area**
  - *Found by:* room-sprite-editors; *category:* correctness
  - draw_instance renders a transformed instance over the footprint (draw_x, draw_y, width*scale_x, height*scale_y) — it translates to center_x = draw_x + (width*scale_x)/2 and applies painter.scale(). But find_instance_at, find_instances_in_rect (line 411), and draw_selection (line 747) all build the rect as QRect(instance.x - ox, instance.y - oy, width, height) using the cached unscaled _sprite_width/_sprite_height. For scale_x=scale_y=2.0 (settable to 1000% in the InstanceProperties panel), the clickable region covers only the top-left quarter of the drawn sprite, and the red selection box outlines the wrong area. Rotated instances are similarly mismatched.
  - **Failure:** A student sets an instance's scale to 200% in the properties panel, then clicks on the (visibly large) sprite's right half to select or drag it — nothing happens, or a different instance underneath is selected. The selection rectangle, when it does appear, covers only part of the sprite, and rubber-band selection misses scaled instances whose unscaled rect lies outside the band.
  - **Suggested fix:** Use the scaled footprint for hit tests and highlight: QRect(instance.x - ox, instance.y - oy, round(width*scale_x), round(height*scale_y)) (and store the scaled size in _sprite_width/_sprite_height during draw_instance, since draw_x/draw_y already match the transformed top-left).
  - **Verifier:** draw_instance applies painter.scale() so the drawn footprint spans width*scale_x/height*scale_y (room_canvas.py:708-724), while find_instance_at, find_instances_in_rect, and draw_selection all build QRect(instance.x-ox, instance.y-oy, width, height) from the unscaled cached dimensions; scale is user-settable 10-1000% in instance_properties.py. Low because the unscaled rect is the top-left portion of the drawn sprite, so scaled instances remain selectable there — mismatch, not lockout.

- [ ] **L13 — `editors/room_editor/room_canvas.py:519` — Duplicate (Ctrl+D) silently overwrites the copy/paste clipboard**
  - *Found by:* room-sprite-editors; *category:* correctness
  - duplicate_selected_instances stores the selection into self.clipboard_instances as its working buffer, clobbering whatever the user previously copied with Ctrl+C/Ctrl+X. Duplicate is a self-contained operation and should not touch the clipboard (paste_instances reads clipboard_instances).
  - **Failure:** A student copies a carefully-configured instance group with Ctrl+C, then duplicates an unrelated wall with Ctrl+D to extend a corridor, then presses Ctrl+V expecting the copied group — the wall is pasted instead, and the copied group is gone from the clipboard.
  - **Suggested fix:** Use a local list in duplicate_selected_instances (`source = [inst.to_dict() for inst in self.selected_instances]`) instead of assigning to self.clipboard_instances.
  - **Verifier:** duplicate_selected_instances (room_canvas.py:519) assigns self.clipboard_instances as its scratch buffer — the same attribute copy/cut write and paste_instances reads — so Ctrl+D silently replaces the user's copied selection. Minor UX/correctness bug; recoverable by re-copying.

- [ ] **L14 — `editors/script_editor.py:31` — Script editor Undo/Redo toolbar buttons and Edit-menu Undo are permanently dead despite available document undo**
  - *Found by:* other-editors; *category:* correctness
  - ScriptEditor inherits BaseEditor's undo()/redo(), which operate on self.undo_stack — but ScriptEditor never pushes any QUndoCommand; all editing happens inside the QPlainTextEdit, whose QTextDocument keeps its own (working) undo history. Consequently the toolbar '↶ Undo'/'↷ Redo' buttons stay disabled forever (canUndoChanged never fires), and the IDE Edit > Undo menu item (ide_window.py:2589-2599 delegates to editor.undo()) is a silent no-op while typing changes are in fact undoable. Only the raw Ctrl+Z keypress works, because QPlainTextEdit accepts the ShortcutOverride and handles it natively — so the same operation works from the keyboard but not from the two visible UI affordances.
  - **Failure:** A student types a bad edit into a script, clicks Edit > Undo (or looks at the editor toolbar's Undo button) — the menu does nothing and the button is greyed out, even though the change is undoable. They conclude undo is broken and retype the code.
  - **Suggested fix:** Override undo()/redo() in ScriptEditor to call self.code_edit.undo()/self.code_edit.redo(), and connect self.code_edit.document().undoAvailable/redoAvailable to self.undo_action.setEnabled/self.redo_action.setEnabled.
  - **Verifier:** ScriptEditor defines no undo/redo override and never pushes to BaseEditor.undo_stack; the toolbar actions (base_editor.py:138/141) and BaseEditor.undo/redo (lines 393-401, gated on undo_stack.canUndo()) are therefore permanently dead, and ide_window.py undo() delegates to that same dead path. The QPlainTextEdit's own document undo still works via keyboard, so impact is limited to the two visible UI affordances — low.

- [ ] **L15 — `editors/sprite_editor/sprite_canvas.py:199` — _screen_to_pixel truncates toward zero instead of flooring: clicks in the margin just left/above the image paint the edge pixels**
  - *Found by:* room-sprite-editors; *category:* correctness
  - int() truncates toward zero, so a screen position up to one zoomed pixel left of (or above) the image maps to pixel coordinate 0 instead of -1. At zoom 10, clicking 9 screen pixels left of the image edge gives (screen-ox) = -9, -9/10 = -0.9, int() -> 0 — an in-bounds coordinate. _set_pixel's bounds check then happily paints column 0 (or row 0). The fill tool similarly flood-fills from the edge pixel, and the brush hover cursor snaps onto the image when the mouse is actually outside it.
  - **Failure:** A student working zoomed-in clicks in the grey margin just outside the sprite's left edge (e.g. to defocus or by overshoot) with the pencil active: a pixel is drawn on the leftmost column. With the fill tool, an outside-the-image click floods the sprite from its edge pixel.
  - **Suggested fix:** Use floor division: `x = (screen_pos.x() - ox) // self._zoom` (Python's // floors for negatives), so out-of-image positions map to negative coordinates that the bounds checks reject.
  - **Verifier:** sprite_canvas.py:199 uses int() which truncates toward zero, so screen positions within one zoom-pixel left/above the image map to coordinate 0; mousePressEvent has no bounds check and _set_pixel/FillTool.on_press treat 0 as in-bounds, so the edge column/row gets painted or flood-filled. Real but a narrow (≤1 zoomed pixel) misclick band — low.

- [ ] **L16 — `editors/sprite_editor/sprite_canvas.py:409` — canvas_modified emitted for non-modifying gestures: selection marquees and picker clicks push no-op undo commands and falsely dirty the sprite**
  - *Found by:* room-sprite-editors; *category:* correctness
  - mouseReleaseEvent unconditionally emits canvas_modified for every left-button gesture (`self.canvas_modified.emit()`), and mousePressEvent unconditionally takes a stroke snapshot. _on_canvas_modified (sprite_editor_main.py:600-613) sees the snapshot and pushes a SpriteEditCommand even when old == new — true for a SelectTool marquee drag (only sets selection_rect, no pixels change) and for a ColorPickerTool click. It also emits data_modified each time. Result: every selection rectangle the user drags adds one inert 'Draw' entry to the undo stack and marks the asset as unsaved.
  - **Failure:** A student draws something, then drags five selection rectangles trying to frame a region. Pressing Ctrl+Z now appears dead five times in a row before the real drawing operation reverts, and the editor shows the sprite as modified (close prompts 'save changes?') even though no pixel changed.
  - **Suggested fix:** Skip pushing the command when the snapshot equals the current image (QImage supports operator==: `if snapshot and snapshot != self.canvas.get_image():`), or only emit canvas_modified from tools that actually mutated the image.
  - **Verifier:** sprite_canvas.py mousePressEvent always calls take_stroke_snapshot and mouseReleaseEvent (line 409) unconditionally emits canvas_modified; _on_canvas_modified (sprite_editor_main.py:600-613) pushes a SpriteEditCommand with no old==new comparison and emits data_modified, so SelectTool marquees and picker clicks add inert 'Draw' undo entries and falsely dirty the sprite. Annoyance-level: undo appears dead for a few presses and a spurious save prompt — low.

- [ ] **L17 — `editors/sprite_editor/sprite_frames.py:305` — Drawing while animation playback is running swaps the canvas image mid-stroke, losing strokes and corrupting undo**
  - *Found by:* room-sprite-editors; *category:* concurrency
  - During playback, _anim_next_frame -> set_current_index -> frame_selected -> SpriteEditor._on_frame_selected -> canvas.set_image() replaces canvas._image while the user may be mid-stroke (canvas._painting is True; nothing pauses the timer when the canvas gets a mouse press — playback only stops via the Play button or clicking a thumbnail). The partial stroke painted into the old QImage is discarded (it's synced to the timeline only at release via canvas_modified). At release, the SpriteEditCommand is built from a snapshot of the frame where the stroke STARTED (old) and the currently displayed frame's image (new), with _frame_index = the current (different) frame — so undo stamps frame A's pre-stroke image over frame B, and update_current_frame writes mixed content into frame B.
  - **Failure:** A student presses Play to preview their 4-frame animation and doodles on the canvas while it loops: strokes intermittently vanish as frames advance under the cursor, and pressing Ctrl+Z afterwards replaces one frame's artwork with a stale copy of a different frame.
  - **Suggested fix:** Stop playback (or ignore the timer tick) while canvas._painting is True — e.g. call stop_playback() from SpriteCanvas.mousePressEvent via a signal, mirroring what _on_thumb_clicked already does.
  - **Verifier:** Nothing pauses the animation timer on canvas interaction (stop_playback is only wired to the Play button and thumbnail clicks), and _on_frame_selected calls canvas.set_image() unconditionally even while canvas._painting is True (sprite_canvas.py:376-409 sets _painting with no guard in set_image). At release, SpriteEditCommand is built from the pre-stroke snapshot of the frame where the stroke started but stamps it onto _frame_index = the currently displayed frame (sprite_editor_main.py:39-53, 600-613), so strokes vanish and undo writes one frame's old content over another. Low: only triggers when drawing during playback.

- [ ] **L18 — `events/action_editor.py:134` — check_empty's 'objects' dropdown offers every project object name, but the runtime collapses anything that isn't 'solid' to 'all'**
  - *Found by:* events-actions-plugins; *category:* correctness
  - ActionConfigDialog routes any parameter whose name contains 'object' through the object-selector branch (line 134: `elif param.name == "object_name" or "object" in param.name.lower()`), which prepends param.choices and then appends all project object names into an editable combo. check_empty's parameter is named 'objects' (a choice param with choices ['solid','all'], action_types.py:704-711), so the dialog also lists every object in the project as if checking emptiness against a specific object were supported. The runtime explicitly supports only two values: execute_check_empty_action maps `object_type = "solid" if objects == "solid" else "all"` (runtime/action_executor.py:1447), so a specific object name like 'obj_monstre' behaves as 'all' — every instance blocks the cell.
  - **Failure:** A student configures Check Empty with Objects = 'obj_wall' (offered in the dropdown), expecting only walls to count as occupying the cell. At runtime the value falls into the 'all' branch, so a coin or the player itself also makes the cell 'occupied', and their movement/push logic blocks in places it shouldn't — with no indication that the chosen object name was ignored.
  - **Suggested fix:** Exempt pure choice params from the substring-based object branch (e.g. only enter it when param.param_type == 'object'), so check_empty's dropdown shows exactly ['solid', 'all'] — or extend execute_check_empty_action to pass a specific object name through to check_collision_at_position, which already accepts object-name filters.
  - **Verifier:** check_empty's param is named 'objects' (action_types.py:704-711), which matches the '"object" in param.name.lower()' branch at action_editor.py:134, so the editable combo appends every project object name after the legitimate ['solid','all'] choices. The runtime collapses any non-'solid' value to 'all' (action_executor.py:1448: object_type = "solid" if objects == "solid" else "all"), so a specific object name silently behaves as 'all'.

- [ ] **L19 — `events/action_editor.py:787` — MultiActionEditor and ConditionalActionEditor hide unknown action types from the list but keep them in the data, so row indexes misalign and Remove/Move operate on the wrong action**
  - *Found by:* events-actions-plugins; *category:* data-integrity
  - MultiActionEditor.refresh_display only adds a tree item `if action_type:` (line 787) — actions whose type isn't in ACTION_TYPES (e.g. 'check_keys_and_move', which the Blockly generator emits (blockly_generators.js:152) and the runtime handles (action_executor.py:1292) but which has no ACTION_TYPES entry; or actions from a plugin that failed to load) are silently omitted from the display while remaining in self.action_list. remove_action (line 859-861), move_up and move_down then map the tree row index directly onto self.action_list indices: `index = self.action_tree.indexOfTopLevelItem(current_item); self.action_list.pop(index)`. With one hidden action at position 0, every visible row is off by one and Remove deletes a different action than the one selected. ConditionalActionEditor has the identical pattern (refresh_action_lists skips unknown types at conditional_editor.py:543-545 while edit_action_in_list/remove_action_from_list index then_actions/else_actions by list row).
  - **Failure:** A student builds an if_condition in Blockly whose DO slot starts with a 'check keys and move' block, then later opens that if_condition in the conditional editor. The then-list shows only the subsequent known actions (the first is invisible). Selecting the displayed first row and clicking '- Remove' pops then_actions[0] — the hidden check_keys_and_move — leaving the action the student wanted to delete in place and silently breaking their grid movement.
  - **Suggested fix:** Render unknown actions too (e.g. '❓ <unknown: name>' with parameters greyed out) so display indexes always equal data indexes — this also stops unknown actions from being invisible/uneditable. Alternatively store the data index in each item's UserRole and use it in remove/move instead of indexOfTopLevelItem.
  - **Verifier:** refresh_display only adds a tree item 'if action_type:' with no placeholder (action_editor.py:783-798) while remove_action/move_up/move_down map the visible row index directly onto self.action_list (:859-886); ConditionalActionEditor has the identical skip-then-index-by-row pattern in refresh_action_lists/remove_action_from_list. The unknown-type state is reachable: Blockly's grid_check_keys_and_move block emits action 'check_keys_and_move' into then_actions (blockly_generators.js:151-152, 202-207), which has no ACTION_TYPES entry or ACTION_TYPE_ALIASES mapping, so get_action_type returns None and the row indexes shift.

- [ ] **L20 — `export/Kivy/buildspec_generator.py:611` — Package-name sanitizer keeps non-ASCII letters — accented project names produce an invalid Android package and the build fails**
  - *Found by:* export-mobile; *category:* correctness
  - _generate_package_name uses `c if c.isalnum() else '_'`, and str.isalnum() is True for accented letters ('é'.isalnum() == True). A project named e.g. 'L'épopée' yields package.name 'l_épopée', giving the Java package 'org.pygamemaker.l_épopée', which aapt/gradle reject — the APK build fails deep into the (15-30 min) buildozer run with a cryptic 'invalid package name' error. Given the project's French educational audience, accented project names are normal use. (The legacy spec writer in kivy_exporter.py:_generate_buildozer_spec is worse — `lower().replace(' ', '')` keeps any punctuation — but that one is only used by the broken adapter path.)
  - **Failure:** A French teacher names the project 'Labyrinthe de Noël' and runs the Android export. After many minutes of building, gradle aborts with an invalid-package error referencing 'labyrinthe_de_noël', with no hint that the project name is the cause.
  - **Suggested fix:** Restrict to ASCII: `''.join(c if ('a' <= c <= 'z' or '0' <= c <= '9') else '_' for c in unicodedata.normalize('NFKD', project_name.lower()).encode('ascii','ignore').decode())`, falling back to 'kivygame' when empty.
  - **Verifier:** buildspec_generator.py:611 uses c.isalnum(), which is True for accented letters, so 'Noel-with-diaeresis' style project names survive into package.name (line 103 of the spec template); android_exporter.py applies no further sanitization (only an accept_sdk_license patch), and Android applicationIds must be ASCII [a-zA-Z0-9_], so aapt/gradle reject the package late in the build. Low: only triggers with non-ASCII project names and fails at build time, not at runtime.

- [ ] **L21 — `export/Kivy/code_generator.py:605` — show_message escaping handles only single quotes — newline or trailing backslash in the message breaks the generated file**
  - *Found by:* export-mobile; *category:* correctness
  - The message is embedded in a single-quoted literal with only `replace("'", "\\'")`. A message containing a real newline gets split across source lines by process_action's multi-line handling (lines 404-409), leaving an unterminated string literal; a message ending in a backslash produces `show_message('...\')` where the backslash escapes the closing quote. Either way the generated object module is a SyntaxError and the exported game fails at import. Backslashes elsewhere also produce stray escape sequences (SyntaxWarning on 3.12+, wrong text).
  - **Failure:** A teacher writes a two-line victory message (or pastes text containing a backslash) in a Show Message action and exports; the exported game crashes at startup with SyntaxError in that object's generated file.
  - **Suggested fix:** Embed the message with `repr(message)` (or json.dumps) instead of manual quote replacement: `return f"from main import show_message; show_message({message!r})"`.
  - **Verifier:** code_generator.py:605 escapes only single quotes; a trailing backslash escapes the closing quote and an embedded newline is split by process_action's multi-line handling (lines 404-409) into an unterminated string literal, both yielding SyntaxError in the generated module. The IDE's string param is a QLineEdit (gm80_action_dialog.py:184) so typed newlines are blocked, but backslashes are typable and GMK-imported messages bypass the widget — genuine but edge-case, low.

- [ ] **L22 — `export/Kivy/kivy_exporter.py:1189` — Background image loader hardcodes '<asset name>.png' while the copied file keeps its original filename/extension**
  - *Found by:* export-mobile; *category:* correctness
  - _generate_scene emits `load_image('assets/images/{bg_image}.png')` where bg_image is the background ASSET NAME from room data, but _export_background copies the file as `src.name` (its original filename). The IDE accepts .jpg/.jpeg/.bmp/.gif/.tga/.webp backgrounds (core/asset_manager.py:26) and allows the asset name to differ from the filename stem. Any non-PNG background, or a renamed background asset, makes the generated path miss; load_image returns None and the exported room silently renders without its background (sprites have the same name-vs-filename pattern but resolve via the sprite's actual file_path, so only backgrounds are affected).
  - **Failure:** A student imports a photo 'fond.jpg' as a room background. The IDE shows it fine; the exported Android/desktop game shows a plain colored room with '[WARN] Image not found: .../assets/images/fond.png' in the log.
  - **Suggested fix:** Resolve the background asset's file_path from project_data (like _generate_object does for sprites) and emit `assets/images/{Path(file_path).name}` instead of appending '.png' to the asset name.
  - **Verifier:** kivy_exporter.py:1189/1214 generate load_image('assets/images/{bg_image}.png') from the asset NAME while _export_background (:177-184) copies the file as src.name with its original extension; asset_manager.py SUPPORTED_FORMATS accepts .jpg/.bmp/etc. for backgrounds, and load_image (:2633-2651) returns None on miss, so any non-PNG or renamed background silently disappears from the exported game.

- [ ] **L23 — `export/android/android_exporter.py:296` — Android/iOS export build directories (mkdtemp) leak on every failed export — cleanup runs only on the full-success path**
  - *Found by:* cross-lifecycle; *category:* resource-leak
  - export_project() creates `build_dir = Path(tempfile.mkdtemp(prefix='pygm_android_'))` (line 379) and only calls `self._cleanup(build_dir)` after ALL steps succeed (and include_debug is off). Every failure return — _generate_kivy_game fails (270), _generate_buildozer_spec fails (276), buildozer build fails (283), no APK found (293) — and the generic `except Exception` (307-312) leave the build directory behind with no cleanup and no user-visible mention of its path (only a logger.info line). A failed buildozer run leaves a build_dir containing the generated Kivy game plus buildozer's working tree, which can be hundreds of MB. ios_exporter.py has the identical structure (mkdtemp at 314, cleanup at ~197 success-only). Buildozer failures are the COMMON case on a machine without the Android toolchain configured, so retry loops multiply the leak.
  - **Failure:** A teacher tries Android export on a freshly set-up machine; buildozer fails (missing SDK/NDK pieces), they fix something and retry 5 times. Each attempt leaves a multi-hundred-MB pygm_android_* directory in /tmp or %TEMP%; on Windows these persist indefinitely and can exhaust disk on lab machines.
  - **Suggested fix:** Wrap steps 4-8 in try/finally and call self._cleanup(build_dir) in the finally (skipping it only when settings['include_debug'] is set, where keeping the tree is the documented intent); same change in ios_exporter.export_project.
  - **Verifier:** Verified: _create_build_directory uses tempfile.mkdtemp (android_exporter.py:380) and the only _cleanup call is at line 298 on the full-success path; there is no finally and none of the failure returns (steps 4-7) or the except block clean up, so every failed Android export leaks a pygm_android_* directory containing the generated game plus buildozer's working tree. Real but a disk-space annoyance, not a crash: low.

- [ ] **L24 — `export/android/android_exporter.py:549` — Cancelling an Android build emits export_complete twice (second message is 'Buildozer build failed: False'); timeout/cancel are dead while buildozer is silent**
  - *Found by:* export-mobile; *category:* concurrency
  - In _run_buildozer's cancel branch the method emits export_complete(False, 'Export cancelled by user.') and returns False. export_project then checks `if result is not True` and emits a second export_complete: 'Buildozer build failed:\n\nFalse' (lines 281-283), so the UI receives two completion signals, the second with a bogus message. Additionally, the advertised 20/40-minute timeout can never fire during the build: `process.wait(timeout=timeout)` (line 584) is only reached after `for line in process.stdout` hits EOF (i.e. after the process already exited), and cancel_requested is only checked when a new output line arrives — a buildozer/gradle hang that produces no output blocks the export thread forever with Cancel having no effect.
  - **Failure:** A teacher clicks Cancel during an APK build: the progress dialog gets a 'cancelled' result immediately followed by an error dialog claiming 'Buildozer build failed: False'. If gradle hangs silently, neither Cancel nor the documented timeout ever takes effect and the export thread is stuck until the IDE is killed.
  - **Suggested fix:** Return a sentinel (e.g. None) from the cancel branch and have export_project skip the second emit for it; read stdout on a helper thread or with a poll loop (process.poll() + non-blocking reads + deadline check) so cancellation and the timeout work without output.
  - **Verifier:** Half-correct: the dead timeout/cancel is real — `for line in process.stdout` blocks until EOF, so process.wait(timeout=) at :584 can never fire during a build and cancel_requested is only checked per output line, so a silent buildozer/gradle hang wedges the export thread with Cancel inert. However the claimed 'Buildozer build failed: False' error dialog never appears: the IDE's export_finished handler (core/ide_window.py:2522) returns early on `_export_cancelled` for both emits, so the double-emit is real but user-invisible.

- [ ] **L25 — `importers/gmk_mappings.py:16` — GMK keyboard events for numpad, punctuation and several special keys map to key names the runtime keymap never produces, so those events can never fire**
  - *Found by:* cross-data-integrity; *category:* correctness
  - GM_VK_TO_KEY_NAME converts GM virtual-key codes to names like 'numpad_0'..'numpad_9', 'numpad_plus', 'semicolon', 'comma', 'period', 'slash', 'quote', 'pause', 'capslock', 'print', 'numlock', 'scrolllock' (gmk_mappings.py:16-78), and the converter stores keyboard/keyboard_press/keyboard_release events under those sub-keys. At runtime, key dispatch goes through pygame_key_name (runtime/_keymap.py), which only produces arrows, a-z, 0-9 and a fixed special_keys table (space/enter/escape/tab/backspace/delete/insert/home/end/pageup/pagedown/f1-f12/shift/control/alt) and returns None for everything else — handle_keyboard_press/release then return early (game_runner.py:2546). So an imported event bound to, e.g., the numpad arrows or ';' exists in the JSON and shows in the editor, but no keypress can ever trigger it, with no warning at import or runtime.
  - **Failure:** A teacher imports a GM8 game whose player uses the numpad keys (common in old GM tutorials) or punctuation shortcuts. The import reports success and the events are visible in the object editor, but at runtime those keys do nothing; the game appears broken with no diagnostic.
  - **Suggested fix:** Extend runtime/_keymap.pygame_key_name to cover the names the importer emits (pygame.K_KP0..K_KP9 → 'numpad_0'.., K_SEMICOLON → 'semicolon', K_COMMA, K_PERIOD, K_SLASH, K_QUOTE, K_PAUSE, K_CAPSLOCK, etc.), or have the GMK importer warn about key bindings outside the runtime-supported set.
  - **Verifier:** GM_VK_TO_KEY_NAME (gmk_mappings.py:46-77) emits names like 'numpad_0'..'numpad_9', 'semicolon', 'comma', 'period', 'slash', 'quote', 'pause', 'capslock', 'numlock', etc., but the runtime pygame_key_name (runtime/_keymap.py) only maps arrows, a-z, 0-9 and a fixed special_keys table (space..f12/shift/control/alt) and returns None otherwise. handle_keyboard_press/release (game_runner.py:2544-2547) early-return on a falsy sub_key, so imported events bound to those keys exist in JSON and the editor but can never fire, with no diagnostic.

- [ ] **L26 — `importers/gmk_parser.py:297` — Attacker-controlled BMP dimensions cause huge allocation and multi-billion-iteration Python loop**
  - *Found by:* importers; *category:* resource-leak
  - _bmp_to_bgra reads width/height from a BMP header (offsets 18/22) that is itself the output of an untrusted zlib block (pre-v800 sprites/backgrounds), then allocates bytearray(width * abs_height * 4) and runs a nested pure-Python loop `for row in range(abs_height): for col in range(width)`. The dimensions are unbounded int32 values; the loop's per-pixel bounds check (src_pixel + channels <= len(pixels)) prevents OOB reads but does nothing to limit iteration count. A tiny compressed BMP whose header claims width=height=46340 yields ~8.6 GB allocation and ~2.1 billion loop iterations.
  - **Failure:** A teacher imports a malicious pre-v800 .gmk with a sprite/background whose embedded BMP header declares enormous dimensions but carries minimal pixel data. The importer hangs for minutes in the pixel loop and/or OOMs on the bytearray, making the IDE unresponsive.
  - **Suggested fix:** Validate width/abs_height against a sane maximum before allocating/looping in _bmp_to_bgra, and reject BMPs whose declared pixel area is far larger than the actual decompressed data length.
  - **Verifier:** gmk_parser.py:280-305 reads width/height straight from the untrusted (zlib-decompressed) BMP header and sizes bytearray(width*abs_height*4) plus a per-pixel Python loop with no dimension cap; the per-pixel bounds check only prevents OOB reads, not iteration count. Mitigations the finder missed: both call sites (lines 753-758, 852-859) wrap _bmp_to_bgra in `except Exception`, so a truly absurd allocation dies as a caught MemoryError and the import continues with a warning — what survives is a minutes-long GUI hang (and possible OOM) for mid-band crafted dimensions. Real but requires a deliberately crafted pre-v800 .gmk and is recoverable, so low rather than medium.

- [ ] **L27 — `importers/roberta_importer.py:597` — Unhandled ValueError crashes Roberta import when an LED RGB color is not a literal**
  - *Found by:* importers; *category:* correctness
  - _extract_color's robColour_rgb branch does int(float(_value_str(color_block, "RED", "0"))) for RED/GREEN/BLUE. _value_str / _resolve_value_block can legitimately return a non-numeric string: a variables_get block returns the variable name, and math_arithmetic returns a parenthesized expression like "(0 + 0)". float() on those raises ValueError, which this branch does not catch (only the robColour_picker hex branch is wrapped in try/except). The exception propagates out of _convert_block → import_roberta_detailed, which has no broad handler; the IDE's import_roberta_xml only catches RobertaImportError, so the ValueError surfaces as an unhandled exception.
  - **Failure:** A student exports an Open Roberta Thymio program where an LED block's color RGB channels are driven by a variable or arithmetic block (not literal numbers). The teacher imports the XML; instead of a clean warning the import aborts with an unhandled ValueError traceback.
  - **Suggested fix:** Wrap the robColour_rgb int(float(...)) conversions in try/except (ValueError, TypeError) and fall back to 0 with a warning, mirroring the robColour_picker branch.
  - **Verifier:** roberta_importer.py:596-600 does int(float(_value_str(...))) for the robColour_rgb branch with no try/except (only the robColour_picker branch is guarded), and _resolve_value_block (lines 527-545) legitimately returns variable names or "(a + b)" expressions, so float() raises ValueError; import_roberta_detailed has no broad handler and ide_window.py:731 catches only RobertaImportError, so the import dies with an unhandled traceback instead of a clean warning. Confirmed, but variable/arithmetic-driven RGB channels are an edge case in classroom use and the failure is a recoverable aborted import — low.

- [ ] **L28 — `runtime/action_executor.py:3102` — create_instance and change_instance look up the create event in unmerged events — an inherited create event never fires**
  - *Found by:* runtime-action-executor; *category:* correctness
  - execute_create_instance_action correctly installs merged (parent-resolved) data on the instance via `new_instance.set_object_data(merged_data)` (line 3085), but then decides whether to run the create event from the raw child data: `events = object_data.get('events', {})` (line 3102) and passes that unmerged dict to `execute_event`/the deferred queue. `_change_single_instance` has the same asymmetry: it sets merged data (line 3201-3202) but reads `events = new_object_data.get('events', {})` (line 3233). resolve_parent_inheritance exists precisely so a child inherits events its ancestors define (game_runner.py:66-119, 'a child inherits any event type its ancestors define and it doesn't'). For a child object whose `create` event lives only on its parent, `'create' in events` is False at both sites, so the inherited create event is silently skipped — while room-placed instances of the same object (built from merged data at game_runner.py:1422) do run it.
  - **Failure:** A project defines `obj_enemy_base` with a create event that sets alarms/hp, and children `obj_enemy_red`/`obj_enemy_blue` that add nothing but a sprite. Enemies placed in the room editor initialize correctly; an enemy spawned at runtime with the 'Create Instance' action (or produced by 'Change Instance' turning one object into an enemy) skips the inherited create event entirely — hp/alarms stay unset, so the spawned enemy behaves differently (e.g. never starts patrolling, dies in one hit).
  - **Suggested fix:** Use the already-computed merged data for the event lookup at both sites: `events = merged_data.get('events', {})` in execute_create_instance_action, and in _change_single_instance resolve `merged_data` once and use it both for set_object_data and the create-event lookup (create_random_instance at line 4820 already does this correctly).
  - **Verifier:** execute_create_instance_action installs merged_data via set_object_data (:3084-3085) but gates/dispatches the create event from raw object_data.get('events') (:3102); _change_single_instance has the identical asymmetry (:3201-3202 vs :3233). resolve_parent_inheritance (game_runner.py:66-119) merges parent events, and room-built instances dispatch create from merged instance.object_data['events'] (game_runner.py:2227, 4042), so an inherited-only create event is silently skipped only for runtime-spawned/changed instances. Low: requires a create event defined solely on an ancestor.

- [ ] **L29 — `runtime/action_executor.py:4617` — Collision context leaks into deferred create events: spawned instance's create event resolves self.hspeed/vspeed/direction to the colliding pair's captured speeds**
  - *Found by:* runtime-action-executor; *category:* correctness
  - execute_collision_event sets `self._collision_other` and `self._collision_speeds` (lines 4597-4601), then runs the action list inside try/finally; the finally block (lines 4609-4615) calls `_process_deferred_create_events()` when depth returns to 0, and only AFTER the finally completes does the cleanup run (lines 4617-4619: `self._collision_other = None; self._collision_speeds = {}` — also not exception-safe since it's outside the finally). Any instance created during a collision event has its create event deferred (`_event_depth > 0`, line 3104-3106), so that create event executes while the stale collision context is still installed. _parse_value then shadows the new instance's own variables: for scope 'self', `if var_name == 'hspeed' and 'self_hspeed' in collision_speeds: return collision_speeds['self_hspeed']` (lines 1856-1859) — returning the COLLIDING instance's captured speed instead of the newly created instance's; same for vspeed, for the derived 'direction' (lines 1862-1870), for bare `hspeed`/`vspeed` tokens in _evaluate_expression (lines 1961-1965), and `other.*` resolves to the collision partner of an event the new instance was never part of.
  - **Failure:** Player (moving at hspeed=4) collides with a monster; the collision handler does create_instance of an 'explosion' or 'pickup' object whose create event uses an expression like set_vspeed value `vspeed - 8` or start_moving_direction with direction_expr `self.direction`. The create event runs deferred inside the collision's finally block, so `vspeed`/`self.direction` evaluate from the player-vs-monster collision_speeds snapshot rather than the new instance's own (zero) speeds — the spawned object inherits the player's motion direction instead of behaving as authored, and the misbehavior appears only when spawned from a collision (the same object spawned from an alarm/step event works).
  - **Suggested fix:** In execute_collision_event, clear the collision context before processing deferred creates: move `self._collision_other = None; self._collision_speeds = {}` into the finally block, placed before the `_process_deferred_create_events()` call (and keep them in finally so an escaping exception can't leave stale context shadowing self.hspeed/vspeed for every subsequent event in the frame).
  - **Verifier:** In execute_collision_event the finally block calls _process_deferred_create_events() at depth 0 (:4609-4615) while self._collision_other/_collision_speeds are still set — cleanup is after the try/finally (:4617-4619). Instances created mid-collision always defer their create event (_event_depth > 0), and _parse_value (:1854-1870) / _evaluate_expression (:1961-1966) prefer collision_speeds over the instance's own hspeed/vspeed/direction, so the spawned instance's create event reads the colliding pair's captured speeds. Low: only affects speed/direction/other.* expressions in create events of collision-spawned instances.

- [ ] **L30 — `runtime/game_runner.py:3460` — check_outside_room_events ignores the sprite origin, so outside_room fires late/early by the origin offset**
  - *Found by:* runtime-game-runner; *category:* correctness
  - The outside test treats (instance.x, instance.y) as the sprite's top-left: `instance.x + w < 0` / `instance.x > room_width`. But everywhere else in the file the sprite's actual screen extent is `x - origin_x .. x - origin_x + w` (rendering at line 760, bbox at line 3781). For a sprite with a nonzero origin (e.g., GMK-imported sprites with a centered origin of 16 on a 32px sprite), the instance is visually fully off the left edge when x < origin_x - w, but the event only fires once x < -w — origin_x pixels too late; on the right edge it fires origin_x pixels too early (while the sprite is still partially visible).
  - **Failure:** A scrolling shooter destroys bullets with an outside_room event, or wraps the player to the opposite edge (asteroid-style). With a center-origin 32px sprite, the right-edge wrap triggers while half the ship is still visible (visible pop), and the left-edge wrap triggers 16px after the ship has fully disappeared (perceptible dead gap), making edge-wrapping look broken compared to the same project in GameMaker.
  - **Suggested fix:** Account for the origin (and ideally the collision bbox) when computing the extent: `left = instance.x - (instance.sprite.origin_x if instance.sprite else 0); outside = left + w < 0 or left > room_width or ...` (same for y).
  - **Verifier:** Traced: check_outside_room_events (:3460-3465) uses instance.x + w < 0 / instance.x > room_width with no origin term, while rendering uses x - origin_x (:760), so the event fires origin_x px late on the left/top and early on the right/bottom for nonzero-origin sprites. Only affects sprites with nonzero origins (mainly GMK imports; the bundled samples all use origin 0), so low.

- [ ] **L31 — `runtime/thymio_action_handlers.py:282` — Thymio handlers' hasattr guard never fires on game instances (thymio_simulator is always defined, possibly None), so conditionals leak None instead of returning False**
  - *Found by:* runtime-support; *category:* correctness
  - Every Thymio handler guards with `if not hasattr(instance, 'thymio_simulator'): return ...`, but GameInstance unconditionally defines the attribute as None (runtime/game_runner.py:535: `self.thymio_simulator = None  # Thymio simulator (set for Thymio instances)`); the simulator is only attached when the object name starts with 'thymio' or instance_data has is_thymio (game_runner.py:1244-1252). For any other instance the guard passes, the handler dereferences None (`instance.thymio_simulator.get_button(...)`) and raises AttributeError, which execute_action's `except AttributeError` swallows and converts to a return of None. For the condition handlers (thymio_if_proximity/ground_dark/ground_light/button_pressed/button_released, lines 237-294) the authored fallback `return False` is therefore never reached: None means 'not a conditional' to the flow control, so in a flat action list the following action is NOT skipped (condition behaves as always-true), and with dialog-authored then_actions/else_actions neither branch runs (the else branch that should run never does). Motor/LED actions silently no-op via the same swallowed exception, which matches the guard's intent only by accident.
  - **Failure:** A student's robot object is named 'MonRobot' (no 'thymio' prefix), so no simulator is attached. Their step event 'if ground dark -> next_room' (flat GM80 form, e.g. via the Thymio events panel) fires next_room unconditionally on the first frame because the condition evaluates to None instead of False — the game instantly skips rooms even though the sensor was never dark.
  - **Suggested fix:** Replace the guard in all 24 handlers with `sim = getattr(instance, 'thymio_simulator', None); if sim is None: return False` (conditionals) / `return` (commands), then use `sim` instead of re-reading the attribute.
  - **Verifier:** GameInstance.__init__ sets self.thymio_simulator = None (game_runner.py:535) and the simulator is only attached when the object name starts with 'thymio' or instance_data['is_thymio'] (lines 1244-52), so the hasattr guard in every Thymio handler is always True; the None dereference raises AttributeError, which execute_action (action_executor.py:446-449) swallows and converts to None, and execute_action_list (line 266) only skips on 'result is False' — so flat-form Thymio conditions act as always-true and nested then/else branches both skip. Low because it requires authoring Thymio condition actions on a non-Thymio-named object.

- [ ] **L32 — `widgets/asset_tree/asset_operations.py:408` — Sprite-delete reference cleanup is immediately undone: objects/<obj>.json keeps the stale sprite name and overrides the cleanup on the forced reload**
  - *Found by:* widgets-assets; *category:* correctness
  - When deleting a sprite, remove_asset_from_project clears `obj_data['sprite'] = ''` in the disk-loaded project.json copy (lines 408-416) and in assets_cache (lines 432-436), then saves project.json. But it never touches the per-object files objects/<obj>.json, which every real project has (samples all carry an objects/ dir; _save_objects_to_files writes them on every save). Step 3 of delete_asset then calls tree.force_project_refresh() -> ProjectManager.load_project(), and _load_objects_from_files / merge_object_file (utils/project_file_merge.py:58, 'sprite' is in _OBJECT_FILE_KEYS, file takes precedence) copies the STALE sprite name from objects/<obj>.json back over the cleaned project.json data; load_assets_from_project_data then rebuilds the cache from that merged data, wiping the cache cleanup too. The next regular save persists the stale reference back into project.json as well. Net: the explicit 'clear sprite references' feature never sticks for any project using external object files (i.e., all of them).
  - **Failure:** A teacher deletes sprite 'spr_old' used by obj_player. The IDE's cleanup runs, but after the automatic refresh obj_player again references the now-deleted 'spr_old' (its PNG is gone). The object renders as missing-sprite in the room editor and at runtime, and every subsequent save re-persists the dangling reference, even though the code clearly intended to clear it.
  - **Suggested fix:** After clearing references, perform a full project save through project_manager (so _save_objects_to_files rewrites objects/*.json from the cleaned data) before force_project_refresh — or update/rewrite each affected objects/<obj>.json directly alongside the project.json edit.
  - **Verifier:** Traced: the cleanup touches only the disk project.json copy and assets_cache (asset_operations.py:408-436), never objects/<obj>.json; the forced reload's merge_object_file ('sprite' in _OBJECT_FILE_KEYS, file precedence) restores the stale name and load_assets_from_project_data rebuilds the cache from it, so the cleanup never sticks for any project with an objects/ dir (all samples have one). Downgraded to low because the observable outcome (missing-sprite render) is nearly identical to the intended cleared reference — the impact is a confusing dangling name, not new breakage.

- [ ] **L33 — `widgets/asset_tree/asset_tree_item.py:71` — Asset tree items hardcode black foreground, making imported assets unreadable under the dark theme**
  - *Found by:* widgets-assets; *category:* other
  - setup_item forces `setForeground(0, Qt.GlobalColor.black)` for every imported asset instead of leaving the palette's text role. The app ships a theme manager with dark themes (utils/theme_manager.py / themes.json), and other widgets in this same module explicitly use palette roles 'so the hint dims correctly under both light and dark themes' (asset_tree_widget.paintEvent). Under a dark theme the tree background is dark, so every imported asset's label becomes black-on-dark.
  - **Failure:** A user switches the IDE to a dark theme: all imported assets in the tree render as black text on a dark background and are effectively invisible, while not-imported assets (gray) remain readable.
  - **Suggested fix:** Remove the explicit black setForeground (the default palette text color is correct), or use the palette's WindowText/Text role instead of Qt.GlobalColor.black.
  - **Verifier:** asset_tree_item.py:71 hardcodes setForeground(0, Qt.GlobalColor.black) for every imported asset; utils/themes.json ships a Dark theme (tree_background #1e1e1e) selectable in preferences_dialog.py:126, and an item's explicit ForegroundRole overrides the theme palette, producing black-on-dark labels.

- [ ] **L34 — `widgets/asset_tree/asset_utils.py:137` — save_project_data rewrites project.json non-atomically, unlike every other save path in the project**
  - *Found by:* widgets-assets; *category:* data-integrity
  - asset_utils.save_project_data opens project.json with 'w' and json.dump's directly into it. A crash, power loss, or disk-full error mid-write leaves project.json truncated/corrupt (the 'w' open already destroyed the previous content). This is the write used by the asset tree's delete (remove_asset_from_project) and duplicate (duplicate_asset) operations. The rest of the codebase deliberately uses _atomic_write_json (tmp file + os.replace) plus a cross-file rollback snapshot in project_manager precisely to prevent this failure mode, so these two operations are the remaining unprotected writers of the project's most critical file.
  - **Failure:** A student deletes a sprite on a nearly-full disk (or the machine loses power mid-write). save_project_data's json.dump fails partway after truncating project.json; the project no longer loads ('Invalid project file format' / JSON decode error) and, because the asset's PNG was already unlinked before the JSON write, the file is gone too.
  - **Suggested fix:** Route save_project_data through core.project_manager._atomic_write_json (or replicate the tmp-file + os.replace pattern); also defer the asset-file unlink in remove_asset_from_project until after the project.json write succeeds.
  - **Verifier:** Verified: asset_utils.py:137-138 opens project.json with 'w' and json.dump's directly (truncate-then-write, no tmp+os.replace), unlike _atomic_write_json used by project_manager. It is the writer for remove_asset_from_project (which unlinks the asset's PNG at asset_operations.py:388-401 BEFORE the JSON write at :439) and duplicate_asset/save_asset_to_project. Real but only manifests on crash/power-loss/disk-full mid-write — low is the right grade.

- [ ] **L35 — `widgets/tutorial_panel.py:111` — TutorialPanel leaves QTextBrowser.openLinks at default True, so clicking an external link blanks the tutorial page**
  - *Found by:* widgets-misc-config-main; *category:* correctness
  - setup_ui calls `self.content_browser.setOpenExternalLinks(False)` and connects anchorClicked to a custom handler (on_link_clicked), but never calls `setOpenLinks(False)`. In Qt, with openExternalLinks=False and openLinks=True (the default), QTextBrowser emits anchorClicked and THEN calls setSource(url) itself. For an http(s) link, on_link_clicked correctly opens the system browser via QDesktopServices, but the subsequent internal setSource('https://...') cannot load a remote document: Qt warns 'QTextBrowser: No document for https://...' and replaces the displayed tutorial page with an empty document. Empirically verified on PySide6: openLinks() returns True by default, and setSource(QUrl('https://example.com')) turns a populated browser's toPlainText() into ''. Internal .html links are also double-handled: after the handler's setHtml (which applies the monospace-font substitution), the automatic setSource reloads the raw file via the configured search paths, silently discarding the font fix.
  - **Failure:** A teacher authors a custom tutorial page containing a link to external documentation (the handler explicitly supports this case). A student clicks the link: the web browser opens, but the tutorial pane in the IDE simultaneously goes blank; the page indicator still says e.g. 'Page 2 of 5' over empty content, and the student must click Previous/Next or Back to List to recover the page.
  - **Suggested fix:** Add `self.content_browser.setOpenLinks(False)` next to setOpenExternalLinks(False) so on_link_clicked is the single navigation authority (this is the documented Qt pattern when handling anchorClicked manually).
  - **Verifier:** Reproduced on PySide6 6.9.2 offscreen: openLinks() defaults True, and clicking an https anchor in a QTextBrowser with setOpenExternalLinks(False) both emitted anchorClicked AND internally called setSource, printing 'QTextBrowser: No document for https://...' and blanking toPlainText() to ''. tutorial_panel.py:110-112 never calls setOpenLinks(False), and on_link_clicked (line 347) explicitly supports http(s) links, so a tutorial with an external link blanks the pane on click. Low: only tutorials containing external links, recoverable via Previous/Next.
