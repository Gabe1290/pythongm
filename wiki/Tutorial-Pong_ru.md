# Учебник: Создание классической игры Pong

> **Выберите язык / Choisissez votre langue / Wählen Sie Ihre Sprache:**
>
> [English](Tutorial-Pong) | [Français](Tutorial-Pong_fr) | [Deutsch](Tutorial-Pong_de) | [Italiano](Tutorial-Pong_it) | [Español](Tutorial-Pong_es) | [Português](Tutorial-Pong_pt) | [Slovenščina](Tutorial-Pong_sl) | [Українська](Tutorial-Pong_uk) | [Русский](Tutorial-Pong_ru)

---

## Введение

В этом учебнике вы создадите классическую игру **Pong** — одну из первых видеоигр! Pong — это игра для двух игроков, где каждый игрок управляет ракеткой и старается забить мяч мимо ракетки противника, чтобы заработать очки.

**Что вы научитесь:**
- Создавать спрайты для ракеток, мяча и стен
- Обрабатывать ввод с клавиатуры для двух игроков
- Заставлять объекты отскакивать друг от друга
- Отслеживать и отображать счёт обоих игроков
- Использовать глобальные переменные

**Сложность:** Начинающий
**Пресет:** Пресет для начинающих

---

## Шаг 1: Спланируйте вашу игру

Перед началом давайте разберёмся, что нам нужно:

| Элемент | Назначение |
|---------|---------|
| **Мяч** | Отскакивает между игроками |
| **Левая ракетка** | Игрок 1 управляет клавишами W/S |
| **Правая ракетка** | Игрок 2 управляет стрелками вверх/вниз |
| **Стены** | Верхние и нижние границы поля |
| **Зоны гола** | Невидимые области за каждой ракеткой для обнаружения забитых голов |
| **Отображение счёта** | Показывает счёт обоих игроков |

---

## Шаг 2: Создайте спрайты

### 2.1 Спрайт мяча

1. В **Дереве ресурсов** щёлкните правой кнопкой мыши на **Sprites** и выберите **Create Sprite**
2. Назовите его `spr_ball`
3. Нажмите **Edit Sprite**, чтобы открыть редактор спрайтов
4. Нарисуйте маленький белый круг (примерно 16x16 пикселей)
5. Нажмите **OK** для сохранения

### 2.2 Спрайты ракеток

Мы создадим две ракетки — по одной для каждого игрока:

**Левая ракетка (Игрок 1):**
1. Создайте новый спрайт с именем `spr_paddle_left`
2. Нарисуйте высокий тонкий прямоугольник, изогнутый как скобка ")" — рекомендуется синий цвет
3. Размер: примерно 16x64 пикселей

**Правая ракетка (Игрок 2):**
1. Создайте новый спрайт с именем `spr_paddle_right`
2. Нарисуйте высокий тонкий прямоугольник, изогнутый как скобка "(" — рекомендуется красный цвет
3. Размер: примерно 16x64 пикселей

### 2.3 Спрайт стены

1. Создайте новый спрайт с именем `spr_wall`
2. Нарисуйте сплошной прямоугольник (серый или белый)
3. Размер: 32x32 пикселя (мы растянем его в комнате)

### 2.4 Спрайт зоны гола (невидимый)

1. Создайте новый спрайт с именем `spr_goal`
2. Размер: 32x32 пикселя
3. Оставьте его прозрачным или сделайте сплошным цветом (он будет невидимым в игре)

---

## Шаг 3: Создайте объект стены

Объект стены создаёт границы в верхней и нижней части игрового поля.

1. Щёлкните правой кнопкой мыши на **Objects** и выберите **Create Object**
2. Назовите его `obj_wall`
3. Установите спрайт на `spr_wall`
4. **Отметьте флажок "Solid"** — это важно для отскока!
5. События не требуются — стена просто стоит на месте

---

## Шаг 4: Создайте объекты ракеток

### 4.1 Левая ракетка (Игрок 1)

1. Создайте новый объект с именем `obj_paddle_left`
2. Установите спрайт на `spr_paddle_left`
3. **Отметьте флажок "Solid"**

**Добавьте события клавиатуры для движения:**

**Событие: Key Press W**
1. Add Event → Keyboard → Key Press W
2. Add Action: **Move** → **Set Vertical Speed**
3. Установите вертикальную скорость на `-8` (движение вверх)

**Событие: Key Release W**
1. Add Event → Keyboard → Key Release W
2. Add Action: **Move** → **Set Vertical Speed**
3. Установите вертикальную скорость на `0` (остановка)

**Событие: Key Press S**
1. Add Event → Keyboard → Key Press S
2. Add Action: **Move** → **Set Vertical Speed**
3. Установите вертикальную скорость на `8` (движение вниз)

**Событие: Key Release S**
1. Add Event → Keyboard → Key Release S
2. Add Action: **Move** → **Set Vertical Speed**
3. Установите вертикальную скорость на `0` (остановка)

**Событие: Collision with obj_wall**
1. Add Event → Collision → obj_wall
2. Add Action: **Move** → **Bounce** (настройка не требуется —
   всегда отскакивает от твёрдых объектов)

### 4.2 Правая ракетка (Игрок 2)

1. Создайте новый объект с именем `obj_paddle_right`
2. Установите спрайт на `spr_paddle_right`
3. **Отметьте флажок "Solid"**

**Добавьте события клавиатуры для движения:**

**Событие: Key Press Up Arrow**
1. Add Event → Keyboard → Key Press Up
2. Add Action: **Move** → **Set Vertical Speed**
3. Установите вертикальную скорость на `-8`

**Событие: Key Release Up Arrow**
1. Add Event → Keyboard → Key Release Up
2. Add Action: **Move** → **Set Vertical Speed**
3. Установите вертикальную скорость на `0`

**Событие: Key Press Down Arrow**
1. Add Event → Keyboard → Key Press Down
2. Add Action: **Move** → **Set Vertical Speed**
3. Установите вертикальную скорость на `8`

**Событие: Key Release Down Arrow**
1. Add Event → Keyboard → Key Release Down
2. Add Action: **Move** → **Set Vertical Speed**
3. Установите вертикальную скорость на `0`

**Событие: Collision with obj_wall**
1. Add Event → Collision → obj_wall
2. Add Action: **Move** → **Bounce** (настройка не требуется —
   всегда отскакивает от твёрдых объектов)

---

## Шаг 5: Создайте объект мяча

1. Создайте новый объект с именем `obj_ball`
2. Установите спрайт на `spr_ball`

**Событие: Create**
1. Add Event → Create
2. Add Action: **Move** → **Start Moving Direction**
3. Выберите диагональную клетку в 3x3-селекторе направления (не прямо вверх или вниз)
4. Установите скорость на `6`

**Событие: Collision with obj_paddle_left**
1. Add Event → Collision → obj_paddle_left
2. Add Action: **Move** → **Bounce** (настройка не требуется —
   всегда отскакивает от твёрдых объектов)

**Событие: Collision with obj_paddle_right**
1. Add Event → Collision → obj_paddle_right
2. Add Action: **Move** → **Bounce** (настройка не требуется —
   всегда отскакивает от твёрдых объектов)

**Событие: Collision with obj_wall**
1. Add Event → Collision → obj_wall
2. Add Action: **Move** → **Bounce** (настройка не требуется —
   всегда отскакивает от твёрдых объектов)

---

## Шаг 6: Создайте объекты зон голов

Зоны голов — это невидимые области за каждой ракеткой. Когда мяч попадает в зону гола, противоположный игрок забивает.

### 6.1 Левая зона гола

1. Создайте новый объект с именем `obj_goal_left`
2. Установите спрайт на `spr_goal`
3. **Снимите флажок "Visible"** — зона гола должна быть невидимой
4. **Отметьте флажок "Solid"**

### 6.2 Правая зона гола

1. Создайте новый объект с именем `obj_goal_right`
2. Установите спрайт на `spr_goal`
3. **Снимите флажок "Visible"**
4. **Отметьте флажок "Solid"**

### 6.3 Добавьте события столкновения мяча с зонами голов

`global.p1score`/`global.p2score` — это собственные переменные, по
одной для каждого игрока — встроенное действие **Set Score** здесь не
подходит, так как оно всегда записывает единственный встроенный счёт
и не имеет способа указать на название переменной. **Set Variable** —
правильное действие для собственной переменной, и оно поддерживает
область видимости `global`:

Вернитесь к `obj_ball` и добавьте эти события:

**Событие: Collision with obj_goal_left**
1. Add Event → Collision → obj_goal_left
2. Add Action: **Move** → **Jump to Start Position** (сбрасывает мяч)
3. Add Action: **Control** → **Set Variable**
   - Variable: `p2score`
   - Value: `1`
   - Scope: `global`
   - Отметьте "Relative" (добавляет 1 к текущему счёту)

**Событие: Collision with obj_goal_right**
1. Add Event → Collision → obj_goal_right
2. Add Action: **Move** → **Jump to Start Position**
3. Add Action: **Control** → **Set Variable**
   - Variable: `p1score`
   - Value: `1`
   - Scope: `global`
   - Отметьте "Relative"

---

## Шаг 7: Создайте объект отображения счёта

1. Создайте новый объект с именем `obj_score`
2. Спрайт не требуется

**Событие: Create**
1. Add Event → Create
2. Add Action: **Control** → **Set Variable**
   - Variable: `p1score`, Value: `0`, Scope: `global`
3. Add Action: **Control** → **Set Variable**
   - Variable: `p2score`, Value: `0`, Scope: `global`

**Событие: Draw**
1. Add Event → Draw
2. Add Action: **Draw** → **Draw Text**
   - Text: `Игрок 1:`
   - X: `10`
   - Y: `10`
3. Add Action: **Draw** → **Draw Variable**
   - Variable: `global.p1score`
   - X: `100`
   - Y: `10`
4. Add Action: **Draw** → **Draw Text**
   - Text: `Игрок 2:`
   - X: `10`
   - Y: `30`
5. Add Action: **Draw** → **Draw Variable**
   - Variable: `global.p2score`
   - X: `100`
   - Y: `30`

---

## Шаг 8: Спроектируйте комнату

1. Щёлкните правой кнопкой мыши на **Rooms** и выберите **Create Room**
2. Назовите её `room_pong`
3. Установите размер комнаты (например, 640x480)

**Расположите объекты:**

1. **Стены**: Поместите экземпляры `obj_wall` вдоль верхнего и нижнего краёв комнаты
2. **Левая ракетка**: Поместите `obj_paddle_left` рядом с левым краем, по центру по вертикали
3. **Правая ракетка**: Поместите `obj_paddle_right` рядом с правым краем, по центру по вертикали
4. **Мяч**: Поместите `obj_ball` в центр комнаты
5. **Зоны голов:**
   - Поместите экземпляры `obj_goal_left` вдоль левого края (позади того места, где расположена ракетка)
   - Поместите экземпляры `obj_goal_right` вдоль правого края
6. **Отображение счёта**: Поместите `obj_score` где-нибудь (у него нет спрайта, он просто рисует текст)

**Пример макета комнаты:**
```
[СТЕНА СТЕНА СТЕНА СТЕНА СТЕНА СТЕНА СТЕНА СТЕНА СТЕНА СТЕНА]
[ГОЛ]  [РАКЕТКА_Л]            [МЯЧ]            [РАКЕТКА_П]  [ГОЛ]
[ГОЛ]  [РАКЕТКА_Л]                             [РАКЕТКА_П]  [ГОЛ]
[ГОЛ]                                                       [ГОЛ]
[СТЕНА СТЕНА СТЕНА СТЕНА СТЕНА СТЕНА СТЕНА СТЕНА СТЕНА СТЕНА]
```

---

## Шаг 9: Протестируйте вашу игру!

1. Нажмите **Run** или нажмите **F5** для тестирования игры
2. Игрок 1 использует **W** (вверх) и **S** (вниз)
3. Игрок 2 использует **стрелку вверх** и **стрелку вниз**
4. Попробуйте забить мяч мимо ракетки противника!

---

## Улучшения (Необязательно)

### Увеличение скорости
Сделайте мяч быстрее каждый раз, когда он ударяется о ракетку.
`speed` здесь не подходит напрямую — в этом движке это скорость
*анимации* спрайта, а не скорость движения — поэтому скорость мяча
отслеживаем в собственной переменной:

1. В событии **Create** для `obj_ball` добавьте **Control** → **Set
   Variable** (Variable: `ball_speed`, Value: `6`) сразу после
   действия Start Moving Direction.
2. В каждом событии столкновения с ракеткой, после действия
   **Bounce**, добавьте **Control** → **Set Variable** (Variable:
   `ball_speed`, Value: `0.5`, отмечено "Relative"), затем **Move** →
   **Set Speed** со значением `self.ball_speed`, чтобы применить
   новую скорость, сохраняя текущее направление (Bounce уже его изменил).

### Звуковые эффекты
Добавьте звуки когда:
- Мяч ударяется о ракетку
- Мяч ударяется о стену
- Игрок забивает

### Условие победы
Добавьте проверку в событие Draw:
- Если `global.p1score >= 10`, отобразите "Игрок 1 победил!"
- Если `global.p2score >= 10`, отобразите "Игрок 2 победил!"

---

## Устранение проблем

| Проблема | Решение |
|---------|----------|
| Мяч проходит сквозь ракетку | Убедитесь, что ракетки имеют отмеченный флажок "Solid" |
| Ракетка не останавливается у стен | Добавьте событие столкновения с obj_wall |
| Счёт не обновляется | Проверьте, что названия переменных совпадают точно (p1score, p2score) |
| Мяч не движется | Проверьте, что событие Create содержит действие движения |

---

## Что вы научились

Поздравляем! Вы создали полную игру Pong для двух игроков! Вы научились:

- Обрабатывать ввод с клавиатуры для двух разных игроков
- Использовать события отпускания клавиш для остановки движения
- Заставлять объекты отскакивать друг от друга
- Использовать глобальные переменные для отслеживания счёта
- Отображать текст и переменные на экране

---

## Смотрите также

- [Пресет для Начинающих](Beginner-Preset_ru) — Обзор возможностей для начинающих
- [Учебник: Breakout](Tutorial-Breakout_ru) — Создание игры в стиле разбиватель кирпичей
- [Справочник событий](Event-Reference_ru) — Полная документация по событиям
- [Полный справочник действий](Full-Action-Reference_ru) — Все доступные действия
